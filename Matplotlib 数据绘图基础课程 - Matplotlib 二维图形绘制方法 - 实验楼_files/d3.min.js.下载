!(function() {
  function id(a) {
    return a && (a.ownerDocument || a.document || a).documentElement
  }
  function h2(a) {
    return (
      a &&
      ((a.ownerDocument && a.ownerDocument.defaultView) ||
        (a.document && a) ||
        a.defaultView)
    )
  }
  function iE(b, a) {
    return a > b ? -1 : b > a ? 1 : b >= a ? 0 : NaN
  }
  function h5(a) {
    return null === a ? NaN : +a
  }
  function h0(a) {
    return !isNaN(a)
  }
  function iw(a) {
    return {
      left: function(d, g, f, b) {
        for (
          arguments.length < 3 && (f = 0),
            arguments.length < 4 && (b = d.length);
          b > f;

        ) {
          var c = (f + b) >>> 1
          a(d[c], g) < 0 ? (f = c + 1) : (b = c)
        }
        return f
      },
      right: function(d, g, f, b) {
        for (
          arguments.length < 3 && (f = 0),
            arguments.length < 4 && (b = d.length);
          b > f;

        ) {
          var c = (f + b) >>> 1
          a(d[c], g) > 0 ? (b = c) : (f = c + 1)
        }
        return f
      },
    }
  }
  function iK(a) {
    return a.length
  }
  function h9(b) {
    for (var a = 1; (b * a) % 1; ) {
      a *= 10
    }
    return a
  }
  function ik(c, a) {
    for (var b in a) {
      Object.defineProperty(c.prototype, b, { value: a[b], enumerable: !1 })
    }
  }
  function iG() {
    this._ = Object.create(null)
  }
  function h3(a) {
    return (a += '') === aA || a[0] === jy ? jy + a : a
  }
  function iB(a) {
    return (a += '')[0] === jy ? a.slice(1) : a
  }
  function iy(a) {
    return h3(a) in this._
  }
  function iz(a) {
    return (a = h3(a)) in this._ && delete this._[a]
  }
  function h8() {
    var b = []
    for (var a in this._) {
      b.push(iB(a))
    }
    return b
  }
  function hY() {
    var b = 0
    for (var a in this._) {
      ++b
    }
    return b
  }
  function iF() {
    for (var a in this._) {
      return !1
    }
    return !0
  }
  function ih() {
    this._ = Object.create(null)
  }
  function hV(a) {
    return a
  }
  function i9(c, a, b) {
    return function() {
      var d = b.apply(a, arguments)
      return d === a ? c : d
    }
  }
  function hW(f, b) {
    if (b in f) {
      return b
    }
    b = b.charAt(0).toUpperCase() + b.slice(1)
    for (var d = 0, c = aG.length; c > d; ++d) {
      var a = aG[d] + b
      if (a in f) {
        return a
      }
    }
  }
  function iJ() {}
  function iL() {}
  function hX(d) {
    function a() {
      for (var g, h = c, e = -1, f = h.length; ++e < f; ) {
        ;(g = h[e].on) && g.apply(this, arguments)
      }
      return d
    }
    var c = [],
      b = new iG()
    return (
      (a.on = function(h, f) {
        var g,
          e = b.get(h)
        return arguments.length < 2
          ? e && e.on
          : (e &&
              ((e.on = null),
              (c = c.slice(0, (g = c.indexOf(e))).concat(c.slice(g + 1))),
              b.remove(h)),
            f && c.push(b.set(h, { on: f })),
            d)
      }),
      a
    )
  }
  function iY() {
    dU.event.preventDefault()
  }
  function im() {
    for (var b, a = dU.event; (b = a.sourceEvent); ) {
      a = b
    }
    return a
  }
  function i7(d) {
    for (var a = new iL(), c = 0, b = arguments.length; ++c < b; ) {
      a[arguments[c]] = hX(a)
    }
    return (
      (a.of = function(g, f) {
        return function(e) {
          try {
            var h = (e.sourceEvent = dU.event)
            ;(e.target = d), (dU.event = e), a[e.type].apply(g, f)
          } finally {
            dU.event = h
          }
        }
      }),
      a
    )
  }
  function jw(a) {
    return e9(a, cG), a
  }
  function jC(a) {
    return 'function' == typeof a
      ? a
      : function() {
          return fx(a, this)
        }
  }
  function jA(a) {
    return 'function' == typeof a
      ? a
      : function() {
          return gS(a, this)
        }
  }
  function hT(k, f) {
    function h() {
      this.removeAttribute(k)
    }
    function g() {
      this.removeAttributeNS(k.space, k.local)
    }
    function c() {
      this.setAttribute(k, f)
    }
    function d() {
      this.setAttributeNS(k.space, k.local, f)
    }
    function b() {
      var a = f.apply(this, arguments)
      null == a ? this.removeAttribute(k) : this.setAttribute(k, a)
    }
    function j() {
      var a = f.apply(this, arguments)
      null == a
        ? this.removeAttributeNS(k.space, k.local)
        : this.setAttributeNS(k.space, k.local, a)
    }
    return (
      (k = dU.ns.qualify(k)),
      null == f
        ? k.local
          ? g
          : h
        : 'function' == typeof f
          ? k.local
            ? j
            : b
          : k.local
            ? d
            : c
    )
  }
  function jb(a) {
    return a.trim().replace(/\s+/g, ' ')
  }
  function h6(a) {
    return new RegExp('(?:^|\\s+)' + dU.requote(a) + '(?:\\s+|$)', 'g')
  }
  function iW(a) {
    return (a + '').trim().split(/^|\s+/)
  }
  function iZ(f, b) {
    function d() {
      for (var g = -1; ++g < a; ) {
        f[g](this, b)
      }
    }
    function c() {
      for (var h = -1, g = b.apply(this, arguments); ++h < a; ) {
        f[h](this, g)
      }
    }
    f = iW(f).map(jx)
    var a = f.length
    return 'function' == typeof b ? c : d
  }
  function jx(b) {
    var a = h6(b)
    return function(f, d) {
      if ((c = f.classList)) {
        return d ? c.add(b) : c.remove(b)
      }
      var c = f.getAttribute('class') || ''
      d
        ? ((a.lastIndex = 0),
          a.test(c) || f.setAttribute('class', jb(c + ' ' + b)))
        : f.setAttribute('class', jb(c.replace(a, ' ')))
    }
  }
  function i2(g, c, f) {
    function d() {
      this.style.removeProperty(g)
    }
    function a() {
      this.style.setProperty(g, c, f)
    }
    function b() {
      var e = c.apply(this, arguments)
      null == e ? this.style.removeProperty(g) : this.style.setProperty(g, e, f)
    }
    return null == c ? d : 'function' == typeof c ? b : a
  }
  function iV(f, b) {
    function d() {
      delete this[f]
    }
    function c() {
      this[f] = b
    }
    function a() {
      var g = b.apply(this, arguments)
      null == g ? delete this[f] : (this[f] = g)
    }
    return null == b ? d : 'function' == typeof b ? a : c
  }
  function iv(c) {
    function a() {
      var d = this.ownerDocument,
        f = this.namespaceURI
      return f === bN && d.documentElement.namespaceURI === bN
        ? d.createElement(c)
        : d.createElementNS(f, c)
    }
    function b() {
      return this.ownerDocument.createElementNS(c.space, c.local)
    }
    return 'function' == typeof c ? c : (c = dU.ns.qualify(c)).local ? b : a
  }
  function jv() {
    var a = this.parentNode
    a && a.removeChild(this)
  }
  function jk(a) {
    return { __data__: a }
  }
  function i5(a) {
    return function() {
      return a4(this, a)
    }
  }
  function jj(a) {
    return (
      arguments.length || (a = iE),
      function(b, c) {
        return b && c ? a(b.__data__, c.__data__) : !b - !c
      }
    )
  }
  function iP(k, f) {
    for (var h = 0, g = k.length; g > h; h++) {
      for (var c, d = k[h], b = 0, j = d.length; j > b; b++) {
        ;(c = d[b]) && f(c, b, h)
      }
    }
    return k
  }
  function iO(a) {
    return e9(a, hC), a
  }
  function iT(c) {
    var a, b
    return function(h, f, g) {
      var e,
        j = c[g].update,
        d = j.length
      for (
        g != b && ((b = g), (a = 0)), f >= a && (a = f + 1);
        !(e = j[a]) && ++a < d;

      ) {}
      return e
    }
  }
  function iQ(f, q, j) {
    function b() {
      var a = this[m]
      a && (this.removeEventListener(f, a, a.$), delete this[m])
    }
    function p() {
      var a = g(q, iH(arguments))
      b.call(this),
        this.addEventListener(f, (this[m] = a), (a.$ = j)),
        (a._ = q)
    }
    function h() {
      var c,
        l = new RegExp('^__on([^.]+)' + dU.requote(f) + '$')
      for (var i in this) {
        if ((c = i.match(l))) {
          var a = this[i]
          this.removeEventListener(c[1], a, a.$), delete this[i]
        }
      }
    }
    var m = '__on' + f,
      d = f.indexOf('.'),
      g = j4
    d > 0 && (f = f.slice(0, d))
    var k = dc.get(f)
    return k && ((f = k), (g = jB)), d ? (q ? p : b) : q ? iJ : h
  }
  function j4(b, a) {
    return function(d) {
      var c = dU.event
      ;(dU.event = d), (a[0] = this.__data__)
      try {
        b.apply(this, a)
      } finally {
        dU.event = c
      }
    }
  }
  function jB(c, a) {
    var b = j4(c, a)
    return function(f) {
      var d = this,
        e = f.relatedTarget
      ;(e && (e === d || 8 & e.compareDocumentPosition(d))) || b.call(d, f)
    }
  }
  function iS(g) {
    var f = '.dragsuppress-' + ++fA,
      c = 'click' + f,
      d = dU
        .select(h2(g))
        .on('touchmove' + f, iY)
        .on('dragstart' + f, iY)
        .on('selectstart' + f, iY)
    if (
      (null == eR &&
        (eR = 'onselectstart' in g ? !1 : hW(g.style, 'userSelect')),
      eR)
    ) {
      var b = id(g).style,
        h = b[eR]
      b[eR] = 'none'
    }
    return function(e) {
      if ((d.on(f, null), eR && (b[eR] = h), e)) {
        var a = function() {
          d.on(c, null)
        }
        d.on(
          c,
          function() {
            iY(), a()
          },
          !0
        ),
          setTimeout(a, 0)
      }
    }
  }
  function jg(j, g) {
    g.changedTouches && (g = g.changedTouches[0])
    var f = j.ownerSVGElement || j
    if (f.createSVGPoint) {
      var c = f.createSVGPoint()
      if (0 > bv) {
        var d = h2(j)
        if (d.scrollX || d.scrollY) {
          f = dU
            .select('body')
            .append('svg')
            .style(
              {
                position: 'absolute',
                top: 0,
                left: 0,
                margin: 0,
                padding: 0,
                border: 'none',
              },
              'important'
            )
          var b = f[0][0].getScreenCTM()
          ;(bv = !(b.f || b.e)), f.remove()
        }
      }
      return (
        bv
          ? ((c.x = g.pageX), (c.y = g.pageY))
          : ((c.x = g.clientX), (c.y = g.clientY)),
        (c = c.matrixTransform(j.getScreenCTM().inverse())),
        [c.x, c.y]
      )
    }
    var h = j.getBoundingClientRect()
    return [g.clientX - h.left - j.clientLeft, g.clientY - h.top - j.clientTop]
  }
  function jp() {
    return dU.event.changedTouches[0].identifier
  }
  function jf(a) {
    return a > 0 ? 1 : 0 > a ? -1 : 0
  }
  function i1(c, a, b) {
    return (a[0] - c[0]) * (b[1] - c[1]) - (a[1] - c[1]) * (b[0] - c[0])
  }
  function d9(a) {
    return a > 1 ? 0 : -1 > a ? fP : Math.acos(a)
  }
  function bP(a) {
    return a > 1 ? gA : -1 > a ? -gA : Math.asin(a)
  }
  function hh(a) {
    return ((a = Math.exp(a)) - 1 / a) / 2
  }
  function cI(a) {
    return ((a = Math.exp(a)) + 1 / a) / 2
  }
  function bx(a) {
    return ((a = Math.exp(2 * a)) - 1) / (a + 1)
  }
  function jQ(a) {
    return (a = Math.sin(a / 2)) * a
  }
  function dK() {}
  function eW(c, a, b) {
    return this instanceof eW
      ? ((this.h = +c), (this.s = +a), void (this.l = +b))
      : arguments.length < 2
        ? c instanceof eW
          ? new eW(c.h, c.s, c.l)
          : aq('' + c, aO, eW)
        : new eW(c, a, b)
  }
  function ib(j, f, h) {
    function g(a) {
      return (
        a > 360 ? (a -= 360) : 0 > a && (a += 360),
        60 > a
          ? d + ((b - d) * a) / 60
          : 180 > a
            ? b
            : 240 > a
              ? d + ((b - d) * (240 - a)) / 60
              : d
      )
    }
    function c(a) {
      return Math.round(255 * g(a))
    }
    var d, b
    return (
      (j = isNaN(j) ? 0 : (j %= 360) < 0 ? j + 360 : j),
      (f = isNaN(f) ? 0 : 0 > f ? 0 : f > 1 ? 1 : f),
      (h = 0 > h ? 0 : h > 1 ? 1 : h),
      (b = 0.5 >= h ? h * (1 + f) : h + f - h * f),
      (d = 2 * h - b),
      new kc(c(j + 120), c(j), c(j - 120))
    )
  }
  function b8(c, a, b) {
    return this instanceof b8
      ? ((this.h = +c), (this.c = +a), void (this.l = +b))
      : arguments.length < 2
        ? c instanceof b8
          ? new b8(c.h, c.c, c.l)
          : c instanceof gg
            ? dp(c.l, c.a, c.b)
            : dp((c = e3((c = dU.rgb(c)).r, c.g, c.b)).l, c.a, c.b)
        : new b8(c, a, b)
  }
  function gX(c, a, b) {
    return (
      isNaN(c) && (c = 0),
      isNaN(a) && (a = 0),
      new gg(b, Math.cos((c *= jH)) * a, Math.sin(c) * a)
    )
  }
  function gg(c, a, b) {
    return this instanceof gg
      ? ((this.l = +c), (this.a = +a), void (this.b = +b))
      : arguments.length < 2
        ? c instanceof gg
          ? new gg(c.l, c.a, c.b)
          : c instanceof b8
            ? gX(c.h, c.c, c.l)
            : e3((c = kc(c)).r, c.g, c.b)
        : new gg(c, a, b)
  }
  function gF(g, c, f) {
    var d = (g + 16) / 116,
      a = d + c / 500,
      b = d - f / 200
    return (
      (a = a6(a) * fS),
      (d = a6(d) * d8),
      (b = a6(b) * bO),
      new kc(
        eE(3.2404542 * a - 1.5371385 * d - 0.4985314 * b),
        eE(-0.969266 * a + 1.8760108 * d + 0.041556 * b),
        eE(0.0556434 * a - 0.2040259 * d + 1.0572252 * b)
      )
    )
  }
  function dp(c, a, b) {
    return c > 0
      ? new b8(Math.atan2(b, a) * cW, Math.sqrt(a * a + b * b), c)
      : new b8(NaN, NaN, c)
  }
  function a6(a) {
    return a > 0.206893034 ? a * a * a : (a - 4 / 29) / 7.787037
  }
  function hG(a) {
    return a > 0.008856 ? Math.pow(a, 1 / 3) : 7.787037 * a + 4 / 29
  }
  function eE(a) {
    return Math.round(
      255 * (0.00304 >= a ? 12.92 * a : 1.055 * Math.pow(a, 1 / 2.4) - 0.055)
    )
  }
  function kc(c, a, b) {
    return this instanceof kc
      ? ((this.r = ~~c), (this.g = ~~a), void (this.b = ~~b))
      : arguments.length < 2
        ? c instanceof kc
          ? new kc(c.r, c.g, c.b)
          : aq('' + c, kc, ib)
        : new kc(c, a, b)
  }
  function g4(a) {
    return new kc(a >> 16, (a >> 8) & 255, 255 & a)
  }
  function af(a) {
    return g4(a) + ''
  }
  function i3(a) {
    return 16 > a
      ? '0' + Math.max(0, a).toString(16)
      : Math.min(255, a).toString(16)
  }
  function aq(d, m, h) {
    var b,
      k,
      g,
      j = 0,
      c = 0,
      f = 0
    if ((b = /([a-z]+)\((.*)\)/.exec((d = d.toLowerCase())))) {
      switch (((k = b[2].split(',')), b[1])) {
        case 'hsl':
          return h(
            parseFloat(k[0]),
            parseFloat(k[1]) / 100,
            parseFloat(k[2]) / 100
          )
        case 'rgb':
          return m(gM(k[0]), gM(k[1]), gM(k[2]))
      }
    }
    return (g = bw.get(d))
      ? m(g.r, g.g, g.b)
      : (null == d ||
          '#' !== d.charAt(0) ||
          isNaN((g = parseInt(d.slice(1), 16))) ||
          (4 === d.length
            ? ((j = (3840 & g) >> 4),
              (j = (j >> 4) | j),
              (c = 240 & g),
              (c = (c >> 4) | c),
              (f = 15 & g),
              (f = (f << 4) | f))
            : 7 === d.length &&
              ((j = (16711680 & g) >> 16),
              (c = (65280 & g) >> 8),
              (f = 255 & g))),
        m(j, c, f))
  }
  function aO(d, m, h) {
    var b,
      k,
      g = Math.min((d /= 255), (m /= 255), (h /= 255)),
      j = Math.max(d, m, h),
      c = j - g,
      f = (j + g) / 2
    return (
      c
        ? ((k = 0.5 > f ? c / (j + g) : c / (2 - j - g)),
          (b =
            d == j
              ? (m - h) / c + (h > m ? 6 : 0)
              : m == j
                ? (h - d) / c + 2
                : (d - m) / c + 4),
          (b *= 60))
        : ((b = NaN), (k = f > 0 && 1 > f ? 0 : b)),
      new eW(b, k, f)
    )
  }
  function e3(g, c, f) {
    ;(g = fg(g)), (c = fg(c)), (f = fg(f))
    var d = hG((0.4124564 * g + 0.3575761 * c + 0.1804375 * f) / fS),
      a = hG((0.2126729 * g + 0.7151522 * c + 0.072175 * f) / d8),
      b = hG((0.0193339 * g + 0.119192 * c + 0.9503041 * f) / bO)
    return gg(116 * a - 16, 500 * (d - a), 200 * (a - b))
  }
  function fg(a) {
    return (a /= 255) <= 0.04045
      ? a / 12.92
      : Math.pow((a + 0.055) / 1.055, 2.4)
  }
  function gM(b) {
    var a = parseFloat(b)
    return '%' === b.charAt(b.length - 1) ? Math.round(2.55 * a) : a
  }
  function aX(a) {
    return 'function' == typeof a
      ? a
      : function() {
          return a
        }
  }
  function cx(a) {
    return function(b, d, c) {
      return (
        2 === arguments.length &&
          'function' == typeof d &&
          ((c = d), (d = null)),
        bG(b, d, a, c)
      )
    }
  }
  function bG(f, q, j, b) {
    function p() {
      var e,
        a = g.status
      if ((!a && hv(g)) || (a >= 200 && 300 > a) || 304 === a) {
        try {
          e = j.call(h, g)
        } catch (c) {
          return void m.error.call(h, c)
        }
        m.load.call(h, e)
      } else {
        m.error.call(h, g)
      }
    }
    var h = {},
      m = dU.dispatch('beforesend', 'progress', 'load', 'error'),
      d = {},
      g = new XMLHttpRequest(),
      k = null
    return (
      !this.XDomainRequest ||
        'withCredentials' in g ||
        !/^(http(s)?:)?\/\//.test(f) ||
        (g = new XDomainRequest()),
      'onload' in g
        ? (g.onload = g.onerror = p)
        : (g.onreadystatechange = function() {
            g.readyState > 3 && p()
          }),
      (g.onprogress = function(c) {
        var a = dU.event
        dU.event = c
        try {
          m.progress.call(h, g)
        } finally {
          dU.event = a
        }
      }),
      (h.header = function(c, a) {
        return (
          (c = (c + '').toLowerCase()),
          arguments.length < 2
            ? d[c]
            : (null == a ? delete d[c] : (d[c] = a + ''), h)
        )
      }),
      (h.mimeType = function(a) {
        return arguments.length ? ((q = null == a ? null : a + ''), h) : q
      }),
      (h.responseType = function(a) {
        return arguments.length ? ((k = a), h) : k
      }),
      (h.response = function(a) {
        return (j = a), h
      }),
      ['get', 'post'].forEach(function(a) {
        h[a] = function() {
          return h.send.apply(h, [a].concat(iH(arguments)))
        }
      }),
      (h.send = function(l, i, a) {
        if (
          (2 === arguments.length &&
            'function' == typeof i &&
            ((a = i), (i = null)),
          g.open(l, f, !0),
          null == q || 'accept' in d || (d.accept = q + ',*/*'),
          g.setRequestHeader)
        ) {
          for (var c in d) {
            g.setRequestHeader(c, d[c])
          }
        }
        return (
          null != q && g.overrideMimeType && g.overrideMimeType(q),
          null != k && (g.responseType = k),
          null != a &&
            h.on('error', a).on('load', function(e) {
              a(null, e)
            }),
          m.beforesend.call(h, g),
          g.send(null == i ? null : i),
          h
        )
      }),
      (h.abort = function() {
        return g.abort(), h
      }),
      dU.rebind(h, m, 'on'),
      null == b ? h : h.get(jJ(b))
    )
  }
  function jJ(a) {
    return 1 === a.length
      ? function(b, c) {
          a(null == b ? c : null)
        }
      : a
  }
  function hv(b) {
    var a = b.responseType
    return a && 'text' !== a ? b.response : b.responseText
  }
  function c1(g, c, f) {
    var d = arguments.length
    2 > d && (c = 0), 3 > d && (f = Date.now())
    var a = f + c,
      b = { c: g, t: a, n: null }
    return (
      jP ? (jP.n = b) : (fW = b),
      (jP = b),
      dJ || ((eV = clearTimeout(eV)), (dJ = 1), h7(eL)),
      b
    )
  }
  function eL() {
    var b = fs(),
      a = bh() - b
    a > 24
      ? (isFinite(a) && (clearTimeout(eV), (eV = setTimeout(eL, a))), (dJ = 0))
      : ((dJ = 1), h7(eL))
  }
  function fs() {
    for (var b = Date.now(), a = fW; a; ) {
      b >= a.t && a.c(b - a.t) && (a.c = null), (a = a.n)
    }
    return b
  }
  function bh() {
    for (var c, a = fW, b = 1 / 0; a; ) {
      a.c
        ? (a.t < b && (b = a.t), (a = (c = a).n))
        : (a = c ? (c.n = a.n) : (fW = a.n))
    }
    return (jP = c), b
  }
  function f3(b, a) {
    return a - (b ? Math.ceil(Math.log(b) / Math.LN10) : 1)
  }
  function ep(c, a) {
    var b = Math.pow(10, 3 * hb(8 - a))
    return {
      scale:
        a > 8
          ? function(d) {
              return d / b
            }
          : function(d) {
              return d * b
            },
      symbol: c,
    }
  }
  function fF(g) {
    var c = g.decimal,
      f = g.thousands,
      d = g.grouping,
      a = g.currency,
      b =
        d && f
          ? function(q, m) {
              for (
                var j = q.length, k = [], h = 0, p = d[0], e = 0;
                j > 0 &&
                p > 0 &&
                (e + p + 1 > m && (p = Math.max(1, m - e)),
                k.push(q.substring((j -= p), j + p)),
                !((e += p + 1) > m));

              ) {
                p = d[(h = (h + 1) % d.length)]
              }
              return k.reverse().join(f)
            }
          : hV
    return function(q) {
      var A = gW.exec(q),
        i = A[1] || ' ',
        F = A[2] || '>',
        k = A[3] || '-',
        u = A[4] || '',
        D = A[5],
        H = +A[6],
        z = A[7],
        w = A[8],
        x = A[9],
        j = 1,
        G = '',
        C = '',
        t = !1,
        E = !0
      switch (
        (w && (w = +w.substring(1)),
        (D || ('0' === i && '=' === F)) && ((D = i = '0'), (F = '=')),
        x)
      ) {
        case 'n':
          ;(z = !0), (x = 'g')
          break
        case '%':
          ;(j = 100), (C = '%'), (x = 'f')
          break
        case 'p':
          ;(j = 100), (C = '%'), (x = 'r')
          break
        case 'b':
        case 'o':
        case 'x':
        case 'X':
          '#' === u && (G = '0' + x.toLowerCase())
        case 'c':
          E = !1
        case 'd':
          ;(t = !0), (w = 0)
          break
        case 's':
          ;(j = -1), (x = 'r')
      }
      '$' === u && ((G = a[0]), (C = a[1])),
        'r' != x || w || (x = 'g'),
        null != w &&
          ('g' == x
            ? (w = Math.max(1, Math.min(21, w)))
            : ('e' == x || 'f' == x) && (w = Math.max(0, Math.min(20, w)))),
        (x = gf.get(x) || aD)
      var B = D && z
      return function(h) {
        var r = C
        if (t && h % 1) {
          return ''
        }
        var J =
          0 > h || (0 === h && 0 > 1 / h) ? ((h = -h), '-') : '-' === k ? '' : k
        if (0 > j) {
          var m = dU.formatPrefix(h, w)
          ;(h = m.scale(h)), (r = m.symbol + C)
        } else {
          h *= j
        }
        h = x(h, w)
        var v,
          s,
          I = h.lastIndexOf('.')
        if (0 > I) {
          var y = E ? h.lastIndexOf('e') : -1
          0 > y
            ? ((v = h), (s = ''))
            : ((v = h.substring(0, y)), (s = h.substring(y)))
        } else {
          ;(v = h.substring(0, I)), (s = c + h.substring(I + 1))
        }
        !D && z && (v = b(v, 1 / 0))
        var p = G.length + v.length + s.length + (B ? 0 : J.length),
          o = H > p ? new Array((p = H - p + 1)).join(i) : ''
        return (
          B && (v = b(o + v, o.length ? H - s.length : 1 / 0)),
          (J += G),
          (h = v + s),
          ('<' === F
            ? J + h + o
            : '>' === F
              ? o + J + h
              : '^' === F
                ? o.substring(0, (p >>= 1)) + J + h + o.substring(p)
                : J + (B ? h : o + h)) + r
        )
      }
    }
  }
  function aD(a) {
    return a + ''
  }
  function jZ() {
    this._ = new Date(
      arguments.length > 1 ? Date.UTC.apply(this, arguments) : arguments[0]
    )
  }
  function gs(d, m, h) {
    function b(a) {
      var l = d(a),
        i = g(l, 1)
      return i - a > a - l ? l : i
    }
    function k(a) {
      return m((a = d(new dm(a - 1))), 1), a
    }
    function g(i, a) {
      return m((i = new dm(+i)), a), i
    }
    function j(s, p, l) {
      var e = k(s),
        q = []
      if (l > 1) {
        for (; p > e; ) {
          h(e) % l || q.push(new Date(+e)), m(e, 1)
        }
      } else {
        for (; p > e; ) {
          q.push(new Date(+e)), m(e, 1)
        }
      }
      return q
    }
    function c(o, a, l) {
      try {
        dm = jZ
        var i = new jZ()
        return (i._ = o), j(i, a, l)
      } finally {
        dm = Date
      }
    }
    ;(d.floor = d), (d.round = b), (d.ceil = k), (d.offset = g), (d.range = j)
    var f = (d.utc = jm(d))
    return (
      (f.floor = f),
      (f.round = jm(b)),
      (f.ceil = jm(k)),
      (f.offset = jm(g)),
      (f.range = c),
      d
    )
  }
  function jm(a) {
    return function(b, d) {
      try {
        dm = jZ
        var c = new jZ()
        return (c._ = b), a(c, d)._
      } finally {
        dm = Date
      }
    }
  }
  function cP(T) {
    function J(c) {
      function a(k) {
        for (var m, g, h, f = [], n = -1, d = 0; ++n < b; ) {
          37 === c.charCodeAt(n) &&
            (f.push(c.slice(d, n)),
            null != (g = eD[(m = c.charAt(++n))]) && (m = c.charAt(++n)),
            (h = P[m]) && (m = h(k, null == g ? ('e' === m ? ' ' : '0') : g)),
            f.push(m),
            (d = n + 1))
        }
        return f.push(c.slice(d, n)), f.join('')
      }
      var b = c.length
      return (
        (a.parse = function(g) {
          var h = { y: 1900, m: 0, d: 1, H: 0, M: 0, S: 0, L: 0, Z: null },
            e = ae(h, c, g, 0)
          if (e != g.length) {
            return null
          }
          'p' in h && (h.H = (h.H % 12) + 12 * h.p)
          var f = null != h.Z && dm !== jZ,
            d = new (f ? jZ : dm)()
          return (
            'j' in h
              ? d.setFullYear(h.y, 0, h.j)
              : 'W' in h || 'U' in h
                ? ('w' in h || (h.w = 'W' in h ? 1 : 0),
                  d.setFullYear(h.y, 0, 1),
                  d.setFullYear(
                    h.y,
                    0,
                    'W' in h
                      ? ((h.w + 6) % 7) + 7 * h.W - ((d.getDay() + 5) % 7)
                      : h.w + 7 * h.U - ((d.getDay() + 6) % 7)
                  ))
                : d.setFullYear(h.y, h.m, h.d),
            d.setHours(h.H + ((h.Z / 100) | 0), h.M + (h.Z % 100), h.S, h.L),
            f ? d._ : d
          )
        }),
        (a.toString = function() {
          return c
        }),
        a
      )
    }
    function ae(f, v, k, b) {
      for (var s, h, p, d = 0, g = v.length, m = k.length; g > d; ) {
        if (b >= m) {
          return -1
        }
        if (((s = v.charCodeAt(d++)), 37 === s)) {
          if (
            ((p = v.charAt(d++)),
            (h = L[p in eD ? v.charAt(d++) : p]),
            !h || (b = h(f, k, b)) < 0)
          ) {
            return -1
          }
        } else {
          if (s != k.charCodeAt(b++)) {
            return -1
          }
        }
      }
      return b
    }
    function O(d, a, c) {
      ar.lastIndex = 0
      var b = ar.exec(a.slice(c))
      return b ? ((d.w = F.get(b[0].toLowerCase())), c + b[0].length) : -1
    }
    function H(d, a, c) {
      D.lastIndex = 0
      var b = D.exec(a.slice(c))
      return b ? ((d.w = an.get(b[0].toLowerCase())), c + b[0].length) : -1
    }
    function X(d, a, c) {
      q.lastIndex = 0
      var b = q.exec(a.slice(c))
      return b ? ((d.m = I.get(b[0].toLowerCase())), c + b[0].length) : -1
    }
    function ao(d, a, c) {
      j.lastIndex = 0
      var b = j.exec(a.slice(c))
      return b ? ((d.m = W.get(b[0].toLowerCase())), c + b[0].length) : -1
    }
    function R(c, a, b) {
      return ae(c, P.c.toString(), a, b)
    }
    function V(c, a, b) {
      return ae(c, P.x.toString(), a, b)
    }
    function al(c, a, b) {
      return ae(c, P.X.toString(), a, b)
    }
    function K(d, a, c) {
      var b = z.get(a.slice(c, (c += 2)).toLowerCase())
      return null == b ? -1 : ((d.p = b), c)
    }
    var aa = T.dateTime,
      Y = T.date,
      Z = T.time,
      Q = T.periods,
      G = T.days,
      ai = T.shortDays,
      U = T.months,
      B = T.shortMonths
    ;(J.utc = function(c) {
      function b(e) {
        try {
          dm = jZ
          var d = new dm()
          return (d._ = e), a(d)
        } finally {
          dm = Date
        }
      }
      var a = J(c)
      return (
        (b.parse = function(e) {
          try {
            dm = jZ
            var d = a.parse(e)
            return d && d._
          } finally {
            dm = Date
          }
        }),
        (b.toString = a.toString),
        b
      )
    }),
      (J.multi = J.utc.multi = h1)
    var z = dU.map(),
      D = dR(G),
      an = c8(G),
      ar = dR(ai),
      F = c8(ai),
      j = dR(U),
      W = c8(U),
      q = dR(B),
      I = c8(B)
    Q.forEach(function(b, a) {
      z.set(b.toLowerCase(), a)
    })
    var P = {
        a: function(a) {
          return ai[a.getDay()]
        },
        A: function(a) {
          return G[a.getDay()]
        },
        b: function(a) {
          return B[a.getMonth()]
        },
        B: function(a) {
          return U[a.getMonth()]
        },
        c: J(aa),
        d: function(b, a) {
          return ch(b.getDate(), a, 2)
        },
        e: function(b, a) {
          return ch(b.getDate(), a, 2)
        },
        H: function(b, a) {
          return ch(b.getHours(), a, 2)
        },
        I: function(b, a) {
          return ch(b.getHours() % 12 || 12, a, 2)
        },
        j: function(b, a) {
          return ch(1 + gE.dayOfYear(b), a, 3)
        },
        L: function(b, a) {
          return ch(b.getMilliseconds(), a, 3)
        },
        m: function(b, a) {
          return ch(b.getMonth() + 1, a, 2)
        },
        M: function(b, a) {
          return ch(b.getMinutes(), a, 2)
        },
        p: function(a) {
          return Q[+(a.getHours() >= 12)]
        },
        S: function(b, a) {
          return ch(b.getSeconds(), a, 2)
        },
        U: function(b, a) {
          return ch(gE.sundayOfYear(b), a, 2)
        },
        w: function(a) {
          return a.getDay()
        },
        W: function(b, a) {
          return ch(gE.mondayOfYear(b), a, 2)
        },
        x: J(Y),
        X: J(Z),
        y: function(b, a) {
          return ch(b.getFullYear() % 100, a, 2)
        },
        Y: function(b, a) {
          return ch(b.getFullYear() % 10000, a, 4)
        },
        Z: dH,
        '%': function() {
          return '%'
        },
      },
      L = {
        a: O,
        A: H,
        b: X,
        B: ao,
        c: R,
        d: bL,
        e: bL,
        H: cE,
        I: cE,
        j: hd,
        L: jM,
        m: d4,
        M: bq,
        p: K,
        S: fU,
        U: bZ,
        w: d2,
        W: dz,
        x: V,
        X: al,
        y: kq,
        Y: iA,
        Z: hN,
        '%': eT,
      }
    return J
  }
  function ch(g, c, f) {
    var d = 0 > g ? '-' : '',
      a = (d ? -g : g) + '',
      b = a.length
    return d + (f > b ? new Array(f - b + 1).join(c) + a : a)
  }
  function dR(a) {
    return new RegExp('^(?:' + a.map(dU.requote).join('|') + ')', 'i')
  }
  function c8(d) {
    for (var a = new iG(), c = -1, b = d.length; ++c < b; ) {
      a.set(d[c].toLowerCase(), c)
    }
    return a
  }
  function d2(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 1))
    return b ? ((d.w = +b[0]), c + b[0].length) : -1
  }
  function bZ(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c))
    return b ? ((d.U = +b[0]), c + b[0].length) : -1
  }
  function dz(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c))
    return b ? ((d.W = +b[0]), c + b[0].length) : -1
  }
  function iA(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 4))
    return b ? ((d.y = +b[0]), c + b[0].length) : -1
  }
  function kq(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.y = fM(+b[0])), c + b[0].length) : -1
  }
  function hN(c, a, b) {
    return /^[+-]\d{4}$/.test((a = a.slice(b, b + 5)))
      ? ((c.Z = -a), b + 5)
      : -1
  }
  function fM(a) {
    return a + (a > 68 ? 1900 : 2000)
  }
  function d4(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.m = b[0] - 1), c + b[0].length) : -1
  }
  function bL(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.d = +b[0]), c + b[0].length) : -1
  }
  function hd(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 3))
    return b ? ((d.j = +b[0]), c + b[0].length) : -1
  }
  function cE(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.H = +b[0]), c + b[0].length) : -1
  }
  function bq(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.M = +b[0]), c + b[0].length) : -1
  }
  function fU(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 2))
    return b ? ((d.S = +b[0]), c + b[0].length) : -1
  }
  function jM(d, a, c) {
    kb.lastIndex = 0
    var b = kb.exec(a.slice(c, c + 3))
    return b ? ((d.L = +b[0]), c + b[0].length) : -1
  }
  function dH(f) {
    var b = f.getTimezoneOffset(),
      d = b > 0 ? '-' : '+',
      c = (hb(b) / 60) | 0,
      a = hb(b) % 60
    return d + ch(c, '0', 2) + ch(a, '0', 2)
  }
  function eT(d, a, c) {
    g3.lastIndex = 0
    var b = g3.exec(a.slice(c, c + 1))
    return b ? c + b[0].length : -1
  }
  function h1(c) {
    for (var a = c.length, b = -1; ++b < a; ) {
      c[b][0] = this(c[b][0])
    }
    return function(d) {
      for (var g = 0, f = c[g]; !f[1](d); ) {
        f = c[++g]
      }
      return f[0](d)
    }
  }
  function b4() {}
  function gU(g, c, f) {
    var d = (f.s = g + c),
      a = d - g,
      b = d - a
    f.t = g - b + (c - a)
  }
  function gc(b, a) {
    b && aN.hasOwnProperty(b.type) && aN[b.type](b, a)
  }
  function gC(g, c, f) {
    var d,
      a = -1,
      b = g.length - f
    for (c.lineStart(); ++a < b; ) {
      ;(d = g[a]), c.point(d[0], d[1], d[2])
    }
    c.lineEnd()
  }
  function dj(d, a) {
    var c = -1,
      b = d.length
    for (a.polygonStart(); ++c < b; ) {
      gC(d[c], a, 1)
    }
    a.polygonEnd()
  }
  function a2() {
    function g(j, v) {
      ;(j *= jH), (v = (v * jH) / 2 + fP / 4)
      var q = j - d,
        u = q >= 0 ? 1 : -1,
        i = u * q,
        k = Math.cos(v),
        r = Math.sin(v),
        w = b * r,
        p = a * k + w * Math.cos(i),
        m = w * u * Math.sin(i)
      ff.add(Math.atan2(m, p)), (d = j), (a = k), (b = r)
    }
    var c, f, d, a, b
    ;(gL.point = function(e, h) {
      ;(gL.point = g),
        (d = (c = e) * jH),
        (a = Math.cos((h = ((f = h) * jH) / 2 + fP / 4))),
        (b = Math.sin(h))
    }),
      (gL.lineEnd = function() {
        g(c, f)
      })
  }
  function hE(d) {
    var a = d[0],
      c = d[1],
      b = Math.cos(c)
    return [b * Math.cos(a), b * Math.sin(a), Math.sin(c)]
  }
  function eB(b, a) {
    return b[0] * a[0] + b[1] * a[1] + b[2] * a[2]
  }
  function j7(b, a) {
    return [
      b[1] * a[2] - b[2] * a[1],
      b[2] * a[0] - b[0] * a[2],
      b[0] * a[1] - b[1] * a[0],
    ]
  }
  function g0(b, a) {
    ;(b[0] += a[0]), (b[1] += a[1]), (b[2] += a[2])
  }
  function ky(b, a) {
    return [b[0] * a, b[1] * a, b[2] * a]
  }
  function iU(b) {
    var a = Math.sqrt(b[0] * b[0] + b[1] * b[1] + b[2] * b[2])
    ;(b[0] /= a), (b[1] /= a), (b[2] /= a)
  }
  function aj(a) {
    return [Math.atan2(a[1], a[0]), bP(a[2])]
  }
  function aK(b, a) {
    return hb(b[0] - a[0]) < f9 && hb(b[1] - a[1]) < f9
  }
  function eZ(c, a) {
    c *= jH
    var b = Math.cos((a *= jH))
    fc(b * Math.cos(c), b * Math.sin(c), Math.sin(a))
  }
  function fc(c, a, b) {
    ++aW, (bF += (c - bF) / aW), (jI += (a - jI) / aW), (hs += (b - hs) / aW)
  }
  function gI() {
    function d(m, g) {
      m *= jH
      var h = Math.cos((g *= jH)),
        f = h * Math.cos(m),
        j = h * Math.sin(m),
        e = Math.sin(g),
        k = Math.atan2(
          Math.sqrt(
            (k = c * e - b * j) * k +
              (k = b * f - a * e) * k +
              (k = a * j - c * f) * k
          ),
          a * f + c * j + b * e
        )
      ;(cw += k),
        (c0 += k * (a + (a = f))),
        (eK += k * (c + (c = j))),
        (fq += k * (b + (b = e))),
        fc(a, c, b)
    }
    var a, c, b
    fE.point = function(f, g) {
      f *= jH
      var e = Math.cos((g *= jH))
      ;(a = e * Math.cos(f)),
        (c = e * Math.sin(f)),
        (b = Math.sin(g)),
        (fE.point = d),
        fc(a, c, b)
    }
  }
  function aT() {
    fE.point = eZ
  }
  function cp() {
    function g(k, A) {
      k *= jH
      var w = Math.cos((A *= jH)),
        y = w * Math.cos(k),
        j = w * Math.sin(k),
        m = Math.sin(A),
        x = a * m - b * j,
        B = b * y - d * m,
        u = d * j - a * y,
        q = Math.sqrt(x * x + B * B + u * u),
        r = d * y + a * j + b * m,
        i = q && -d9(r) / q,
        z = Math.atan2(q, r)
      ;(bg += i * x),
        (f2 += i * B),
        (em += i * u),
        (cw += z),
        (c0 += z * (d + (d = y))),
        (eK += z * (a + (a = j))),
        (fq += z * (b + (b = m))),
        fc(d, a, b)
    }
    var c, f, d, a, b
    ;(fE.point = function(h, i) {
      ;(c = h), (f = i), (fE.point = g), (h *= jH)
      var e = Math.cos((i *= jH))
      ;(d = e * Math.cos(h)),
        (a = e * Math.sin(h)),
        (b = Math.sin(i)),
        fc(d, a, b)
    }),
      (fE.lineEnd = function() {
        g(c, f), (fE.lineEnd = aT), (fE.point = eZ)
      })
  }
  function bC(c, a) {
    function b(f, d) {
      return (f = c(f, d)), a(f[0], f[1])
    }
    return (
      c.invert &&
        a.invert &&
        (b.invert = function(f, d) {
          return (f = a.invert(f, d)), f && c.invert(f[0], f[1])
        }),
      b
    )
  }
  function jF() {
    return !0
  }
  function hm(k, C, y, b, B) {
    var q = [],
      A = []
    if (
      (k.forEach(function(s) {
        if (!((f = s.length - 1) <= 0)) {
          var f,
            h = s[0],
            g = s[f]
          if (aK(h, g)) {
            B.lineStart()
            for (var i = 0; f > i; ++i) {
              B.point((h = s[i])[0], h[1])
            }
            return void B.lineEnd()
          }
          var a = new eH(h, s, null, !0),
            p = new eH(h, null, a, !1)
          ;(a.o = p),
            q.push(a),
            A.push(p),
            (a = new eH(g, s, null, !1)),
            (p = new eH(g, null, a, !0)),
            (a.o = p),
            q.push(a),
            A.push(p)
        }
      }),
      A.sort(C),
      cY(q),
      cY(A),
      q.length)
    ) {
      for (var j = 0, m = y, z = A.length; z > j; ++j) {
        A[j].e = m = !m
      }
      for (var D, x, v = q[0]; ; ) {
        for (var w = v, d = !0; w.v; ) {
          if ((w = w.n) === v) {
            return
          }
        }
        ;(D = w.z), B.lineStart()
        do {
          if (((w.v = w.o.v = !0), w.e)) {
            if (d) {
              for (var j = 0, z = D.length; z > j; ++j) {
                B.point((x = D[j])[0], x[1])
              }
            } else {
              b(w.x, w.n.x, 1, B)
            }
            w = w.n
          } else {
            if (d) {
              D = w.p.z
              for (var j = D.length - 1; j >= 0; --j) {
                B.point((x = D[j])[0], x[1])
              }
            } else {
              b(w.x, w.p.x, -1, B)
            }
            w = w.p
          }
          ;(w = w.o), (D = w.z), (d = !d)
        } while (!w.v)
        B.lineEnd()
      }
    }
  }
  function cY(f) {
    if ((b = f.length)) {
      for (var b, d, c = 0, a = f[0]; ++c < b; ) {
        ;(a.n = d = f[c]), (d.p = a), (a = d)
      }
      ;(a.n = d = f[0]), (d.p = a)
    }
  }
  function eH(d, a, c, b) {
    ;(this.x = d),
      (this.z = a),
      (this.o = c),
      (this.e = b),
      (this.v = !1),
      (this.n = this.p = null)
  }
  function fk(d, a, c, b) {
    return function(q, B) {
      function I(f, h) {
        var g = q(f, h)
        d((f = g[0]), (h = g[1])) && B.point(f, h)
      }
      function w(h, f) {
        var g = q(h, f)
        F.point(g[0], g[1])
      }
      function A() {
        ;(j.point = w), F.lineStart()
      }
      function G() {
        ;(j.point = I), F.lineEnd()
      }
      function r(h, f) {
        n.push([h, f])
        var g = q(h, f)
        k.point(g[0], g[1])
      }
      function E() {
        k.lineStart(), (n = [])
      }
      function C() {
        r(n[0][0], n[0][1]), k.lineEnd()
        var m,
          h = k.clean(),
          l = e.buffer(),
          i = l.length
        if ((n.pop(), t.push(n), (n = null), i)) {
          if (1 & h) {
            m = l[0]
            var g,
              i = m.length - 1,
              f = -1
            if (i > 0) {
              for (
                H || (B.polygonStart(), (H = !0)), B.lineStart();
                ++f < i;

              ) {
                B.point((g = m[f])[0], g[1])
              }
              B.lineEnd()
            }
          } else {
            i > 1 && 2 & h && l.push(l.pop().concat(l.shift())),
              D.push(l.filter(bc))
          }
        }
      }
      var D,
        t,
        n,
        F = a(B),
        z = q.invert(b[0], b[1]),
        j = {
          point: I,
          lineStart: A,
          lineEnd: G,
          polygonStart: function() {
            ;(j.point = r),
              (j.lineStart = E),
              (j.lineEnd = C),
              (D = []),
              (t = [])
          },
          polygonEnd: function() {
            ;(j.point = I),
              (j.lineStart = A),
              (j.lineEnd = G),
              (D = dU.merge(D))
            var f = gk(z, t)
            D.length
              ? (H || (B.polygonStart(), (H = !0)), hm(D, eg, f, c, B))
              : f &&
                (H || (B.polygonStart(), (H = !0)),
                B.lineStart(),
                c(null, null, 1, B),
                B.lineEnd()),
              H && (B.polygonEnd(), (H = !1)),
              (D = t = null)
          },
          sphere: function() {
            B.polygonStart(),
              B.lineStart(),
              c(null, null, 1, B),
              B.lineEnd(),
              B.polygonEnd()
          },
        },
        e = fZ(),
        k = a(e),
        H = !1
      return j
    }
  }
  function bc(a) {
    return a.length > 1
  }
  function fZ() {
    var b,
      a = []
    return {
      lineStart: function() {
        a.push((b = []))
      },
      point: function(c, d) {
        b.push([c, d])
      },
      lineEnd: iJ,
      buffer: function() {
        var c = a
        return (a = []), (b = null), c
      },
      rejoin: function() {
        a.length > 1 && a.push(a.pop().concat(a.shift()))
      },
    }
  }
  function eg(b, a) {
    return (
      ((b = b.x)[0] < 0 ? b[1] - gA - f9 : gA - b[1]) -
      ((a = a.x)[0] < 0 ? a[1] - gA - f9 : gA - a[1])
    )
  }
  function fC(f) {
    var b,
      d = NaN,
      c = NaN,
      a = NaN
    return {
      lineStart: function() {
        f.lineStart(), (b = 1)
      },
      point: function(h, g) {
        var j = h > 0 ? fP : -fP,
          e = hb(h - d)
        hb(e - fP) < f9
          ? (f.point(d, (c = (c + g) / 2 > 0 ? gA : -gA)),
            f.point(a, c),
            f.lineEnd(),
            f.lineStart(),
            f.point(j, c),
            f.point(h, c),
            (b = 0))
          : a !== j &&
            e >= fP &&
            (hb(d - a) < f9 && (d -= a * f9),
            hb(h - j) < f9 && (h -= j * f9),
            (c = ay(d, c, h, g)),
            f.point(a, c),
            f.lineEnd(),
            f.lineStart(),
            f.point(j, c),
            (b = 0)),
          f.point((d = h), (c = g)),
          (a = j)
      },
      lineEnd: function() {
        f.lineEnd(), (d = c = NaN)
      },
      clean: function() {
        return 2 - b
      },
    }
  }
  function ay(j, f, h, g) {
    var c,
      d,
      b = Math.sin(j - h)
    return hb(b) > f9
      ? Math.atan(
          (Math.sin(f) * (d = Math.cos(g)) * Math.sin(h) -
            Math.sin(g) * (c = Math.cos(f)) * Math.sin(j)) /
            (c * d * b)
        )
      : (f + g) / 2
  }
  function jU(g, c, f, d) {
    var a
    if (null == g) {
      ;(a = f * gA),
        d.point(-fP, a),
        d.point(0, a),
        d.point(fP, a),
        d.point(fP, 0),
        d.point(fP, -a),
        d.point(0, -a),
        d.point(-fP, -a),
        d.point(-fP, 0),
        d.point(-fP, a)
    } else {
      if (hb(g[0] - c[0]) > f9) {
        var b = g[0] < c[0] ? fP : -fP
        ;(a = (f * b) / 2), d.point(-b, a), d.point(0, a), d.point(b, a)
      } else {
        d.point(c[0], c[1])
      }
    }
  }
  function gk(Q, I) {
    var Z = Q[0],
      K = Q[1],
      G = [Math.sin(Z), -Math.cos(Z), 0],
      V = 0,
      al = 0
    ff.reset()
    for (var P = 0, T = I.length; T > P; ++P) {
      var ae = I[P],
        J = ae.length
      if (J) {
        for (
          var Y = ae[0],
            W = Y[0],
            X = Y[1] / 2 + fP / 4,
            O = Math.sin(X),
            F = Math.cos(X),
            aa = 1;
          ;

        ) {
          aa === J && (aa = 0), (Q = ae[aa])
          var R = Q[0],
            B = Q[1] / 2 + fP / 4,
            z = Math.sin(B),
            C = Math.cos(B),
            ai = R - W,
            an = ai >= 0 ? 1 : -1,
            D = an * ai,
            j = D > fP,
            U = O * z
          if (
            (ff.add(Math.atan2(U * an * Math.sin(D), F * C + U * Math.cos(D))),
            (V += j ? ai + an * aM : ai),
            j ^ (W >= Z) ^ (R >= Z))
          ) {
            var q = j7(hE(Y), hE(Q))
            iU(q)
            var H = j7(G, q)
            iU(H)
            var L = (j ^ (ai >= 0) ? -1 : 1) * bP(H[2])
            ;(K > L || (K === L && (q[0] || q[1]))) &&
              (al += j ^ (ai >= 0) ? 1 : -1)
          }
          if (!aa++) {
            break
          }
          ;(W = R), (O = z), (F = C), (Y = Q)
        }
      }
    }
    return (-f9 > V || (f9 > V && 0 > ff)) ^ (1 & al)
  }
  function i8(d) {
    function m(e, a) {
      return Math.cos(e) * Math.cos(a) > g
    }
    function h(t) {
      var q, o, a, r, p
      return {
        lineStart: function() {
          ;(r = a = !1), (p = 1)
        },
        point: function(s, l) {
          var n,
            u = [s, l],
            i = m(s, l),
            w = j ? (i ? 0 : k(s, l)) : i ? k(s + (0 > s ? fP : -fP), l) : 0
          if (
            (!q && (r = a = i) && t.lineStart(),
            i !== a &&
              ((n = b(q, u)),
              (aK(q, n) || aK(u, n)) &&
                ((u[0] += f9), (u[1] += f9), (i = m(u[0], u[1])))),
            i !== a)
          ) {
            ;(p = 0),
              i
                ? (t.lineStart(), (n = b(u, q)), t.point(n[0], n[1]))
                : ((n = b(q, u)), t.point(n[0], n[1]), t.lineEnd()),
              (q = n)
          } else {
            if (c && q && j ^ i) {
              var e
              w & o ||
                !(e = b(u, q, !0)) ||
                ((p = 0),
                j
                  ? (t.lineStart(),
                    t.point(e[0][0], e[0][1]),
                    t.point(e[1][0], e[1][1]),
                    t.lineEnd())
                  : (t.point(e[1][0], e[1][1]),
                    t.lineEnd(),
                    t.lineStart(),
                    t.point(e[0][0], e[0][1])))
            }
          }
          !i || (q && aK(q, u)) || t.point(u[0], u[1]),
            (q = u),
            (a = i),
            (o = w)
        },
        lineEnd: function() {
          a && t.lineEnd(), (q = null)
        },
        clean: function() {
          return p | ((r && a) << 1)
        },
      }
    }
    function b(V, L, ai) {
      var Q = hE(V),
        J = hE(L),
        ar = [1, 0, 0],
        U = j7(Q, J),
        X = eB(U, U),
        an = U[0],
        O = X - an * an
      if (!O) {
        return !ai && V
      }
      var ae = (g * X) / O,
        Z = (-g * an) / O,
        aa = j7(ar, U),
        T = ky(ar, ae),
        I = ky(U, Z)
      g0(T, I)
      var al = aa,
        W = eB(T, al),
        F = eB(al, al),
        B = W * W - F * (eB(T, T) - 1)
      if (!(0 > B)) {
        var G = Math.sqrt(B),
          ao = ky(al, (-W - G) / F)
        if ((g0(ao, T), (ao = aj(ao)), !ai)) {
          return ao
        }
        var at,
          H = V[0],
          i = L[0],
          Y = V[1],
          q = L[1]
        H > i && ((at = H), (H = i), (i = at))
        var K = i - H,
          R = hb(K - fP) < f9,
          P = R || f9 > K
        if (
          (!R && Y > q && ((at = Y), (Y = q), (q = at)),
          P
            ? R
              ? (Y + q > 0) ^ (ao[1] < (hb(ao[0] - H) < f9 ? Y : q))
              : Y <= ao[1] && ao[1] <= q
            : (K > fP) ^ (H <= ao[0] && ao[0] <= i))
        ) {
          var D = ky(al, (-W + G) / F)
          return g0(D, T), [ao, aj(D)]
        }
      }
    }
    function k(i, n) {
      var l = j ? d : fP - d,
        a = 0
      return (
        -l > i ? (a |= 1) : i > l && (a |= 2),
        -l > n ? (a |= 4) : n > l && (a |= 8),
        a
      )
    }
    var g = Math.cos(d),
      j = g > 0,
      c = hb(g) > f9,
      f = bb(d, 6 * jH)
    return fk(m, h, f, j ? [0, -d] : [-fP, d - fP])
  }
  function cL(d, a, c, b) {
    return function(y) {
      var m,
        w = y.a,
        j = y.b,
        k = w.x,
        t = w.y,
        z = j.x,
        r = j.y,
        n = 0,
        q = 1,
        e = z - k,
        x = r - t
      if (((m = d - k), e || !(m > 0))) {
        if (((m /= e), 0 > e)) {
          if (n > m) {
            return
          }
          q > m && (q = m)
        } else {
          if (e > 0) {
            if (m > q) {
              return
            }
            m > n && (n = m)
          }
        }
        if (((m = c - k), e || !(0 > m))) {
          if (((m /= e), 0 > e)) {
            if (m > q) {
              return
            }
            m > n && (n = m)
          } else {
            if (e > 0) {
              if (n > m) {
                return
              }
              q > m && (q = m)
            }
          }
          if (((m = a - t), x || !(m > 0))) {
            if (((m /= x), 0 > x)) {
              if (n > m) {
                return
              }
              q > m && (q = m)
            } else {
              if (x > 0) {
                if (m > q) {
                  return
                }
                m > n && (n = m)
              }
            }
            if (((m = b - t), x || !(0 > m))) {
              if (((m /= x), 0 > x)) {
                if (m > q) {
                  return
                }
                m > n && (n = m)
              } else {
                if (x > 0) {
                  if (n > m) {
                    return
                  }
                  q > m && (q = m)
                }
              }
              return (
                n > 0 && (y.a = { x: k + n * e, y: t + n * x }),
                1 > q && (y.b = { x: k + q * e, y: t + q * x }),
                y
              )
            }
          }
        }
      }
    }
  }
  function cc(j, f, h, g) {
    function c(e, a) {
      return hb(e[0] - j) < f9
        ? a > 0
          ? 0
          : 3
        : hb(e[0] - h) < f9
          ? a > 0
            ? 2
            : 1
          : hb(e[1] - f) < f9
            ? a > 0
              ? 1
              : 0
            : a > 0
              ? 3
              : 2
    }
    function d(e, a) {
      return b(e.x, a.x)
    }
    function b(l, a) {
      var k = c(l, 1),
        i = c(a, 1)
      return k !== i
        ? k - i
        : 0 === k
          ? a[1] - l[1]
          : 1 === k
            ? l[0] - a[0]
            : 2 === k
              ? l[1] - a[1]
              : a[0] - l[0]
    }
    return function(G) {
      function I(p) {
        for (var C = 0, w = P.length, k = p[1], A = 0; w > A; ++A) {
          for (var v, y = 1, m = P[A], s = m.length, x = m[0]; s > y; ++y) {
            ;(v = m[y]),
              x[1] <= k
                ? v[1] > k && i1(x, v, p) > 0 && ++C
                : v[1] <= k && i1(x, v, p) < 0 && --C,
              (x = v)
          }
        }
        return 0 !== C
      }
      function Q(m, w, k, x) {
        var p = 0,
          v = 0
        if (
          null == m ||
          (p = c(m, k)) !== (v = c(w, k)) ||
          (b(m, w) < 0) ^ (k > 0)
        ) {
          do {
            x.point(0 === p || 3 === p ? j : h, p > 1 ? g : f)
          } while ((p = (p + k + 4) % 4) !== v)
        } else {
          x.point(w[0], w[1])
        }
      }
      function z(k, l) {
        return k >= j && h >= k && l >= f && g >= l
      }
      function O(l, k) {
        z(l, k) && G.point(l, k)
      }
      function K() {
        ;(B.point = F), P && P.push((H = [])), (a = !0), (r = !1), (R = T = NaN)
      }
      function L() {
        t && (F(n, i), q && r && u.rejoin(), t.push(u.buffer())),
          (B.point = O),
          r && G.lineEnd()
      }
      function F(o, k) {
        ;(o = Math.max(-jY, Math.min(jY, o))),
          (k = Math.max(-jY, Math.min(jY, k)))
        var m = z(o, k)
        if ((P && H.push([o, k]), a)) {
          ;(n = o),
            (i = k),
            (q = m),
            (a = !1),
            m && (G.lineStart(), G.point(o, k))
        } else {
          if (m && r) {
            G.point(o, k)
          } else {
            var l = { a: { x: R, y: T }, b: { x: o, y: k } }
            D(l)
              ? (r || (G.lineStart(), G.point(l.a.x, l.a.y)),
                G.point(l.b.x, l.b.y),
                m || G.lineEnd(),
                (J = !1))
              : m && (G.lineStart(), G.point(o, k), (J = !1))
          }
        }
        ;(R = o), (T = k), (r = m)
      }
      var t,
        P,
        H,
        n,
        i,
        q,
        R,
        T,
        r,
        a,
        J,
        e = G,
        u = fZ(),
        D = cL(j, f, h, g),
        B = {
          point: O,
          lineStart: K,
          lineEnd: L,
          polygonStart: function() {
            ;(G = u), (t = []), (P = []), (J = !0)
          },
          polygonEnd: function() {
            ;(G = e), (t = dU.merge(t))
            var l = I([j, g]),
              m = J && l,
              k = t.length
            ;(m || k) &&
              (G.polygonStart(),
              m && (G.lineStart(), Q(null, null, 1, G), G.lineEnd()),
              k && hm(t, d, l, Q, G),
              G.polygonEnd()),
              (t = P = H = null)
          },
        }
      return B
    }
  }
  function dN(f) {
    var b = 0,
      d = fP / 3,
      c = dS(f),
      a = c(b, d)
    return (
      (a.parallels = function(e) {
        return arguments.length
          ? c((b = (e[0] * fP) / 180), (d = (e[1] * fP) / 180))
          : [(b / fP) * 180, (d / fP) * 180]
      }),
      a
    )
  }
  function c4(j, f) {
    function h(k, a) {
      var i = Math.sqrt(d - 2 * c * Math.sin(a)) / c
      return [i * Math.sin((k *= c)), b - i * Math.cos(k)]
    }
    var g = Math.sin(j),
      c = (g + Math.sin(f)) / 2,
      d = 1 + g * (2 * c - g),
      b = Math.sqrt(d) / c
    return (
      (h.invert = function(k, a) {
        var i = b - a
        return [
          Math.atan2(k, i) / c,
          bP((d - (k * k + i * i) * c * c) / (2 * c)),
        ]
      }),
      h
    )
  }
  function dY() {
    function f(g, e) {
      ;(jh += a * g - c * e), (c = g), (a = e)
    }
    var b, d, c, a
    ;(d1.point = function(g, e) {
      ;(d1.point = f), (b = c = g), (d = a = e)
    }),
      (d1.lineEnd = function() {
        f(b, d)
      })
  }
  function bU(b, a) {
    cO > b && (cO = b),
      b > dQ && (dQ = b),
      cg > a && (cg = a),
      a > c7 && (c7 = a)
  }
  function dv() {
    function k(e, a) {
      b.push('M', e, ',', a, d)
    }
    function f(e, a) {
      b.push('M', e, ',', a), (j.point = h)
    }
    function h(e, a) {
      b.push('L', e, ',', a)
    }
    function g() {
      j.point = k
    }
    function c() {
      b.push('Z')
    }
    var d = ij(4.5),
      b = [],
      j = {
        point: k,
        lineStart: function() {
          j.point = f
        },
        lineEnd: g,
        polygonStart: function() {
          j.lineEnd = c
        },
        polygonEnd: function() {
          ;(j.lineEnd = g), (j.point = k)
        },
        pointRadius: function(a) {
          return (d = ij(a)), j
        },
        result: function() {
          if (b.length) {
            var a = b.join('')
            return (b = []), a
          }
        },
      }
    return j
  }
  function ij(a) {
    return (
      'm0,' +
      a +
      'a' +
      a +
      ',' +
      a +
      ' 0 1,1 0,' +
      -2 * a +
      'a' +
      a +
      ',' +
      a +
      ' 0 1,1 0,' +
      2 * a +
      'z'
    )
  }
  function kj(b, a) {
    ;(bF += b), (jI += a), ++hs
  }
  function hJ() {
    function c(h, g) {
      var e = h - a,
        f = g - b,
        d = Math.sqrt(e * e + f * f)
      ;(c0 += (d * (a + h)) / 2),
        (eK += (d * (b + g)) / 2),
        (fq += d),
        kj((a = h), (b = g))
    }
    var a, b
    dy.point = function(e, d) {
      ;(dy.point = c), kj((a = e), (b = d))
    }
  }
  function fI() {
    dy.point = kj
  }
  function eq() {
    function f(l, j) {
      var k = l - c,
        h = j - a,
        g = Math.sqrt(k * k + h * h)
      ;(c0 += (g * (c + l)) / 2),
        (eK += (g * (a + j)) / 2),
        (fq += g),
        (g = a * l - c * j),
        (bg += g * (c + l)),
        (f2 += g * (a + j)),
        (em += 3 * g),
        kj((c = l), (a = j))
    }
    var b, d, c, a
    ;(dy.point = function(g, e) {
      ;(dy.point = f), kj((b = c = g), (d = a = e))
    }),
      (dy.lineEnd = function() {
        f(b, d)
      })
  }
  function bT(k) {
    function f(a, i) {
      k.moveTo(a + b, i), k.arc(a, i, b, 0, aM)
    }
    function h(a, i) {
      k.moveTo(a, i), (j.point = g)
    }
    function g(a, i) {
      k.lineTo(a, i)
    }
    function c() {
      j.point = f
    }
    function d() {
      k.closePath()
    }
    var b = 4.5,
      j = {
        point: f,
        lineStart: function() {
          j.point = h
        },
        lineEnd: c,
        polygonStart: function() {
          j.lineEnd = d
        },
        polygonEnd: function() {
          ;(j.lineEnd = c), (j.point = f)
        },
        pointRadius: function(a) {
          return (b = a), j
        },
        result: iJ,
      }
    return j
  }
  function hw(k) {
    function f(a) {
      return (j ? g : h)(a)
    }
    function h(a) {
      return f4(a, function(l, i) {
        ;(l = k(l, i)), a.point(l[0], l[1])
      })
    }
    function g(B) {
      function L(i, a) {
        ;(i = k(i, a)), B.point(i[0], i[1])
      }
      function D() {
        ;(o = NaN), (n.point = H), B.lineStart()
      }
      function H(s, p) {
        var m = hE([s, p]),
          l = k(s, p)
        c(
          o,
          u,
          q,
          P,
          R,
          z,
          (o = l[0]),
          (u = l[1]),
          (q = s),
          (P = m[0]),
          (R = m[1]),
          (z = m[2]),
          j,
          B
        ),
          B.point(o, u)
      }
      function Q() {
        ;(n.point = L), B.lineEnd()
      }
      function G() {
        D(), (n.point = O), (n.lineEnd = C)
      }
      function O(e, a) {
        H((K = e), (I = a)),
          (J = o),
          (E = u),
          (A = P),
          (N = R),
          (F = z),
          (n.point = H)
      }
      function C() {
        c(o, u, q, P, R, z, J, E, K, A, N, F, j, B), (n.lineEnd = Q), Q()
      }
      var K,
        I,
        J,
        E,
        A,
        N,
        F,
        q,
        o,
        u,
        P,
        R,
        z,
        n = {
          point: L,
          lineStart: D,
          lineEnd: Q,
          polygonStart: function() {
            B.polygonStart(), (n.lineStart = G)
          },
          polygonEnd: function() {
            B.polygonEnd(), (n.lineStart = D)
          },
        }
      return n
    }
    function c(J, ae, P, U, W, al, K, aa, Y, Z, T, H, ai, V) {
      var D = K - J,
        n = aa - ae,
        F = D * D + n * n
      if (F > 4 * d && ai--) {
        var an = U + Z,
          ao = W + T,
          G = al + H,
          a = Math.sqrt(an * an + ao * ao + G * G),
          X = Math.asin((G /= a)),
          i =
            hb(hb(G) - 1) < f9 || hb(P - Y) < f9
              ? (P + Y) / 2
              : Math.atan2(ao, an),
          I = k(i, X),
          R = I[0],
          O = I[1],
          B = R - J,
          u = O - ae,
          Q = n * B - D * u
        ;((Q * Q) / F > d ||
          hb((D * B + n * u) / F - 0.5) > 0.3 ||
          b > U * Z + W * T + al * H) &&
          (c(J, ae, P, U, W, al, R, O, i, (an /= a), (ao /= a), G, ai, V),
          V.point(R, O),
          c(R, O, i, an, ao, G, K, aa, Y, Z, T, H, ai, V))
      }
    }
    var d = 0.5,
      b = Math.cos(30 * jH),
      j = 16
    return (
      (f.precision = function(a) {
        return arguments.length
          ? ((j = (d = a * a) > 0 && 16), f)
          : Math.sqrt(d)
      }),
      f
    )
  }
  function cQ(b) {
    var a = hw(function(c, d) {
      return b([c * cW, d * cW])
    })
    return function(c) {
      return e4(a(c))
    }
  }
  function bB(a) {
    this.stream = a
  }
  function f4(b, a) {
    return {
      point: a,
      sphere: function() {
        b.sphere()
      },
      lineStart: function() {
        b.lineStart()
      },
      lineEnd: function() {
        b.lineEnd()
      },
      polygonStart: function() {
        b.polygonStart()
      },
      polygonEnd: function() {
        b.polygonEnd()
      },
    }
  }
  function j0(a) {
    return dS(function() {
      return a
    })()
  }
  function dS(G) {
    function B(a) {
      return (a = F(a[0] * jH, a[1] * jH)), [a[0] * K + I, Q - a[1] * K]
    }
    function O(a) {
      return (
        (a = F.invert((a[0] - I) / K, (Q - a[1]) / K)),
        a && [a[0] * cW, a[1] * cW]
      )
    }
    function D() {
      F = bC((T = g5(H, k, q)), J)
      var a = J(z, P)
      return (I = L - a[0] * K), (Q = E + a[1] * K), A()
    }
    function A() {
      return C && ((C.valid = !1), (C = null)), B
    }
    var J,
      T,
      F,
      I,
      Q,
      C,
      N = hw(function(b, a) {
        return (b = J(b, a)), [b[0] * K + I, Q - b[1] * K]
      }),
      K = 150,
      L = 480,
      E = 250,
      z = 0,
      P = 0,
      H = 0,
      k = 0,
      q = 0,
      R = aC,
      U = hV,
      y = null,
      j = null
    return (
      (B.stream = function(a) {
        return C && (C.valid = !1), (C = e4(R(T, N(U(a))))), (C.valid = !0), C
      }),
      (B.clipAngle = function(a) {
        return arguments.length
          ? ((R = null == a ? ((y = a), aC) : i8((y = +a) * jH)), A())
          : y
      }),
      (B.clipExtent = function(a) {
        return arguments.length
          ? ((j = a),
            (U = a ? cc(a[0][0], a[0][1], a[1][0], a[1][1]) : hV),
            A())
          : j
      }),
      (B.scale = function(a) {
        return arguments.length ? ((K = +a), D()) : K
      }),
      (B.translate = function(a) {
        return arguments.length ? ((L = +a[0]), (E = +a[1]), D()) : [L, E]
      }),
      (B.center = function(a) {
        return arguments.length
          ? ((z = (a[0] % 360) * jH), (P = (a[1] % 360) * jH), D())
          : [z * cW, P * cW]
      }),
      (B.rotate = function(a) {
        return arguments.length
          ? ((H = (a[0] % 360) * jH),
            (k = (a[1] % 360) * jH),
            (q = a.length > 2 ? (a[2] % 360) * jH : 0),
            D())
          : [H * cW, k * cW, q * cW]
      }),
      dU.rebind(B, N, 'precision'),
      function() {
        return (J = G.apply(this, arguments)), (B.invert = J.invert && O), D()
      }
    )
  }
  function e4(a) {
    return f4(a, function(b, c) {
      a.point(b * jH, c * jH)
    })
  }
  function iC(b, a) {
    return [b, a]
  }
  function ck(b, a) {
    return [b > fP ? b - aM : -fP > b ? b + aM : b, a]
  }
  function g5(c, a, b) {
    return c ? (a || b ? bC(gN(c), dA(a, b)) : gN(c)) : a || b ? dA(a, b) : ck
  }
  function gv(a) {
    return function(b, c) {
      return (b += a), [b > fP ? b - aM : -fP > b ? b + aM : b, c]
    }
  }
  function gN(b) {
    var a = gv(b)
    return (a.invert = gv(-b)), a
  }
  function dA(j, f) {
    function h(r, i) {
      var m = Math.cos(i),
        p = Math.cos(r) * m,
        a = Math.sin(r) * m,
        q = Math.sin(i),
        k = q * g + p * c
      return [Math.atan2(a * d - k * b, p * g - q * c), bP(k * d + a * b)]
    }
    var g = Math.cos(j),
      c = Math.sin(j),
      d = Math.cos(f),
      b = Math.sin(f)
    return (
      (h.invert = function(r, i) {
        var m = Math.cos(i),
          p = Math.cos(r) * m,
          a = Math.sin(r) * m,
          q = Math.sin(i),
          k = q * d - a * b
        return [Math.atan2(a * d + q * b, p * g + k * c), bP(k * g - p * c)]
      }),
      h
    )
  }
  function bb(d, a) {
    var c = Math.cos(d),
      b = Math.sin(d)
    return function(g, h, f, k) {
      var e = f * a
      null != g
        ? ((g = hO(c, g)),
          (h = hO(c, h)),
          (f > 0 ? h > g : g > h) && (g += f * aM))
        : ((g = d + f * aM), (h = d - 0.5 * e))
      for (var m, j = g; f > 0 ? j > h : h > j; j -= e) {
        k.point((m = aj([c, -b * Math.cos(j), -b * Math.sin(j)]))[0], m[1])
      }
    }
  }
  function hO(d, a) {
    var c = hE(a)
    ;(c[0] -= d), iU(c)
    var b = d9(-c[1])
    return ((-c[2] < 0 ? -b : b) + 2 * Math.PI - f9) % (2 * Math.PI)
  }
  function eM(d, a, c) {
    var b = dU.range(d, a - f9, c).concat(a)
    return function(e) {
      return b.map(function(f) {
        return [e, f]
      })
    }
  }
  function kh(d, a, c) {
    var b = dU.range(d, a - f9, c).concat(a)
    return function(e) {
      return b.map(function(f) {
        return [f, e]
      })
    }
  }
  function g9(a) {
    return a.source
  }
  function aw(a) {
    return a.target
  }
  function jq(k, C, y, b) {
    var B = Math.cos(C),
      q = Math.sin(C),
      A = Math.cos(b),
      j = Math.sin(b),
      m = B * Math.cos(k),
      z = B * Math.sin(k),
      D = A * Math.cos(y),
      x = A * Math.sin(y),
      v = 2 * Math.asin(Math.sqrt(jQ(b - C) + B * A * jQ(y - k))),
      w = 1 / Math.sin(v),
      d = v
        ? function(l) {
            var g = Math.sin((l *= v)) * w,
              i = Math.sin(v - l) * w,
              h = i * m + g * D,
              f = i * z + g * x,
              c = i * q + g * j
            return [
              Math.atan2(f, h) * cW,
              Math.atan2(c, Math.sqrt(h * h + f * f)) * cW,
            ]
          }
        : function() {
            return [k * cW, C * cW]
          }
    return (d.distance = v), d
  }
  function aE() {
    function d(k, g) {
      var h = Math.sin((g *= jH)),
        f = Math.cos(g),
        j = hb((k *= jH) - a),
        e = Math.cos(j)
      ;(ix += Math.atan2(
        Math.sqrt((j = f * Math.sin(j)) * j + (j = b * h - c * f * e) * j),
        c * h + b * f * e
      )),
        (a = k),
        (c = h),
        (b = f)
    }
    var a, c, b
    ;(kp.point = function(e, f) {
      ;(a = e * jH),
        (c = Math.sin((f *= jH))),
        (b = Math.cos(f)),
        (kp.point = d)
    }),
      (kp.lineEnd = function() {
        kp.point = kp.lineEnd = iJ
      })
  }
  function aS(c, a) {
    function b(g, j) {
      var h = Math.cos(g),
        d = Math.cos(j),
        f = c(h * d)
      return [f * d * Math.sin(g), f * Math.sin(j)]
    }
    return (
      (b.invert = function(k, j) {
        var h = Math.sqrt(k * k + j * j),
          f = a(h),
          g = Math.sin(f),
          d = Math.cos(f)
        return [Math.atan2(k * g, h * d), Math.asin(h && (j * g) / h)]
      }),
      b
    )
  }
  function e8(j, f) {
    function h(k, a) {
      b > 0 ? -gA + f9 > a && (a = -gA + f9) : a > gA - f9 && (a = gA - f9)
      var i = b / Math.pow(c(a), d)
      return [i * Math.sin(d * k), b - i * Math.cos(d * k)]
    }
    var g = Math.cos(j),
      c = function(a) {
        return Math.tan(fP / 4 + a / 2)
      },
      d =
        j === f
          ? Math.sin(j)
          : Math.log(g / Math.cos(f)) / Math.log(c(f) / c(j)),
      b = (g * Math.pow(c(j), d)) / d
    return d
      ? ((h.invert = function(l, a) {
          var k = b - a,
            i = jf(d) * Math.sqrt(l * l + k * k)
          return [
            Math.atan2(l, k) / d,
            2 * Math.atan(Math.pow(b / i, 1 / d)) - gA,
          ]
        }),
        h)
      : gR
  }
  function fv(g, c) {
    function f(j, h) {
      var i = b - h
      return [i * Math.sin(a * j), b - i * Math.cos(a * j)]
    }
    var d = Math.cos(g),
      a = g === c ? Math.sin(g) : (d - Math.cos(c)) / (c - g),
      b = d / a + g
    return hb(a) < f9
      ? iC
      : ((f.invert = function(j, h) {
          var i = b - h
          return [Math.atan2(j, i) / a, b - jf(a) * Math.sqrt(j * j + i * i)]
        }),
        f)
  }
  function gR(b, a) {
    return [b, Math.log(Math.tan(fP / 4 + a / 2))]
  }
  function a1(g) {
    var c,
      f = j0(g),
      d = f.scale,
      a = f.translate,
      b = f.clipExtent
    return (
      (f.scale = function() {
        var e = d.apply(f, arguments)
        return e === f ? (c ? f.clipExtent(null) : f) : e
      }),
      (f.translate = function() {
        var e = a.apply(f, arguments)
        return e === f ? (c ? f.clipExtent(null) : f) : e
      }),
      (f.clipExtent = function(j) {
        var h = b.apply(f, arguments)
        if (h === f) {
          if ((c = null == j)) {
            var i = fP * d(),
              e = a()
            b([[e[0] - i, e[1] - i], [e[0] + i, e[1] + i]])
          }
        } else {
          c && (h = null)
        }
        return h
      }),
      f.clipExtent(null)
    )
  }
  function cD(b, a) {
    return [Math.log(Math.tan(fP / 4 + a / 2)), -b]
  }
  function bK(a) {
    return a[0]
  }
  function jS(a) {
    return a[1]
  }
  function hB(f) {
    for (var b = f.length, d = [0, 1], c = 2, a = 2; b > a; a++) {
      for (; c > 1 && i1(f[d[c - 2]], f[d[c - 1]], f[a]) <= 0; ) {
        --c
      }
      d[c++] = a
    }
    return d.slice(0, c)
  }
  function c9(b, a) {
    return b[0] - a[0] || b[1] - a[1]
  }
  function eQ(c, a, b) {
    return (b[0] - a[0]) * (c[1] - a[1]) < (b[1] - a[1]) * (c[0] - a[0])
  }
  function fz(g, y, q, b) {
    var x = g[0],
      k = q[0],
      w = y[0] - x,
      d = b[0] - k,
      j = g[1],
      v = q[1],
      z = y[1] - j,
      p = b[1] - v,
      m = (d * (j - v) - p * (x - k)) / (p * w - d * z)
    return [x + m * w, j + m * z]
  }
  function bp(c) {
    var a = c[0],
      b = c[c.length - 1]
    return !(a[0] - b[0] || a[1] - b[1])
  }
  function f8() {
    cF(this), (this.edge = this.site = this.circle = null)
  }
  function ey(b) {
    var a = ig.pop() || new f8()
    return (a.site = b), a
  }
  function fN(a) {
    b3(a), jT.remove(a), ig.push(a), cF(a)
  }
  function aJ(g) {
    var w = g.circle,
      m = w.x,
      b = w.cy,
      v = { x: m, y: b },
      j = g.P,
      q = g.N,
      d = [g]
    fN(g)
    for (
      var h = j;
      h.circle && hb(m - h.circle.x) < f9 && hb(b - h.circle.cy) < f9;

    ) {
      ;(j = h.P), d.unshift(h), fN(h), (h = j)
    }
    d.unshift(h), b3(h)
    for (
      var p = q;
      p.circle && hb(m - p.circle.x) < f9 && hb(b - p.circle.cy) < f9;

    ) {
      ;(q = p.N), d.push(p), fN(p), (p = q)
    }
    d.push(p), b3(p)
    var x,
      k = d.length
    for (x = 1; k > x; ++x) {
      ;(p = d[x]), (h = d[x - 1]), d6(p.edge, h.site, p.site, v)
    }
    ;(h = d[0]),
      (p = d[k - 1]),
      (p.edge = hS(h.site, p.site, null, v)),
      ef(h),
      ef(p)
  }
  function j6(E) {
    for (var z, L, B, w, H = E.x, P = E.y, D = jT._; D; ) {
      if (((B = gz(D, P) - H), B > f9)) {
        D = D.L
      } else {
        if (((w = H - jE(D, P)), !(w > f9))) {
          B > -f9
            ? ((z = D.P), (L = D))
            : w > -f9
              ? ((z = D), (L = D.N))
              : (z = L = D)
          break
        }
        if (!D.R) {
          z = D
          break
        }
        D = D.R
      }
    }
    var G = ey(E)
    if ((jT.insert(z, G), z || L)) {
      if (z === L) {
        return (
          b3(z),
          (L = ey(z.site)),
          jT.insert(G, L),
          (G.edge = L.edge = hS(z.site, G.site)),
          ef(z),
          void ef(L)
        )
      }
      if (!L) {
        return void (G.edge = hS(z.site, G.site))
      }
      b3(z), b3(L)
      var O = z.site,
        A = O.x,
        K = O.y,
        I = E.x - A,
        J = E.y - K,
        C = L.site,
        q = C.x - A,
        N = C.y - K,
        F = 2 * (I * N - J * q),
        j = I * I + J * J,
        b = q * q + N * N,
        k = { x: (N * j - J * b) / F + A, y: (I * b - q * j) / F + K }
      d6(L.edge, O, C, k),
        (G.edge = hS(O, E, null, k)),
        (L.edge = hS(E, C, null, k)),
        ef(z),
        ef(L)
    }
  }
  function gz(g, y) {
    var q = g.site,
      b = q.x,
      x = q.y,
      k = x - y
    if (!k) {
      return b
    }
    var w = g.P
    if (!w) {
      return -(1 / 0)
    }
    q = w.site
    var d = q.x,
      j = q.y,
      v = j - y
    if (!v) {
      return d
    }
    var z = d - b,
      p = 1 / k - 1 / v,
      m = z / v
    return p
      ? (-m +
          Math.sqrt(
            m * m - 2 * p * ((z * z) / (-2 * v) - j + v / 2 + x - k / 2)
          )) /
          p +
          b
      : (b + d) / 2
  }
  function jE(d, a) {
    var c = d.N
    if (c) {
      return gz(c, a)
    }
    var b = d.site
    return b.y === a ? b.x : 1 / 0
  }
  function cV(a) {
    ;(this.site = a), (this.edges = [])
  }
  function cy(m) {
    for (
      var G,
        A,
        b,
        F,
        w,
        D,
        k,
        q,
        C,
        H,
        z = m[0][0],
        x = m[1][0],
        y = m[0][1],
        j = m[1][1],
        E = fY,
        B = E.length;
      B--;

    ) {
      if (((w = E[B]), w && w.prepare())) {
        for (k = w.edges, q = k.length, D = 0; q > D; ) {
          ;(H = k[D].end()),
            (b = H.x),
            (F = H.y),
            (C = k[++D % q].start()),
            (G = C.x),
            (A = C.y),
            (hb(b - G) > f9 || hb(F - A) > f9) &&
              (k.splice(
                D,
                0,
                new bM(
                  fR(
                    w.site,
                    H,
                    hb(b - z) < f9 && j - F > f9
                      ? { x: z, y: hb(G - z) < f9 ? A : j }
                      : hb(F - j) < f9 && x - b > f9
                        ? { x: hb(A - j) < f9 ? G : x, y: j }
                        : hb(b - x) < f9 && F - y > f9
                          ? { x: x, y: hb(G - x) < f9 ? A : y }
                          : hb(F - y) < f9 && b - z > f9
                            ? { x: hb(A - y) < f9 ? G : z, y: y }
                            : null
                  ),
                  w.site,
                  null
                )
              ),
              ++q)
        }
      }
    }
  }
  function dX(b, a) {
    return a.angle - b.angle
  }
  function df() {
    cF(this), (this.x = this.y = this.arc = this.site = this.cy = null)
  }
  function ef(C) {
    var w = C.P,
      J = C.N
    if (w && J) {
      var z = w.site,
        q = C.site,
        F = J.site
      if (z !== F) {
        var N = q.x,
          B = q.y,
          E = z.x - N,
          L = z.y - B,
          x = F.x - N,
          I = F.y - B,
          G = 2 * (E * I - L * x)
        if (!(G >= -ez)) {
          var H = E * E + L * L,
            A = x * x + I * I,
            k = (I * H - L * A) / G,
            K = (E * A - x * H) / G,
            I = K + B,
            D = cb.pop() || new df()
          ;(D.arc = C),
            (D.site = q),
            (D.x = k + N),
            (D.y = I + Math.sqrt(k * k + K * K)),
            (D.cy = I),
            (C.circle = D)
          for (var j = null, b = eY._; b; ) {
            if (D.y < b.y || (D.y === b.y && D.x <= b.x)) {
              if (!b.L) {
                j = b.P
                break
              }
              b = b.L
            } else {
              if (!b.R) {
                j = b
                break
              }
              b = b.R
            }
          }
          eY.insert(j, D), j || (dM = D)
        }
      }
    }
  }
  function b3(b) {
    var a = b.circle
    a && (a.P || (dM = a.N), eY.remove(a), cb.push(a), cF(a), (b.circle = null))
  }
  function dE(f) {
    for (
      var b, d = by, c = cL(f[0][0], f[0][1], f[1][0], f[1][1]), a = d.length;
      a--;

    ) {
      ;(b = d[a]),
        (!iM(b, f) ||
          !c(b) ||
          (hb(b.a.x - b.b.x) < f9 && hb(b.a.y - b.b.y) < f9)) &&
          ((b.a = b.b = null), d.splice(a, 1))
    }
  }
  function iM(z, k) {
    var G = z.b
    if (G) {
      return !0
    }
    var w,
      j,
      C = z.a,
      J = k[0][0],
      y = k[1][0],
      B = k[0][1],
      I = k[1][1],
      q = z.l,
      F = z.r,
      D = q.x,
      E = q.y,
      x = F.x,
      b = F.y,
      H = (D + x) / 2,
      A = (E + b) / 2
    if (b === E) {
      if (J > H || H >= y) {
        return
      }
      if (D > x) {
        if (C) {
          if (C.y >= I) {
            return
          }
        } else {
          C = { x: H, y: B }
        }
        G = { x: H, y: I }
      } else {
        if (C) {
          if (C.y < B) {
            return
          }
        } else {
          C = { x: H, y: I }
        }
        G = { x: H, y: B }
      }
    } else {
      if (((w = (D - x) / (b - E)), (j = A - w * H), -1 > w || w > 1)) {
        if (D > x) {
          if (C) {
            if (C.y >= I) {
              return
            }
          } else {
            C = { x: (B - j) / w, y: B }
          }
          G = { x: (I - j) / w, y: I }
        } else {
          if (C) {
            if (C.y < B) {
              return
            }
          } else {
            C = { x: (I - j) / w, y: I }
          }
          G = { x: (B - j) / w, y: B }
        }
      } else {
        if (b > E) {
          if (C) {
            if (C.x >= y) {
              return
            }
          } else {
            C = { x: J, y: w * J + j }
          }
          G = { x: y, y: w * y + j }
        } else {
          if (C) {
            if (C.x < J) {
              return
            }
          } else {
            C = { x: y, y: w * y + j }
          }
          G = { x: J, y: w * J + j }
        }
      }
    }
    return (z.a = C), (z.b = G), !0
  }
  function kx(b, a) {
    ;(this.l = b), (this.r = a), (this.a = this.b = null)
  }
  function hS(f, b, d, c) {
    var a = new kx(f, b)
    return (
      by.push(a),
      d && d6(a, f, b, d),
      c && d6(a, b, f, c),
      fY[f.i].edges.push(new bM(a, f, b)),
      fY[b.i].edges.push(new bM(a, b, f)),
      a
    )
  }
  function fR(d, a, c) {
    var b = new kx(d, null)
    return (b.a = a), (b.b = c), by.push(b), b
  }
  function d6(d, a, c, b) {
    d.a || d.b
      ? d.l === c
        ? (d.b = b)
        : (d.a = b)
      : ((d.a = b), (d.l = a), (d.r = c))
  }
  function bM(f, b, d) {
    var c = f.a,
      a = f.b
    ;(this.edge = f),
      (this.site = b),
      (this.angle = d
        ? Math.atan2(d.y - b.y, d.x - b.x)
        : f.l === b
          ? Math.atan2(a.x - c.x, c.y - a.y)
          : Math.atan2(c.x - a.x, a.y - c.y))
  }
  function hf() {
    this._ = null
  }
  function cF(a) {
    a.U = a.C = a.L = a.R = a.P = a.N = null
  }
  function bs(f, b) {
    var d = b,
      c = b.R,
      a = d.U
    a ? (a.L === d ? (a.L = c) : (a.R = c)) : (f._ = c),
      (c.U = a),
      (d.U = c),
      (d.R = c.L),
      d.R && (d.R.U = d),
      (c.L = d)
  }
  function fV(f, b) {
    var d = b,
      c = b.L,
      a = d.U
    a ? (a.L === d ? (a.L = c) : (a.R = c)) : (f._ = c),
      (c.U = a),
      (d.U = c),
      (d.L = c.R),
      d.L && (d.L.U = d),
      (c.R = d)
  }
  function jN(a) {
    for (; a.L; ) {
      a = a.L
    }
    return a
  }
  function dI(j, f) {
    var h,
      g,
      c,
      d = j.sort(eU).pop()
    for (by = [], fY = new Array(j.length), jT = new hf(), eY = new hf(); ; ) {
      if (((c = dM), d && (!c || d.y < c.y || (d.y === c.y && d.x < c.x)))) {
        ;(d.x !== h || d.y !== g) &&
          ((fY[d.i] = new cV(d)), j6(d), (h = d.x), (g = d.y)),
          (d = j.pop())
      } else {
        if (!c) {
          break
        }
        aJ(c.arc)
      }
    }
    f && (dE(f), cy(f))
    var b = { cells: fY, edges: by }
    return (jT = eY = by = fY = null), b
  }
  function eU(b, a) {
    return a.y - b.y || a.x - b.x
  }
  function h4(c, a, b) {
    return (c.x - b.x) * (a.y - c.y) - (c.x - a.x) * (b.y - c.y)
  }
  function b5(a) {
    return a.x
  }
  function gV(a) {
    return a.y
  }
  function gd() {
    return { leaf: !0, nodes: [], point: null, x: null, y: null }
  }
  function gD(d, m, h, b, k, g) {
    if (!d(m, h, b, k, g)) {
      var j = 0.5 * (h + k),
        c = 0.5 * (b + g),
        f = m.nodes
      f[0] && gD(d, f[0], h, b, j, c),
        f[1] && gD(d, f[1], j, b, k, c),
        f[2] && gD(d, f[2], h, c, j, g),
        f[3] && gD(d, f[3], j, c, k, g)
    }
  }
  function dk(f, q, j, b, p, h, m) {
    var d,
      g = 1 / 0
    return (
      (function k(c, G, t, o, r) {
        if (!(G > h || t > m || b > o || p > r)) {
          if ((a = c.point)) {
            var a,
              F = q - c.x,
              z = j - c.y,
              e = F * F + z * z
            if (g > e) {
              var B = Math.sqrt((g = e))
              ;(b = q - B), (p = j - B), (h = q + B), (m = j + B), (d = a)
            }
          }
          for (
            var u = c.nodes,
              C = 0.5 * (G + o),
              A = 0.5 * (t + r),
              E = q >= C,
              D = j >= A,
              l = (D << 1) | E,
              i = l + 4;
            i > l;
            ++l
          ) {
            if ((c = u[3 & l])) {
              switch (3 & l) {
                case 0:
                  k(c, G, t, C, A)
                  break
                case 1:
                  k(c, C, t, o, A)
                  break
                case 2:
                  k(c, G, A, C, r)
                  break
                case 3:
                  k(c, C, A, o, r)
              }
            }
          }
        }
      })(f, b, p, h, m),
      d
    )
  }
  function a3(k, f) {
    ;(k = dU.rgb(k)), (f = dU.rgb(f))
    var h = k.r,
      g = k.g,
      c = k.b,
      d = f.r - h,
      b = f.g - g,
      j = f.b - c
    return function(a) {
      return (
        '#' +
        i3(Math.round(h + d * a)) +
        i3(Math.round(g + b * a)) +
        i3(Math.round(c + j * a))
      )
    }
  }
  function hF(f, b) {
    var d,
      c = {},
      a = {}
    for (d in f) {
      d in b ? (c[d] = g1(f[d], b[d])) : (a[d] = f[d])
    }
    for (d in b) {
      d in f || (a[d] = b[d])
    }
    return function(e) {
      for (d in c) {
        a[d] = c[d](e)
      }
      return a
    }
  }
  function eC(b, a) {
    return (
      (b = +b),
      (a = +a),
      function(c) {
        return b * (1 - c) + a * c
      }
    )
  }
  function j8(d, m) {
    var h,
      b,
      k,
      g = (gj.lastIndex = gH.lastIndex = 0),
      j = -1,
      c = [],
      f = []
    for (d += '', m += ''; (h = gj.exec(d)) && (b = gH.exec(m)); ) {
      ;(k = b.index) > g &&
        ((k = m.slice(g, k)), c[j] ? (c[j] += k) : (c[++j] = k)),
        (h = h[0]) === (b = b[0])
          ? c[j]
            ? (c[j] += b)
            : (c[++j] = b)
          : ((c[++j] = null), f.push({ i: j, x: eC(h, b) })),
        (g = gH.lastIndex)
    }
    return (
      g < m.length && ((k = m.slice(g)), c[j] ? (c[j] += k) : (c[++j] = k)),
      c.length < 2
        ? f[0]
          ? ((m = f[0].x),
            function(a) {
              return m(a) + ''
            })
          : function() {
              return m
            }
        : ((m = f.length),
          function(l) {
            for (var i, a = 0; m > a; ++a) {
              c[(i = f[a]).i] = i.x(l)
            }
            return c.join('')
          })
    )
  }
  function g1(d, a) {
    for (
      var c, b = dU.interpolators.length;
      --b >= 0 && !(c = dU.interpolators[b](d, a));

    ) {}
    return c
  }
  function ab(k, f) {
    var h,
      g = [],
      c = [],
      d = k.length,
      b = f.length,
      j = Math.min(k.length, f.length)
    for (h = 0; j > h; ++h) {
      g.push(g1(k[h], f[h]))
    }
    for (; d > h; ++h) {
      c[h] = k[h]
    }
    for (; b > h; ++h) {
      c[h] = f[h]
    }
    return function(a) {
      for (h = 0; j > h; ++h) {
        c[h] = g[h](a)
      }
      return c
    }
  }
  function iX(a) {
    return function(b) {
      return 0 >= b ? 0 : b >= 1 ? 1 : a(b)
    }
  }
  function ak(a) {
    return function(b) {
      return 1 - a(1 - b)
    }
  }
  function aL(a) {
    return function(b) {
      return 0.5 * (0.5 > b ? a(2 * b) : 2 - a(2 - 2 * b))
    }
  }
  function e0(a) {
    return a * a
  }
  function fd(a) {
    return a * a * a
  }
  function gJ(c) {
    if (0 >= c) {
      return 0
    }
    if (c >= 1) {
      return 1
    }
    var a = c * c,
      b = a * c
    return 4 * (0.5 > c ? b : 3 * (c - a) + b - 0.75)
  }
  function aU(a) {
    return function(b) {
      return Math.pow(b, a)
    }
  }
  function cq(a) {
    return 1 - Math.cos(a * gA)
  }
  function bD(a) {
    return Math.pow(2, 10 * (a - 1))
  }
  function jG(a) {
    return 1 - Math.sqrt(1 - a * a)
  }
  function hp(c, a) {
    var b
    return (
      arguments.length < 2 && (a = 0.45),
      arguments.length
        ? (b = (a / aM) * Math.asin(1 / c))
        : ((c = 1), (b = a / 4)),
      function(d) {
        return 1 + c * Math.pow(2, -10 * d) * Math.sin(((d - b) * aM) / a)
      }
    )
  }
  function cZ(a) {
    return (
      a || (a = 1.70158),
      function(b) {
        return b * b * ((a + 1) * b - a)
      }
    )
  }
  function eI(a) {
    return 1 / 2.75 > a
      ? 7.5625 * a * a
      : 2 / 2.75 > a
        ? 7.5625 * (a -= 1.5 / 2.75) * a + 0.75
        : 2.5 / 2.75 > a
          ? 7.5625 * (a -= 2.25 / 2.75) * a + 0.9375
          : 7.5625 * (a -= 2.625 / 2.75) * a + 0.984375
  }
  function fm(k, f) {
    ;(k = dU.hcl(k)), (f = dU.hcl(f))
    var h = k.h,
      g = k.c,
      c = k.l,
      d = f.h - h,
      b = f.c - g,
      j = f.l - c
    return (
      isNaN(b) && ((b = 0), (g = isNaN(g) ? f.c : g)),
      isNaN(d)
        ? ((d = 0), (h = isNaN(h) ? f.h : h))
        : d > 180
          ? (d -= 360)
          : -180 > d && (d += 360),
      function(a) {
        return gX(h + d * a, g + b * a, c + j * a) + ''
      }
    )
  }
  function bd(k, f) {
    ;(k = dU.hsl(k)), (f = dU.hsl(f))
    var h = k.h,
      g = k.s,
      c = k.l,
      d = f.h - h,
      b = f.s - g,
      j = f.l - c
    return (
      isNaN(b) && ((b = 0), (g = isNaN(g) ? f.s : g)),
      isNaN(d)
        ? ((d = 0), (h = isNaN(h) ? f.h : h))
        : d > 180
          ? (d -= 360)
          : -180 > d && (d += 360),
      function(a) {
        return ib(h + d * a, g + b * a, c + j * a) + ''
      }
    )
  }
  function f0(k, f) {
    ;(k = dU.lab(k)), (f = dU.lab(f))
    var h = k.l,
      g = k.a,
      c = k.b,
      d = f.l - h,
      b = f.a - g,
      j = f.b - c
    return function(a) {
      return gF(h + d * a, g + b * a, c + j * a) + ''
    }
  }
  function eh(b, a) {
    return (
      (a -= b),
      function(c) {
        return Math.round(b + a * c)
      }
    )
  }
  function fD(g) {
    var c = [g.a, g.b],
      f = [g.c, g.d],
      d = jV(c),
      a = az(c, f),
      b = jV(gm(f, c, -a)) || 0
    c[0] * f[1] < f[0] * c[1] &&
      ((c[0] *= -1), (c[1] *= -1), (d *= -1), (a *= -1)),
      (this.rotate =
        (d ? Math.atan2(c[1], c[0]) : Math.atan2(-f[0], f[1])) * cW),
      (this.translate = [g.e, g.f]),
      (this.scale = [d, b]),
      (this.skew = b ? Math.atan2(a, b) * cW : 0)
  }
  function az(b, a) {
    return b[0] * a[0] + b[1] * a[1]
  }
  function jV(b) {
    var a = Math.sqrt(az(b, b))
    return a && ((b[0] /= a), (b[1] /= a)), a
  }
  function gm(c, a, b) {
    return (c[0] += b * a[0]), (c[1] += b * a[1]), c
  }
  function jc(a) {
    return a.length ? a.pop() + ',' : ''
  }
  function cM(f, b, d, c) {
    if (f[0] !== b[0] || f[1] !== b[1]) {
      var a = d.push('translate(', null, ',', null, ')')
      c.push({ i: a - 4, x: eC(f[0], b[0]) }, { i: a - 2, x: eC(f[1], b[1]) })
    } else {
      ;(b[0] || b[1]) && d.push('translate(' + b + ')')
    }
  }
  function cd(d, a, c, b) {
    d !== a
      ? (d - a > 180 ? (a += 360) : a - d > 180 && (d += 360),
        b.push({ i: c.push(jc(c) + 'rotate(', null, ')') - 2, x: eC(d, a) }))
      : a && c.push(jc(c) + 'rotate(' + a + ')')
  }
  function dO(d, a, c, b) {
    d !== a
      ? b.push({ i: c.push(jc(c) + 'skewX(', null, ')') - 2, x: eC(d, a) })
      : a && c.push(jc(c) + 'skewX(' + a + ')')
  }
  function c5(f, b, d, c) {
    if (f[0] !== b[0] || f[1] !== b[1]) {
      var a = d.push(jc(d) + 'scale(', null, ',', null, ')')
      c.push({ i: a - 4, x: eC(f[0], b[0]) }, { i: a - 2, x: eC(f[1], b[1]) })
    } else {
      ;(1 !== b[0] || 1 !== b[1]) && d.push(jc(d) + 'scale(' + b + ')')
    }
  }
  function dZ(d, a) {
    var c = [],
      b = []
    return (
      (d = dU.transform(d)),
      (a = dU.transform(a)),
      cM(d.translate, a.translate, c, b),
      cd(d.rotate, a.rotate, c, b),
      dO(d.skew, a.skew, c, b),
      c5(d.scale, a.scale, c, b),
      (d = a = null),
      function(h) {
        for (var g, e = -1, f = b.length; ++e < f; ) {
          c[(g = b[e]).i] = g.x(h)
        }
        return c.join('')
      }
    )
  }
  function bW(b, a) {
    return (
      (a = (a -= b = +b) || 1 / a),
      function(c) {
        return (c - b) / a
      }
    )
  }
  function dw(b, a) {
    return (
      (a = (a -= b = +b) || 1 / a),
      function(c) {
        return Math.max(0, Math.min(1, (c - b) / a))
      }
    )
  }
  function ip(g) {
    for (var c = g.source, f = g.target, d = hK(c, f), a = [c]; c !== d; ) {
      ;(c = c.parent), a.push(c)
    }
    for (var b = a.length; f !== d; ) {
      a.splice(b, 0, f), (f = f.parent)
    }
    return a
  }
  function kk(c) {
    for (var a = [], b = c.parent; null != b; ) {
      a.push(c), (c = b), (b = b.parent)
    }
    return a.push(c), a
  }
  function hK(j, f) {
    if (j === f) {
      return j
    }
    for (
      var h = kk(j), g = kk(f), c = h.pop(), d = g.pop(), b = null;
      c === d;

    ) {
      ;(b = c), (c = h.pop()), (d = g.pop())
    }
    return b
  }
  function fJ(a) {
    a.fixed |= 2
  }
  function d3(a) {
    a.fixed &= -7
  }
  function bJ(a) {
    ;(a.fixed |= 4), (a.px = a.x), (a.py = a.y)
  }
  function hc(a) {
    a.fixed &= -5
  }
  function cC(f, q, j) {
    var b = 0,
      p = 0
    if (((f.charge = 0), !f.leaf)) {
      for (var h, m = f.nodes, d = m.length, g = -1; ++g < d; ) {
        ;(h = m[g]),
          null != h &&
            (cC(h, q, j),
            (f.charge += h.charge),
            (b += h.charge * h.cx),
            (p += h.charge * h.cy))
      }
    }
    if (f.point) {
      f.leaf ||
        ((f.point.x += Math.random() - 0.5), (f.point.y += Math.random() - 0.5))
      var k = q * j[f.point.index]
      ;(f.charge += f.pointCharge = k),
        (b += k * f.point.x),
        (p += k * f.point.y)
    }
    ;(f.cx = b / f.charge), (f.cy = p / f.charge)
  }
  function bm(b, a) {
    return (
      dU.rebind(b, a, 'sort', 'children', 'value'),
      (b.nodes = b),
      (b.links = b2),
      b
    )
  }
  function fT(f, b) {
    for (var d = [f]; null != (f = d.pop()); ) {
      if ((b(f), (a = f.children) && (c = a.length))) {
        for (var c, a; --c >= 0; ) {
          d.push(a[c])
        }
      }
    }
  }
  function jK(j, f) {
    for (var h = [j], g = []; null != (j = h.pop()); ) {
      if ((g.push(j), (d = j.children) && (c = d.length))) {
        for (var c, d, b = -1; ++b < c; ) {
          h.push(d[b])
        }
      }
    }
    for (; null != (j = g.pop()); ) {
      f(j)
    }
  }
  function dG(a) {
    return a.children
  }
  function eS(a) {
    return a.value
  }
  function hZ(b, a) {
    return a.value - b.value
  }
  function b2(a) {
    return dU.merge(
      a.map(function(b) {
        return (b.children || []).map(function(c) {
          return { source: b, target: c }
        })
      })
    )
  }
  function gT(a) {
    return a.x
  }
  function gb(a) {
    return a.y
  }
  function gB(c, a, b) {
    ;(c.y0 = a), (c.y = b)
  }
  function dh(a) {
    return dU.range(a.length)
  }
  function a0(d) {
    for (var a = -1, c = d[0].length, b = []; ++a < c; ) {
      b[a] = 0
    }
    return b
  }
  function hD(g) {
    for (var c, f = 1, d = 0, a = g[0][1], b = g.length; b > f; ++f) {
      ;(c = g[f][1]) > a && ((d = f), (a = c))
    }
    return d
  }
  function eA(a) {
    return a.reduce(j5, 0)
  }
  function j5(b, a) {
    return b + a[1]
  }
  function gY(b, a) {
    return kw(b, Math.ceil(Math.log(a.length) / Math.LN2 + 1))
  }
  function kw(g, c) {
    for (var f = -1, d = +g[0], a = (g[1] - d) / c, b = []; ++f <= c; ) {
      b[f] = a * f + d
    }
    return b
  }
  function iR(a) {
    return [dU.min(a), dU.max(a)]
  }
  function ag(b, a) {
    return b.value - a.value
  }
  function aI(c, a) {
    var b = c._pack_next
    ;(c._pack_next = a),
      (a._pack_prev = c),
      (a._pack_next = b),
      (b._pack_prev = a)
  }
  function eX(b, a) {
    ;(b._pack_next = a), (a._pack_prev = b)
  }
  function fb(f, b) {
    var d = b.x - f.x,
      c = b.y - f.y,
      a = f.r + b.r
    return 0.999 * a * a > d * d + c * c
  }
  function gG(C) {
    function w(a) {
      ;(x = Math.min(a.x - a.r, x)),
        (I = Math.max(a.x + a.r, I)),
        (G = Math.min(a.y - a.r, G)),
        (H = Math.max(a.y + a.r, H))
    }
    if ((J = C.children) && (L = J.length)) {
      var J,
        z,
        q,
        F,
        N,
        B,
        E,
        L,
        x = 1 / 0,
        I = -(1 / 0),
        G = 1 / 0,
        H = -(1 / 0)
      if (
        (J.forEach(aR),
        (z = J[0]),
        (z.x = -z.r),
        (z.y = 0),
        w(z),
        L > 1 && ((q = J[1]), (q.x = q.r), (q.y = 0), w(q), L > 2))
      ) {
        for (
          F = J[2],
            jD(z, q, F),
            w(F),
            aI(z, F),
            z._pack_prev = F,
            aI(F, q),
            q = z._pack_next,
            N = 3;
          L > N;
          N++
        ) {
          jD(z, q, (F = J[N]))
          var A = 0,
            k = 1,
            K = 1
          for (B = q._pack_next; B !== q; B = B._pack_next, k++) {
            if (fb(B, F)) {
              A = 1
              break
            }
          }
          if (1 == A) {
            for (
              E = z._pack_prev;
              E !== B._pack_prev && !fb(E, F);
              E = E._pack_prev, K++
            ) {}
          }
          A
            ? (K > k || (k == K && q.r < z.r) ? eX(z, (q = B)) : eX((z = E), q),
              N--)
            : (aI(z, F), (q = F), w(F))
        }
      }
      var D = (x + I) / 2,
        j = (G + H) / 2,
        b = 0
      for (N = 0; L > N; N++) {
        ;(F = J[N]),
          (F.x -= D),
          (F.y -= j),
          (b = Math.max(b, F.r + Math.sqrt(F.x * F.x + F.y * F.y)))
      }
      ;(C.r = b), J.forEach(cj)
    }
  }
  function aR(a) {
    a._pack_next = a._pack_prev = a
  }
  function cj(a) {
    delete a._pack_next, delete a._pack_prev
  }
  function bA(j, f, h, g) {
    var c = j.children
    if (((j.x = f += g * j.x), (j.y = h += g * j.y), (j.r *= g), c)) {
      for (var d = -1, b = c.length; ++d < b; ) {
        bA(c[d], f, h, g)
      }
    }
  }
  function jD(f, q, j) {
    var b = f.r + j.r,
      p = q.x - f.x,
      h = q.y - f.y
    if (b && (p || h)) {
      var m = q.r + j.r,
        d = p * p + h * h
      ;(m *= m), (b *= b)
      var g = 0.5 + (b - m) / (2 * d),
        k =
          Math.sqrt(Math.max(0, 2 * m * (b + d) - (b -= d) * b - m * m)) /
          (2 * d)
      ;(j.x = f.x + g * p + k * h), (j.y = f.y + g * h - k * p)
    } else {
      ;(j.x = f.x + b), (j.y = f.y)
    }
  }
  function hj(b, a) {
    return b.parent == a.parent ? 1 : 2
  }
  function cX(b) {
    var a = b.children
    return a.length ? a[0] : b.t
  }
  function eF(c) {
    var a,
      b = c.children
    return (a = b.length) ? b[a - 1] : c.t
  }
  function fh(d, a, c) {
    var b = c / (a.i - d.i)
    ;(a.c -= b), (a.s += c), (d.c += b), (a.z += c), (a.m += c)
  }
  function a9(g) {
    for (var c, f = 0, d = 0, a = g.children, b = a.length; --b >= 0; ) {
      ;(c = a[b]), (c.z += f), (c.m += f), (f += c.s + (d += c.c))
    }
  }
  function fX(c, a, b) {
    return c.a.parent === a.parent ? c.a : b
  }
  function eb(a) {
    return (
      1 +
      dU.max(a, function(b) {
        return b.y
      })
    )
  }
  function fB(a) {
    return (
      a.reduce(function(c, b) {
        return c + b.x
      }, 0) / a.length
    )
  }
  function av(b) {
    var a = b.children
    return a && a.length ? av(a[0]) : b
  }
  function jR(c) {
    var a,
      b = c.children
    return b && (a = b.length) ? jR(b[a - 1]) : c
  }
  function gh(a) {
    return { x: a.x, y: a.y, dx: a.dx, dy: a.dy }
  }
  function i4(g, c) {
    var f = g.x + c[3],
      d = g.y + c[0],
      a = g.dx - c[1] - c[3],
      b = g.dy - c[0] - c[2]
    return (
      0 > a && ((f += a / 2), (a = 0)),
      0 > b && ((d += b / 2), (b = 0)),
      { x: f, y: d, dx: a, dy: b }
    )
  }
  function cJ(c) {
    var a = c[0],
      b = c[c.length - 1]
    return b > a ? [a, b] : [b, a]
  }
  function b9(a) {
    return a.rangeExtent ? a.rangeExtent() : cJ(a.range())
  }
  function dL(g, c, f, d) {
    var a = f(g[0], g[1]),
      b = d(c[0], c[1])
    return function(e) {
      return b(a(e))
    }
  }
  function c2(j, f) {
    var h,
      g = 0,
      c = j.length - 1,
      d = j[g],
      b = j[c]
    return (
      d > b && ((h = g), (g = c), (c = h), (h = d), (d = b), (b = h)),
      (j[g] = f.floor(d)),
      (j[c] = f.ceil(b)),
      j
    )
  }
  function dW(a) {
    return a
      ? {
          floor: function(b) {
            return Math.floor(b / a) * a
          },
          ceil: function(b) {
            return Math.ceil(b / a) * a
          },
        }
      : e5
  }
  function bS(k, f, h, g) {
    var c = [],
      d = [],
      b = 0,
      j = Math.min(k.length, f.length) - 1
    for (
      k[j] < k[0] && ((k = k.slice().reverse()), (f = f.slice().reverse()));
      ++b <= j;

    ) {
      c.push(h(k[b - 1], k[b])), d.push(g(f[b - 1], f[b]))
    }
    return function(a) {
      var i = dU.bisect(k, a, 1, j) - 1
      return d[i](c[i](a))
    }
  }
  function dq(k, f, h, g) {
    function c() {
      var e = Math.min(k.length, f.length) > 2 ? bS : dL,
        a = g ? dw : bW
      return (b = e(k, f, a, h)), (j = e(f, k, a, g1)), d
    }
    function d(a) {
      return b(a)
    }
    var b, j
    return (
      (d.invert = function(a) {
        return j(a)
      }),
      (d.domain = function(a) {
        return arguments.length ? ((k = a.map(Number)), c()) : k
      }),
      (d.range = function(a) {
        return arguments.length ? ((f = a), c()) : f
      }),
      (d.rangeRound = function(a) {
        return d.range(a).interpolate(eh)
      }),
      (d.clamp = function(a) {
        return arguments.length ? ((g = a), c()) : g
      }),
      (d.interpolate = function(a) {
        return arguments.length ? ((h = a), c()) : h
      }),
      (d.ticks = function(a) {
        return fG(k, a)
      }),
      (d.tickFormat = function(a, i) {
        return ej(k, a, i)
      }),
      (d.nice = function(a) {
        return kg(k, a), c()
      }),
      (d.copy = function() {
        return dq(k, f, h, g)
      }),
      c()
    )
  }
  function ic(b, a) {
    return dU.rebind(b, a, 'range', 'rangeRound', 'interpolate', 'clamp')
  }
  function kg(b, a) {
    return c2(b, dW(hH(b, a)[2])), c2(b, dW(hH(b, a)[2])), b
  }
  function hH(g, c) {
    null == c && (c = 10)
    var f = cJ(g),
      d = f[1] - f[0],
      a = Math.pow(10, Math.floor(Math.log(d / c) / Math.LN10)),
      b = (c / d) * a
    return (
      0.15 >= b ? (a *= 10) : 0.35 >= b ? (a *= 5) : 0.75 >= b && (a *= 2),
      (f[0] = Math.ceil(f[0] / a) * a),
      (f[1] = Math.floor(f[1] / a) * a + 0.5 * a),
      (f[2] = a),
      f
    )
  }
  function fG(b, a) {
    return dU.range.apply(dU, hH(b, a))
  }
  function ej(g, c, f) {
    var d = hH(g, c)
    if (f) {
      var a = gW.exec(f)
      if ((a.shift(), 's' === a[8])) {
        var b = dU.formatPrefix(Math.max(hb(d[0]), hb(d[1])))
        return (
          a[7] || (a[7] = '.' + bR(b.scale(d[2]))),
          (a[8] = 'f'),
          (f = dU.format(a.join(''))),
          function(e) {
            return f(b.scale(e)) + b.symbol
          }
        )
      }
      a[7] || (a[7] = '.' + hq(a[8], d)), (f = a.join(''))
    } else {
      f = ',.' + bR(d[2]) + 'f'
    }
    return dU.format(f)
  }
  function bR(a) {
    return -Math.floor(Math.log(a) / Math.LN10 + 0.01)
  }
  function hq(c, a) {
    var b = bR(a[2])
    return c in fj
      ? Math.abs(b - bR(Math.max(hb(a[0]), hb(a[1])))) + +('e' !== c)
      : b - 2 * ('%' === c)
  }
  function cN(j, f, h, g) {
    function c(a) {
      return (
        (h ? Math.log(0 > a ? 0 : a) : -Math.log(a > 0 ? 0 : -a)) / Math.log(f)
      )
    }
    function d(a) {
      return h ? Math.pow(f, a) : -Math.pow(f, -a)
    }
    function b(a) {
      return j(c(a))
    }
    return (
      (b.invert = function(a) {
        return d(j.invert(a))
      }),
      (b.domain = function(a) {
        return arguments.length
          ? ((h = a[0] >= 0), j.domain((g = a.map(Number)).map(c)), b)
          : g
      }),
      (b.base = function(a) {
        return arguments.length ? ((f = +a), j.domain(g.map(c)), b) : f
      }),
      (b.nice = function() {
        var a = c2(g.map(c), h ? Math : aY)
        return j.domain(a), (g = a.map(d)), b
      }),
      (b.ticks = function() {
        var t = cJ(g),
          i = [],
          q = t[0],
          e = t[1],
          r = Math.floor(c(q)),
          m = Math.ceil(c(e)),
          p = f % 1 ? 2 : f
        if (isFinite(m - r)) {
          if (h) {
            for (; m > r; r++) {
              for (var k = 1; p > k; k++) {
                i.push(d(r) * k)
              }
            }
            i.push(d(r))
          } else {
            for (i.push(d(r)); r++ < m; ) {
              for (var k = p - 1; k > 0; k--) {
                i.push(d(r) * k)
              }
            }
          }
          for (r = 0; i[r] < q; r++) {}
          for (m = i.length; i[m - 1] > e; m--) {}
          i = i.slice(r, m)
        }
        return i
      }),
      (b.tickFormat = function(k, i) {
        if (!arguments.length) {
          return gO
        }
        arguments.length < 2
          ? (i = gO)
          : 'function' != typeof i && (i = dU.format(i))
        var a = Math.max(1, (f * k) / b.ticks().length)
        return function(l) {
          var e = l / d(Math.round(c(l)))
          return f - 0.5 > e * f && (e *= f), a >= e ? i(l) : ''
        }
      }),
      (b.copy = function() {
        return cN(j.copy(), f, h, g)
      }),
      ic(b, j)
    )
  }
  function bz(g, c, f) {
    function d(e) {
      return g(a(e))
    }
    var a = f1(c),
      b = f1(1 / c)
    return (
      (d.invert = function(e) {
        return b(g.invert(e))
      }),
      (d.domain = function(e) {
        return arguments.length ? (g.domain((f = e.map(Number)).map(a)), d) : f
      }),
      (d.ticks = function(e) {
        return fG(f, e)
      }),
      (d.tickFormat = function(h, e) {
        return ej(f, h, e)
      }),
      (d.nice = function(e) {
        return d.domain(kg(f, e))
      }),
      (d.exponent = function(e) {
        return arguments.length
          ? ((a = f1((c = e))), (b = f1(1 / c)), g.domain(f.map(a)), d)
          : c
      }),
      (d.copy = function() {
        return bz(g.copy(), c, f)
      }),
      ic(d, g)
    )
  }
  function f1(a) {
    return function(b) {
      return 0 > b ? -Math.pow(-b, a) : Math.pow(b, a)
    }
  }
  function jX(j, f) {
    function h(a) {
      return d[
        ((c.get(a) || ('range' === f.t ? c.set(a, j.push(a)) : NaN)) - 1) %
          d.length
      ]
    }
    function g(a, i) {
      return dU.range(j.length).map(function(e) {
        return a + i * e
      })
    }
    var c, d, b
    return (
      (h.domain = function(l) {
        if (!arguments.length) {
          return j
        }
        ;(j = []), (c = new iG())
        for (var k, e = -1, m = l.length; ++e < m; ) {
          c.has((k = l[e])) || c.set(k, j.push(k))
        }
        return h[f.t].apply(h, f.a)
      }),
      (h.range = function(a) {
        return arguments.length
          ? ((d = a), (b = 0), (f = { t: 'range', a: arguments }), h)
          : d
      }),
      (h.rangePoints = function(e, k) {
        arguments.length < 2 && (k = 0)
        var a = e[0],
          m = e[1],
          i =
            j.length < 2 ? ((a = (a + m) / 2), 0) : (m - a) / (j.length - 1 + k)
        return (
          (d = g(a + (i * k) / 2, i)),
          (b = 0),
          (f = { t: 'rangePoints', a: arguments }),
          h
        )
      }),
      (h.rangeRoundPoints = function(e, k) {
        arguments.length < 2 && (k = 0)
        var a = e[0],
          m = e[1],
          i =
            j.length < 2
              ? ((a = m = Math.round((a + m) / 2)), 0)
              : ((m - a) / (j.length - 1 + k)) | 0
        return (
          (d = g(
            a + Math.round((i * k) / 2 + (m - a - (j.length - 1 + k) * i) / 2),
            i
          )),
          (b = 0),
          (f = { t: 'rangeRoundPoints', a: arguments }),
          h
        )
      }),
      (h.rangeBands = function(e, n, a) {
        arguments.length < 2 && (n = 0), arguments.length < 3 && (a = n)
        var p = e[1] < e[0],
          k = e[p - 0],
          m = e[1 - p],
          i = (m - k) / (j.length - n + 2 * a)
        return (
          (d = g(k + i * a, i)),
          p && d.reverse(),
          (b = i * (1 - n)),
          (f = { t: 'rangeBands', a: arguments }),
          h
        )
      }),
      (h.rangeRoundBands = function(e, n, a) {
        arguments.length < 2 && (n = 0), arguments.length < 3 && (a = n)
        var p = e[1] < e[0],
          k = e[p - 0],
          m = e[1 - p],
          i = Math.floor((m - k) / (j.length - n + 2 * a))
        return (
          (d = g(k + Math.round((m - k - (j.length - n) * i) / 2), i)),
          p && d.reverse(),
          (b = Math.round(i * (1 - n))),
          (f = { t: 'rangeRoundBands', a: arguments }),
          h
        )
      }),
      (h.rangeBand = function() {
        return b
      }),
      (h.rangeExtent = function() {
        return cJ(f.a[0])
      }),
      (h.copy = function() {
        return jX(j, f)
      }),
      h.domain(j)
    )
  }
  function dP(f, d) {
    function c() {
      var g = 0,
        a = d.length
      for (e = []; ++g < a; ) {
        e[g - 1] = dU.quantile(f, g / a)
      }
      return b
    }
    function b(a) {
      return isNaN((a = +a)) ? void 0 : d[dU.bisect(e, a)]
    }
    var e
    return (
      (b.domain = function(a) {
        return arguments.length
          ? ((f = a
              .map(h5)
              .filter(h0)
              .sort(iE)),
            c())
          : f
      }),
      (b.range = function(a) {
        return arguments.length ? ((d = a), c()) : d
      }),
      (b.quantiles = function() {
        return e
      }),
      (b.invertExtent = function(a) {
        return (
          (a = d.indexOf(a)),
          0 > a
            ? [NaN, NaN]
            : [a > 0 ? e[a - 1] : f[0], a < e.length ? e[a] : f[f.length - 1]]
        )
      }),
      (b.copy = function() {
        return dP(f, d)
      }),
      c()
    )
  }
  function e1(j, f, h) {
    function g(a) {
      return h[Math.max(0, Math.min(b, Math.floor(d * (a - j))))]
    }
    function c() {
      return (d = h.length / (f - j)), (b = h.length - 1), g
    }
    var d, b
    return (
      (g.domain = function(a) {
        return arguments.length
          ? ((j = +a[0]), (f = +a[a.length - 1]), c())
          : [j, f]
      }),
      (g.range = function(a) {
        return arguments.length ? ((h = a), c()) : h
      }),
      (g.invertExtent = function(a) {
        return (a = h.indexOf(a)), (a = 0 > a ? NaN : a / d + j), [a, a + 1 / d]
      }),
      (g.copy = function() {
        return e1(j, f, h)
      }),
      c()
    )
  }
  function iq(c, a) {
    function b(d) {
      return d >= d ? a[dU.bisect(c, d)] : void 0
    }
    return (
      (b.domain = function(d) {
        return arguments.length ? ((c = d), b) : c
      }),
      (b.range = function(d) {
        return arguments.length ? ((a = d), b) : a
      }),
      (b.invertExtent = function(d) {
        return (d = a.indexOf(d)), [c[d - 1], c[d]]
      }),
      (b.copy = function() {
        return iq(c, a)
      }),
      b
    )
  }
  function cf(b) {
    function a(c) {
      return +c
    }
    return (
      (a.invert = a),
      (a.domain = a.range = function(c) {
        return arguments.length ? ((b = c.map(a)), a) : b
      }),
      (a.ticks = function(c) {
        return fG(b, c)
      }),
      (a.tickFormat = function(c, d) {
        return ej(b, c, d)
      }),
      (a.copy = function() {
        return cf(b)
      }),
      a
    )
  }
  function g2() {
    return 0
  }
  function gp(a) {
    return a.innerRadius
  }
  function gK(a) {
    return a.outerRadius
  }
  function dx(a) {
    return a.startAngle
  }
  function a8(a) {
    return a.endAngle
  }
  function hL(a) {
    return a && a.padAngle
  }
  function eJ(d, a, c, b) {
    return (d - c) * a - (a - b) * d > 0 ? 0 : 1
  }
  function kf(T, J, ae, O, H) {
    var X = T[0] - J[0],
      ao = T[1] - J[1],
      R = (H ? O : -O) / Math.sqrt(X * X + ao * ao),
      V = R * ao,
      al = -R * X,
      K = T[0] + V,
      aa = T[1] + al,
      Y = J[0] + V,
      Z = J[1] + al,
      Q = (K + Y) / 2,
      G = (aa + Z) / 2,
      ai = Y - K,
      U = Z - aa,
      B = ai * ai + U * U,
      z = ae - O,
      D = K * Z - Y * aa,
      an = (0 > U ? -1 : 1) * Math.sqrt(Math.max(0, z * z * B - D * D)),
      ar = (D * U - ai * an) / B,
      F = (-D * ai - U * an) / B,
      j = (D * U + ai * an) / B,
      W = (-D * ai + U * an) / B,
      q = ar - Q,
      I = F - G,
      P = j - Q,
      L = W - G
    return (
      q * q + I * I > P * P + L * L && ((ar = j), (F = W)),
      [[ar - V, F - al], [(ar * ae) / z, (F * ae) / z]]
    )
  }
  function g8(k) {
    function f(u) {
      function r() {
        q.push('M', d(k(v), j))
      }
      for (
        var i, q = [], v = [], o = -1, m = u.length, n = aX(h), e = aX(g);
        ++o < m;

      ) {
        c.call(this, (i = u[o]), o)
          ? v.push([+n.call(this, i, o), +e.call(this, i, o)])
          : v.length && (r(), (v = []))
      }
      return v.length && r(), q.length ? q.join('') : null
    }
    var h = bK,
      g = jS,
      c = jF,
      d = am,
      b = d.key,
      j = 0.7
    return (
      (f.x = function(a) {
        return arguments.length ? ((h = a), f) : h
      }),
      (f.y = function(a) {
        return arguments.length ? ((g = a), f) : g
      }),
      (f.defined = function(a) {
        return arguments.length ? ((c = a), f) : c
      }),
      (f.interpolate = function(a) {
        return arguments.length
          ? ((b = 'function' == typeof a ? (d = a) : (d = eN.get(a) || am).key),
            f)
          : b
      }),
      (f.tension = function(a) {
        return arguments.length ? ((j = a), f) : j
      }),
      f
    )
  }
  function am(a) {
    return a.length > 1 ? a.join('L') : a + 'Z'
  }
  function jd(a) {
    return a.join('L') + 'Z'
  }
  function aB(f) {
    for (var b = 0, d = f.length, c = f[0], a = [c[0], ',', c[1]]; ++b < d; ) {
      a.push('H', (c[0] + (c = f[b])[0]) / 2, 'V', c[1])
    }
    return d > 1 && a.push('H', c[0]), a.join('')
  }
  function aQ(f) {
    for (var b = 0, d = f.length, c = f[0], a = [c[0], ',', c[1]]; ++b < d; ) {
      a.push('V', (c = f[b])[1], 'H', c[0])
    }
    return a.join('')
  }
  function e7(f) {
    for (var b = 0, d = f.length, c = f[0], a = [c[0], ',', c[1]]; ++b < d; ) {
      a.push('H', (c = f[b])[0], 'V', c[1])
    }
    return a.join('')
  }
  function fp(b, a) {
    return b.length < 4 ? am(b) : b[1] + cB(b.slice(1, -1), bI(b, a))
  }
  function gQ(b, a) {
    return b.length < 3
      ? jd(b)
      : b[0] + cB((b.push(b[0]), b), bI([b[b.length - 2]].concat(b, [b[1]]), a))
  }
  function aZ(b, a) {
    return b.length < 3 ? am(b) : b[0] + cB(b, bI(b, a))
  }
  function cB(f, q) {
    if (q.length < 1 || (f.length != q.length && f.length != q.length + 2)) {
      return am(f)
    }
    var j = f.length != q.length,
      b = '',
      p = f[0],
      h = f[1],
      m = q[0],
      d = m,
      g = 1
    if (
      (j &&
        ((b +=
          'Q' +
          (h[0] - (2 * m[0]) / 3) +
          ',' +
          (h[1] - (2 * m[1]) / 3) +
          ',' +
          h[0] +
          ',' +
          h[1]),
        (p = f[1]),
        (g = 2)),
      q.length > 1)
    ) {
      ;(d = q[1]),
        (h = f[g]),
        g++,
        (b +=
          'C' +
          (p[0] + m[0]) +
          ',' +
          (p[1] + m[1]) +
          ',' +
          (h[0] - d[0]) +
          ',' +
          (h[1] - d[1]) +
          ',' +
          h[0] +
          ',' +
          h[1])
      for (var k = 2; k < q.length; k++, g++) {
        ;(h = f[g]),
          (d = q[k]),
          (b +=
            'S' + (h[0] - d[0]) + ',' + (h[1] - d[1]) + ',' + h[0] + ',' + h[1])
      }
    }
    if (j) {
      var v = f[g]
      b +=
        'Q' +
        (h[0] + (2 * d[0]) / 3) +
        ',' +
        (h[1] + (2 * d[1]) / 3) +
        ',' +
        v[0] +
        ',' +
        v[1]
    }
    return b
  }
  function bI(d, m) {
    for (
      var h, b = [], k = (1 - m) / 2, g = d[0], j = d[1], c = 1, f = d.length;
      ++c < f;

    ) {
      ;(h = g),
        (g = j),
        (j = d[c]),
        b.push([k * (j[0] - h[0]), k * (j[1] - h[1])])
    }
    return b
  }
  function jO(d) {
    if (d.length < 3) {
      return am(d)
    }
    var m = 1,
      h = d.length,
      b = d[0],
      k = b[0],
      g = b[1],
      j = [k, k, k, (b = d[1])[0]],
      c = [g, g, g, b[1]],
      f = [k, ',', g, 'L', fy(f5, j), ',', fy(f5, c)]
    for (d.push(d[h - 1]); ++m <= h; ) {
      ;(b = d[m]), j.shift(), j.push(b[0]), c.shift(), c.push(b[1]), bk(f, j, c)
    }
    return d.pop(), f.push('L', b), f.join('')
  }
  function hA(j) {
    if (j.length < 4) {
      return am(j)
    }
    for (var f, h = [], g = -1, c = j.length, d = [0], b = [0]; ++g < 3; ) {
      ;(f = j[g]), d.push(f[0]), b.push(f[1])
    }
    for (h.push(fy(f5, d) + ',' + fy(f5, b)), --g; ++g < c; ) {
      ;(f = j[g]), d.shift(), d.push(f[0]), b.shift(), b.push(f[1]), bk(h, d, b)
    }
    return h.join('')
  }
  function c6(k) {
    for (var f, h, g = -1, c = k.length, d = c + 4, b = [], j = []; ++g < 4; ) {
      ;(h = k[g % c]), b.push(h[0]), j.push(h[1])
    }
    for (f = [fy(f5, b), ',', fy(f5, j)], --g; ++g < d; ) {
      ;(h = k[g % c]),
        b.shift(),
        b.push(h[0]),
        j.shift(),
        j.push(h[1]),
        bk(f, b, j)
    }
    return f.join('')
  }
  function eP(f, q) {
    var j = f.length - 1
    if (j) {
      for (
        var b,
          p,
          h = f[0][0],
          m = f[0][1],
          d = f[j][0] - h,
          g = f[j][1] - m,
          k = -1;
        ++k <= j;

      ) {
        ;(b = f[k]),
          (p = k / j),
          (b[0] = q * b[0] + (1 - q) * (h + p * d)),
          (b[1] = q * b[1] + (1 - q) * (m + p * g))
      }
    }
    return jO(f)
  }
  function fy(b, a) {
    return b[0] * a[0] + b[1] * a[1] + b[2] * a[2] + b[3] * a[3]
  }
  function bk(c, a, b) {
    c.push(
      'C',
      fy(fw, a),
      ',',
      fy(fw, b),
      ',',
      fy(bj, a),
      ',',
      fy(bj, b),
      ',',
      fy(f5, a),
      ',',
      fy(f5, b)
    )
  }
  function f7(b, a) {
    return (a[1] - b[1]) / (a[0] - b[0])
  }
  function ex(j) {
    for (
      var f = 0,
        h = j.length - 1,
        g = [],
        c = j[0],
        d = j[1],
        b = (g[0] = f7(c, d));
      ++f < h;

    ) {
      g[f] = (b + (b = f7((c = d), (d = j[f + 1])))) / 2
    }
    return (g[f] = b), g
  }
  function fK(d) {
    for (
      var m, h, b, k, g = [], j = ex(d), c = -1, f = d.length - 1;
      ++c < f;

    ) {
      ;(m = f7(d[c], d[c + 1])),
        hb(m) < f9
          ? (j[c] = j[c + 1] = 0)
          : ((h = j[c] / m),
            (b = j[c + 1] / m),
            (k = h * h + b * b),
            k > 9 &&
              ((k = (3 * m) / Math.sqrt(k)),
              (j[c] = k * h),
              (j[c + 1] = k * b)))
    }
    for (c = -1; ++c <= f; ) {
      ;(k =
        (d[Math.min(f, c + 1)][0] - d[Math.max(0, c - 1)][0]) /
        (6 * (1 + j[c] * j[c]))),
        g.push([k || 0, j[c] * k || 0])
    }
    return g
  }
  function aH(a) {
    return a.length < 3 ? am(a) : a[0] + cB(a, fK(a))
  }
  function j3(g) {
    for (var c, f, d, a = -1, b = g.length; ++a < b; ) {
      ;(c = g[a]),
        (f = c[0]),
        (d = c[1] - gA),
        (c[0] = f * Math.cos(d)),
        (c[1] = f * Math.sin(d))
    }
    return g
  }
  function gy(g) {
    function w(C) {
      function e() {
        B.push('M', d(g(c), k), x, p(g(o.reverse()), k), 'Z')
      }
      for (
        var f,
          i,
          a,
          B = [],
          o = [],
          c = [],
          s = -1,
          n = C.length,
          u = aX(m),
          r = aX(v),
          A =
            m === b
              ? function() {
                  return i
                }
              : aX(b),
          z =
            v === j
              ? function() {
                  return a
                }
              : aX(j);
        ++s < n;

      ) {
        q.call(this, (f = C[s]), s)
          ? (o.push([(i = +u.call(this, f, s)), (a = +r.call(this, f, s))]),
            c.push([+A.call(this, f, s), +z.call(this, f, s)]))
          : o.length && (e(), (o = []), (c = []))
      }
      return o.length && e(), B.length ? B.join('') : null
    }
    var m = bK,
      b = bK,
      v = 0,
      j = jS,
      q = jF,
      d = am,
      h = d.key,
      p = d,
      x = 'L',
      k = 0.7
    return (
      (w.x = function(a) {
        return arguments.length ? ((m = b = a), w) : b
      }),
      (w.x0 = function(a) {
        return arguments.length ? ((m = a), w) : m
      }),
      (w.x1 = function(a) {
        return arguments.length ? ((b = a), w) : b
      }),
      (w.y = function(a) {
        return arguments.length ? ((v = j = a), w) : j
      }),
      (w.y0 = function(a) {
        return arguments.length ? ((v = a), w) : v
      }),
      (w.y1 = function(a) {
        return arguments.length ? ((j = a), w) : j
      }),
      (w.defined = function(a) {
        return arguments.length ? ((q = a), w) : q
      }),
      (w.interpolate = function(a) {
        return arguments.length
          ? ((h = 'function' == typeof a ? (d = a) : (d = eN.get(a) || am).key),
            (p = d.reverse || d),
            (x = d.closed ? 'M' : 'L'),
            w)
          : h
      }),
      (w.tension = function(a) {
        return arguments.length ? ((k = a), w) : k
      }),
      w
    )
  }
  function jz(a) {
    return a.radius
  }
  function cU(a) {
    return [a.x, a.y]
  }
  function cv(a) {
    return function() {
      var b = a.apply(this, arguments),
        d = b[0],
        c = b[1] - gA
      return [d * Math.cos(c), d * Math.sin(c)]
    }
  }
  function dV() {
    return 64
  }
  function dd() {
    return 'circle'
  }
  function d7(b) {
    var a = Math.sqrt(b / fP)
    return (
      'M0,' +
      a +
      'A' +
      a +
      ',' +
      a +
      ' 0 1,1 0,' +
      -a +
      'A' +
      a +
      ',' +
      a +
      ' 0 1,1 0,' +
      a +
      'Z'
    )
  }
  function b1(a) {
    return function() {
      var b, d, c
      ;(b = this[a]) &&
        (c = b[(d = b.active)]) &&
        ((c.timer.c = null),
        (c.timer.t = NaN),
        --b.count ? delete b[d] : delete this[a],
        (b.active += 0.5),
        c.event && c.event.interrupt.call(this, this.__data__, c.index))
    }
  }
  function dD(c, a, b) {
    return e9(c, cR), (c.namespace = a), (c.id = b), c
  }
  function iI(g, c, f, d) {
    var a = g.id,
      b = g.namespace
    return iP(
      g,
      'function' == typeof f
        ? function(i, e, h) {
            i[b][a].tween.set(c, d(f.call(i, i.__data__, e, h)))
          }
        : ((f = d(f)),
          function(e) {
            e[b][a].tween.set(c, f)
          })
    )
  }
  function kv(a) {
    return (
      null == a && (a = ''),
      function() {
        this.textContent = a
      }
    )
  }
  function hR(a) {
    return null == a ? '__transition__' : '__transition_' + a + '__'
  }
  function fQ(j, C, y, b, B) {
    function m(e) {
      var a = A.delay
      return (D.t = a + k), e >= a ? z(e - a) : void (D.c = z)
    }
    function z(l) {
      var g = c.active,
        h = c[g]
      h &&
        ((h.timer.c = null),
        (h.timer.t = NaN),
        --c.count,
        delete c[g],
        h.event && h.event.interrupt.call(j, j.__data__, h.index))
      for (var f in c) {
        if (b > +f) {
          var n = c[f]
          ;(n.timer.c = null), (n.timer.t = NaN), --c.count, delete c[f]
        }
      }
      ;(D.c = d),
        c1(
          function() {
            return D.c && d(l || 1) && ((D.c = null), (D.t = NaN)), 1
          },
          0,
          k
        ),
        (c.active = b),
        A.event && A.event.start.call(j, j.__data__, C),
        (w = []),
        A.tween.forEach(function(i, a) {
          ;(a = a.call(j, j.__data__, C)) && w.push(a)
        }),
        (q = A.ease),
        (x = A.duration)
    }
    function d(f) {
      for (var g = f / x, e = q(g), h = w.length; h > 0; ) {
        w[--h].call(j, e)
      }
      return g >= 1
        ? (A.event && A.event.end.call(j, j.__data__, C),
          --c.count ? delete c[b] : delete j[y],
          1)
        : void 0
    }
    var k,
      D,
      x,
      q,
      w,
      c = j[y] || (j[y] = { active: 0, count: 0 }),
      A = c[b]
    A ||
      ((k = B.time),
      (D = c1(m, 0, k)),
      (A = c[b] = {
        tween: new iG(),
        time: k,
        timer: D,
        delay: B.delay,
        duration: B.duration,
        ease: B.ease,
        index: C,
      }),
      (B = null),
      ++c.count)
  }
  function ew(c, a, b) {
    c.attr('transform', function(e) {
      var d = a(e)
      return 'translate(' + (isFinite(d) ? d : b(e)) + ',0)'
    })
  }
  function bX(c, a, b) {
    c.attr('transform', function(e) {
      var d = a(e)
      return 'translate(0,' + (isFinite(d) ? d : b(e)) + ')'
    })
  }
  function hz(a) {
    return a.toISOString()
  }
  function cT(f, b, d) {
    function c(e) {
      return f(e)
    }
    function a(l, k) {
      var j = l[1] - l[0],
        g = j / k,
        h = dU.bisect(hP, g)
      return h == hP.length
        ? [
            b.year,
            hH(
              l.map(function(e) {
                return e / 31536000000
              }),
              k
            )[2],
          ]
        : h
          ? b[g / hP[h - 1] < hP[h] / g ? h - 1 : h]
          : [bV, hH(l, k)[2]]
    }
    return (
      (c.invert = function(e) {
        return bE(f.invert(e))
      }),
      (c.domain = function(e) {
        return arguments.length ? (f.domain(e), c) : f.domain().map(bE)
      }),
      (c.nice = function(m, j) {
        function k(i) {
          return !isNaN(i) && !m.range(i, bE(+i + 1), j).length
        }
        var h = c.domain(),
          g = cJ(h),
          l = null == m ? a(g, 10) : 'number' == typeof m && a(g, m)
        return (
          l && ((m = l[0]), (j = l[1])),
          c.domain(
            c2(
              h,
              j > 1
                ? {
                    floor: function(e) {
                      for (; k((e = m.floor(e))); ) {
                        e = bE(e - 1)
                      }
                      return e
                    },
                    ceil: function(e) {
                      for (; k((e = m.ceil(e))); ) {
                        e = bE(+e + 1)
                      }
                      return e
                    },
                  }
                : m
            )
          )
        )
      }),
      (c.ticks = function(k, h) {
        var j = cJ(c.domain()),
          g =
            null == k
              ? a(j, 10)
              : 'number' == typeof k
                ? a(j, k)
                : !k.range && [{ range: k }, h]
        return (
          g && ((k = g[0]), (h = g[1])),
          k.range(j[0], bE(+j[1] + 1), 1 > h ? 1 : h)
        )
      }),
      (c.tickFormat = function() {
        return d
      }),
      (c.copy = function() {
        return cT(f.copy(), b, d)
      }),
      ic(c, f)
    )
  }
  function bE(a) {
    return new Date(a)
  }
  function f6(a) {
    return JSON.parse(a.responseText)
  }
  function j2(b) {
    var a = cs.createRange()
    return a.selectNode(cs.body), a.createContextualFragment(b.responseText)
  }
  var dU = { version: '3.5.16' },
    e6 = [].slice,
    iH = function(a) {
      return e6.call(a)
    },
    cs = this.document
  if (cs) {
    try {
      iH(cs.documentElement.childNodes)[0].nodeType
    } catch (g7) {
      iH = function(c) {
        for (var a = c.length, b = new Array(a); a--; ) {
          b[a] = c[a]
        }
        return b
      }
    }
  }
  if (
    (Date.now ||
      (Date.now = function() {
        return +new Date()
      }),
    cs)
  ) {
    try {
      cs.createElement('DIV').style.setProperty('opacity', 0, '')
    } catch (gx) {
      var gP = this.Element.prototype,
        dC = gP.setAttribute,
        bf = gP.setAttributeNS,
        hQ = this.CSSStyleDeclaration.prototype,
        eO = hQ.setProperty
      ;(gP.setAttribute = function(b, a) {
        dC.call(this, b, a + '')
      }),
        (gP.setAttributeNS = function(c, a, b) {
          bf.call(this, c, a, b + '')
        }),
        (hQ.setProperty = function(c, a, b) {
          eO.call(this, c, a + '', b)
        })
    }
  }
  ;(dU.ascending = iE),
    (dU.descending = function(b, a) {
      return b > a ? -1 : a > b ? 1 : a >= b ? 0 : NaN
    }),
    (dU.min = function(g, c) {
      var f,
        d,
        a = -1,
        b = g.length
      if (1 === arguments.length) {
        for (; ++a < b; ) {
          if (null != (d = g[a]) && d >= d) {
            f = d
            break
          }
        }
        for (; ++a < b; ) {
          null != (d = g[a]) && f > d && (f = d)
        }
      } else {
        for (; ++a < b; ) {
          if (null != (d = c.call(g, g[a], a)) && d >= d) {
            f = d
            break
          }
        }
        for (; ++a < b; ) {
          null != (d = c.call(g, g[a], a)) && f > d && (f = d)
        }
      }
      return f
    }),
    (dU.max = function(g, c) {
      var f,
        d,
        a = -1,
        b = g.length
      if (1 === arguments.length) {
        for (; ++a < b; ) {
          if (null != (d = g[a]) && d >= d) {
            f = d
            break
          }
        }
        for (; ++a < b; ) {
          null != (d = g[a]) && d > f && (f = d)
        }
      } else {
        for (; ++a < b; ) {
          if (null != (d = c.call(g, g[a], a)) && d >= d) {
            f = d
            break
          }
        }
        for (; ++a < b; ) {
          null != (d = c.call(g, g[a], a)) && d > f && (f = d)
        }
      }
      return f
    }),
    (dU.extent = function(j, f) {
      var h,
        g,
        c,
        d = -1,
        b = j.length
      if (1 === arguments.length) {
        for (; ++d < b; ) {
          if (null != (g = j[d]) && g >= g) {
            h = c = g
            break
          }
        }
        for (; ++d < b; ) {
          null != (g = j[d]) && (h > g && (h = g), g > c && (c = g))
        }
      } else {
        for (; ++d < b; ) {
          if (null != (g = f.call(j, j[d], d)) && g >= g) {
            h = c = g
            break
          }
        }
        for (; ++d < b; ) {
          null != (g = f.call(j, j[d], d)) &&
            (h > g && (h = g), g > c && (c = g))
        }
      }
      return [h, c]
    }),
    (dU.sum = function(h, d) {
      var g,
        f = 0,
        c = h.length,
        b = -1
      if (1 === arguments.length) {
        for (; ++b < c; ) {
          h0((g = +h[b])) && (f += g)
        }
      } else {
        for (; ++b < c; ) {
          h0((g = +d.call(h, h[b], b))) && (f += g)
        }
      }
      return f
    }),
    (dU.mean = function(j, f) {
      var g,
        d = 0,
        c = j.length,
        h = -1,
        b = c
      if (1 === arguments.length) {
        for (; ++h < c; ) {
          h0((g = h5(j[h]))) ? (d += g) : --b
        }
      } else {
        for (; ++h < c; ) {
          h0((g = h5(f.call(j, j[h], h)))) ? (d += g) : --b
        }
      }
      return b ? d / b : void 0
    }),
    (dU.quantile = function(g, c) {
      var f = (g.length - 1) * c + 1,
        d = Math.floor(f),
        a = +g[d - 1],
        b = f - d
      return b ? a + b * (g[d] - a) : a
    }),
    (dU.median = function(g, e) {
      var d,
        c = [],
        f = g.length,
        b = -1
      if (1 === arguments.length) {
        for (; ++b < f; ) {
          h0((d = h5(g[b]))) && c.push(d)
        }
      } else {
        for (; ++b < f; ) {
          h0((d = h5(e.call(g, g[b], b)))) && c.push(d)
        }
      }
      return c.length ? dU.quantile(c.sort(iE), 0.5) : void 0
    }),
    (dU.variance = function(d, m) {
      var h,
        g,
        k = d.length,
        b = 0,
        f = 0,
        j = -1,
        p = 0
      if (1 === arguments.length) {
        for (; ++j < k; ) {
          h0((h = h5(d[j]))) &&
            ((g = h - b), (b += g / ++p), (f += g * (h - b)))
        }
      } else {
        for (; ++j < k; ) {
          h0((h = h5(m.call(d, d[j], j)))) &&
            ((g = h - b), (b += g / ++p), (f += g * (h - b)))
        }
      }
      return p > 1 ? f / (p - 1) : void 0
    }),
    (dU.deviation = function() {
      var a = dU.variance.apply(this, arguments)
      return a ? Math.sqrt(a) : a
    })
  var km = iw(iE)
  ;(dU.bisectLeft = km.left),
    (dU.bisect = dU.bisectRight = km.right),
    (dU.bisector = function(a) {
      return iw(
        1 === a.length
          ? function(b, c) {
              return iE(a(b), c)
            }
          : a
      )
    }),
    (dU.shuffle = function(g, c, f) {
      ;(b = arguments.length) < 3 && ((f = g.length), 2 > b && (c = 0))
      for (var d, a, b = f - c; b; ) {
        ;(a = (Math.random() * b--) | 0),
          (d = g[b + c]),
          (g[b + c] = g[a + c]),
          (g[a + c] = d)
      }
      return g
    }),
    (dU.permute = function(d, a) {
      for (var c = a.length, b = new Array(c); c--; ) {
        b[c] = d[a[c]]
      }
      return b
    }),
    (dU.pairs = function(g) {
      for (
        var c, f = 0, d = g.length - 1, a = g[0], b = new Array(0 > d ? 0 : d);
        d > f;

      ) {
        b[f] = [(c = a), (a = g[++f])]
      }
      return b
    }),
    (dU.transpose = function(h) {
      if (!(a = h.length)) {
        return []
      }
      for (var c = -1, f = dU.min(h, iK), d = new Array(f); ++c < f; ) {
        for (var a, b = -1, g = (d[c] = new Array(a)); ++b < a; ) {
          g[b] = h[b][c]
        }
      }
      return d
    }),
    (dU.zip = function() {
      return dU.transpose(arguments)
    }),
    (dU.keys = function(c) {
      var a = []
      for (var b in c) {
        a.push(b)
      }
      return a
    }),
    (dU.values = function(c) {
      var a = []
      for (var b in c) {
        a.push(c[b])
      }
      return a
    }),
    (dU.entries = function(c) {
      var a = []
      for (var b in c) {
        a.push({ key: b, value: c[b] })
      }
      return a
    }),
    (dU.merge = function(j) {
      for (var f, h, g, c = j.length, d = -1, b = 0; ++d < c; ) {
        b += j[d].length
      }
      for (h = new Array(b); --c >= 0; ) {
        for (g = j[c], f = g.length; --f >= 0; ) {
          h[--b] = g[f]
        }
      }
      return h
    })
  var hb = Math.abs
  ;(dU.range = function(j, f, h) {
    if (
      (arguments.length < 3 &&
        ((h = 1), arguments.length < 2 && ((f = j), (j = 0))),
      (f - j) / h === 1 / 0)
    ) {
      throw new Error('infinite range')
    }
    var g,
      c = [],
      d = h9(hb(h)),
      b = -1
    if (((j *= d), (f *= d), (h *= d), 0 > h)) {
      for (; (g = j + h * ++b) > f; ) {
        c.push(g / d)
      }
    } else {
      for (; (g = j + h * ++b) < f; ) {
        c.push(g / d)
      }
    }
    return c
  }),
    (dU.map = function(j, f) {
      var h = new iG()
      if (j instanceof iG) {
        j.forEach(function(e, a) {
          h.set(e, a)
        })
      } else {
        if (Array.isArray(j)) {
          var g,
            c = -1,
            d = j.length
          if (1 === arguments.length) {
            for (; ++c < d; ) {
              h.set(c, j[c])
            }
          } else {
            for (; ++c < d; ) {
              h.set(f.call(j, (g = j[c]), c), g)
            }
          }
        } else {
          for (var b in j) {
            h.set(b, j[b])
          }
        }
      }
      return h
    })
  var aA = '__proto__',
    jy = '\x00'
  ik(iG, {
    has: iy,
    get: function(a) {
      return this._[h3(a)]
    },
    set: function(b, a) {
      return (this._[h3(b)] = a)
    },
    remove: iz,
    keys: h8,
    values: function() {
      var b = []
      for (var a in this._) {
        b.push(this._[a])
      }
      return b
    },
    entries: function() {
      var b = []
      for (var a in this._) {
        b.push({ key: iB(a), value: this._[a] })
      }
      return b
    },
    size: hY,
    empty: iF,
    forEach: function(b) {
      for (var a in this._) {
        b.call(this, iB(a), this._[a])
      }
    },
  }),
    (dU.nest = function() {
      function j(x, u, i) {
        if (i >= d.length) {
          return g ? g.call(c, u) : h ? u.sort(h) : u
        }
        for (
          var k, y, q, m, n = -1, e = u.length, w = d[i++], r = new iG();
          ++n < e;

        ) {
          ;(m = r.get((k = w((y = u[n]))))) ? m.push(y) : r.set(k, [y])
        }
        return (
          x
            ? ((y = x()),
              (q = function(l, a) {
                y.set(l, j(x, a, i))
              }))
            : ((y = {}),
              (q = function(l, a) {
                y[l] = j(x, a, i)
              })),
          r.forEach(q),
          y
        )
      }
      function f(l, k) {
        if (k >= d.length) {
          return l
        }
        var i = [],
          a = b[k++]
        return (
          l.forEach(function(m, e) {
            i.push({ key: m, values: f(e, k) })
          }),
          a
            ? i.sort(function(m, e) {
                return a(m.key, e.key)
              })
            : i
        )
      }
      var h,
        g,
        c = {},
        d = [],
        b = []
      return (
        (c.map = function(a, i) {
          return j(i, a, 0)
        }),
        (c.entries = function(a) {
          return f(j(dU.map, a, 0), 0)
        }),
        (c.key = function(a) {
          return d.push(a), c
        }),
        (c.sortKeys = function(a) {
          return (b[d.length - 1] = a), c
        }),
        (c.sortValues = function(a) {
          return (h = a), c
        }),
        (c.rollup = function(a) {
          return (g = a), c
        }),
        c
      )
    }),
    (dU.set = function(d) {
      var a = new ih()
      if (d) {
        for (var c = 0, b = d.length; b > c; ++c) {
          a.add(d[c])
        }
      }
      return a
    }),
    ik(ih, {
      has: iy,
      add: function(a) {
        return (this._[h3((a += ''))] = !0), a
      },
      remove: iz,
      values: h8,
      size: hY,
      empty: iF,
      forEach: function(b) {
        for (var a in this._) {
          b.call(this, iB(a))
        }
      },
    }),
    (dU.behavior = {}),
    (dU.rebind = function(f, b) {
      for (var d, c = 1, a = arguments.length; ++c < a; ) {
        f[(d = arguments[c])] = i9(f, b, b[d])
      }
      return f
    })
  var aG = ['webkit', 'ms', 'moz', 'Moz', 'o', 'O']
  ;(dU.dispatch = function() {
    for (var c = new iL(), a = -1, b = arguments.length; ++a < b; ) {
      c[arguments[a]] = hX(c)
    }
    return c
  }),
    (iL.prototype.on = function(d, a) {
      var c = d.indexOf('.'),
        b = ''
      if ((c >= 0 && ((b = d.slice(c + 1)), (d = d.slice(0, c))), d)) {
        return arguments.length < 2 ? this[d].on(b) : this[d].on(b, a)
      }
      if (2 === arguments.length) {
        if (null == a) {
          for (d in this) {
            this.hasOwnProperty(d) && this[d].on(b, null)
          }
        }
        return this
      }
    }),
    (dU.event = null),
    (dU.requote = function(a) {
      return a.replace(aV, '\\$&')
    })
  var aV = /[\\\^\$\*\+\?\|\[\]\(\)\.\{\}]/g,
    e9 = {}.__proto__
      ? function(b, a) {
          b.__proto__ = a
        }
      : function(c, a) {
          for (var b in a) {
            c[b] = a[b]
          }
        },
    fx = function(b, a) {
      return a.querySelector(b)
    },
    gS = function(b, a) {
      return a.querySelectorAll(b)
    },
    a4 = function(c, a) {
      var b = c.matches || c[hW(c, 'matchesSelector')]
      return (a4 = function(e, d) {
        return b.call(e, d)
      })(c, a)
    }
  'function' == typeof Sizzle &&
    ((fx = function(b, a) {
      return Sizzle(b, a)[0] || null
    }),
    (gS = Sizzle),
    (a4 = Sizzle.matchesSelector)),
    (dU.selection = function() {
      return dU.select(cs.documentElement)
    })
  var cG = (dU.selection.prototype = [])
  ;(cG.select = function(f) {
    var q,
      j,
      b,
      p,
      h = []
    f = jC(f)
    for (var m = -1, d = this.length; ++m < d; ) {
      h.push((q = [])), (q.parentNode = (b = this[m]).parentNode)
      for (var g = -1, k = b.length; ++g < k; ) {
        ;(p = b[g])
          ? (q.push((j = f.call(p, p.__data__, g, m))),
            j && '__data__' in p && (j.__data__ = p.__data__))
          : q.push(null)
      }
    }
    return jw(h)
  }),
    (cG.selectAll = function(d) {
      var m,
        h,
        b = []
      d = jA(d)
      for (var k = -1, g = this.length; ++k < g; ) {
        for (var j = this[k], c = -1, f = j.length; ++c < f; ) {
          ;(h = j[c]) &&
            (b.push((m = iH(d.call(h, h.__data__, c, k)))), (m.parentNode = h))
        }
      }
      return jw(b)
    })
  var bN = 'http://www.w3.org/1999/xhtml',
    jW = {
      svg: 'http://www.w3.org/2000/svg',
      xhtml: bN,
      xlink: 'http://www.w3.org/1999/xlink',
      xml: 'http://www.w3.org/XML/1998/namespace',
      xmlns: 'http://www.w3.org/2000/xmlns/',
    }
  ;(dU.ns = {
    prefix: jW,
    qualify: function(c) {
      var a = c.indexOf(':'),
        b = c
      return (
        a >= 0 && 'xmlns' !== (b = c.slice(0, a)) && (c = c.slice(a + 1)),
        jW.hasOwnProperty(b) ? { space: jW[b], local: c } : c
      )
    },
  }),
    (cG.attr = function(c, a) {
      if (arguments.length < 2) {
        if ('string' == typeof c) {
          var b = this.node()
          return (
            (c = dU.ns.qualify(c)),
            c.local ? b.getAttributeNS(c.space, c.local) : b.getAttribute(c)
          )
        }
        for (a in c) {
          this.each(hT(a, c[a]))
        }
        return this
      }
      return this.each(hT(c, a))
    }),
    (cG.classed = function(f, b) {
      if (arguments.length < 2) {
        if ('string' == typeof f) {
          var d = this.node(),
            c = (f = iW(f)).length,
            a = -1
          if ((b = d.classList)) {
            for (; ++a < c; ) {
              if (!b.contains(f[a])) {
                return !1
              }
            }
          } else {
            for (b = d.getAttribute('class'); ++a < c; ) {
              if (!h6(f[a]).test(b)) {
                return !1
              }
            }
          }
          return !0
        }
        for (b in f) {
          this.each(iZ(b, f[b]))
        }
        return this
      }
      return this.each(iZ(f, b))
    }),
    (cG.style = function(f, d, c) {
      var a = arguments.length
      if (3 > a) {
        if ('string' != typeof f) {
          2 > a && (d = '')
          for (c in f) {
            this.each(i2(c, f[c], d))
          }
          return this
        }
        if (2 > a) {
          var b = this.node()
          return h2(b)
            .getComputedStyle(b, null)
            .getPropertyValue(f)
        }
        c = ''
      }
      return this.each(i2(f, d, c))
    }),
    (cG.property = function(b, a) {
      if (arguments.length < 2) {
        if ('string' == typeof b) {
          return this.node()[b]
        }
        for (a in b) {
          this.each(iV(a, b[a]))
        }
        return this
      }
      return this.each(iV(b, a))
    }),
    (cG.text = function(a) {
      return arguments.length
        ? this.each(
            'function' == typeof a
              ? function() {
                  var b = a.apply(this, arguments)
                  this.textContent = null == b ? '' : b
                }
              : null == a
                ? function() {
                    this.textContent = ''
                  }
                : function() {
                    this.textContent = a
                  }
          )
        : this.node().textContent
    }),
    (cG.html = function(a) {
      return arguments.length
        ? this.each(
            'function' == typeof a
              ? function() {
                  var b = a.apply(this, arguments)
                  this.innerHTML = null == b ? '' : b
                }
              : null == a
                ? function() {
                    this.innerHTML = ''
                  }
                : function() {
                    this.innerHTML = a
                  }
          )
        : this.node().innerHTML
    }),
    (cG.append = function(a) {
      return (
        (a = iv(a)),
        this.select(function() {
          return this.appendChild(a.apply(this, arguments))
        })
      )
    }),
    (cG.insert = function(b, a) {
      return (
        (b = iv(b)),
        (a = jC(a)),
        this.select(function() {
          return this.insertBefore(
            b.apply(this, arguments),
            a.apply(this, arguments) || null
          )
        })
      )
    }),
    (cG.remove = function() {
      return this.each(jv)
    }),
    (cG.data = function(d, m) {
      function h(q, A) {
        var l,
          F,
          t,
          D = q.length,
          z = A.length,
          w = Math.min(D, z),
          x = new Array(z),
          o = new Array(z),
          E = new Array(D)
        if (m) {
          var B,
            s = new iG(),
            C = new Array(D)
          for (l = -1; ++l < D; ) {
            ;(F = q[l]) &&
              (s.has((B = m.call(F, F.__data__, l))) ? (E[l] = F) : s.set(B, F),
              (C[l] = B))
          }
          for (l = -1; ++l < z; ) {
            ;(F = s.get((B = m.call(A, (t = A[l]), l))))
              ? F !== !0 && ((x[l] = F), (F.__data__ = t))
              : (o[l] = jk(t)),
              s.set(B, !0)
          }
          for (l = -1; ++l < D; ) {
            l in C && s.get(C[l]) !== !0 && (E[l] = q[l])
          }
        } else {
          for (l = -1; ++l < w; ) {
            ;(F = q[l]),
              (t = A[l]),
              F ? ((F.__data__ = t), (x[l] = F)) : (o[l] = jk(t))
          }
          for (; z > l; ++l) {
            o[l] = jk(A[l])
          }
          for (; D > l; ++l) {
            E[l] = q[l]
          }
        }
        ;(o.update = x),
          (o.parentNode = x.parentNode = E.parentNode = q.parentNode),
          c.push(o),
          f.push(x),
          p.push(E)
      }
      var b,
        k,
        g = -1,
        j = this.length
      if (!arguments.length) {
        for (d = new Array((j = (b = this[0]).length)); ++g < j; ) {
          ;(k = b[g]) && (d[g] = k.__data__)
        }
        return d
      }
      var c = iO([]),
        f = jw([]),
        p = jw([])
      if ('function' == typeof d) {
        for (; ++g < j; ) {
          h((b = this[g]), d.call(b, b.parentNode.__data__, g))
        }
      } else {
        for (; ++g < j; ) {
          h((b = this[g]), d)
        }
      }
      return (
        (f.enter = function() {
          return c
        }),
        (f.exit = function() {
          return p
        }),
        f
      )
    }),
    (cG.datum = function(a) {
      return arguments.length
        ? this.property('__data__', a)
        : this.property('__data__')
    }),
    (cG.filter = function(d) {
      var m,
        h,
        b,
        k = []
      'function' != typeof d && (d = i5(d))
      for (var g = 0, j = this.length; j > g; g++) {
        k.push((m = [])), (m.parentNode = (h = this[g]).parentNode)
        for (var c = 0, f = h.length; f > c; c++) {
          ;(b = h[c]) && d.call(b, b.__data__, c, g) && m.push(b)
        }
      }
      return jw(k)
    }),
    (cG.order = function() {
      for (var g = -1, c = this.length; ++g < c; ) {
        for (var f, d = this[g], a = d.length - 1, b = d[a]; --a >= 0; ) {
          ;(f = d[a]) &&
            (b && b !== f.nextSibling && b.parentNode.insertBefore(f, b),
            (b = f))
        }
      }
      return this
    }),
    (cG.sort = function(c) {
      c = jj.apply(this, arguments)
      for (var a = -1, b = this.length; ++a < b; ) {
        this[a].sort(c)
      }
      return this.order()
    }),
    (cG.each = function(a) {
      return iP(this, function(b, d, c) {
        a.call(b, b.__data__, d, c)
      })
    }),
    (cG.call = function(b) {
      var a = iH(arguments)
      return b.apply((a[0] = this), a), this
    }),
    (cG.empty = function() {
      return !this.node()
    }),
    (cG.node = function() {
      for (var g = 0, c = this.length; c > g; g++) {
        for (var f = this[g], d = 0, a = f.length; a > d; d++) {
          var b = f[d]
          if (b) {
            return b
          }
        }
      }
      return null
    }),
    (cG.size = function() {
      var a = 0
      return (
        iP(this, function() {
          ++a
        }),
        a
      )
    })
  var hC = []
  ;(dU.selection.enter = iO),
    (dU.selection.enter.prototype = hC),
    (hC.append = cG.append),
    (hC.empty = cG.empty),
    (hC.node = cG.node),
    (hC.call = cG.call),
    (hC.size = cG.size),
    (hC.select = function(f) {
      for (var q, j, b, p, h, m = [], d = -1, g = this.length; ++d < g; ) {
        ;(b = (p = this[d]).update),
          m.push((q = [])),
          (q.parentNode = p.parentNode)
        for (var k = -1, v = p.length; ++k < v; ) {
          ;(h = p[k])
            ? (q.push((b[k] = j = f.call(p.parentNode, h.__data__, k, d))),
              (j.__data__ = h.__data__))
            : q.push(null)
        }
      }
      return jw(m)
    }),
    (hC.insert = function(b, a) {
      return arguments.length < 2 && (a = iT(this)), cG.insert.call(this, b, a)
    }),
    (dU.select = function(a) {
      var b
      return (
        'string' == typeof a
          ? ((b = [fx(a, cs)]), (b.parentNode = cs.documentElement))
          : ((b = [a]), (b.parentNode = id(a))),
        jw([b])
      )
    }),
    (dU.selectAll = function(b) {
      var a
      return (
        'string' == typeof b
          ? ((a = iH(gS(b, cs))), (a.parentNode = cs.documentElement))
          : ((a = iH(b)), (a.parentNode = null)),
        jw([a])
      )
    }),
    (cG.on = function(d, a, c) {
      var b = arguments.length
      if (3 > b) {
        if ('string' != typeof d) {
          2 > b && (a = !1)
          for (c in d) {
            this.each(iQ(c, d[c], a))
          }
          return this
        }
        if (2 > b) {
          return (b = this.node()['__on' + d]) && b._
        }
        c = !1
      }
      return this.each(iQ(d, a, c))
    })
  var dc = dU.map({ mouseenter: 'mouseover', mouseleave: 'mouseout' })
  cs &&
    dc.forEach(function(a) {
      'on' + a in cs && dc.remove(a)
    })
  var eR,
    fA = 0
  dU.mouse = function(a) {
    return jg(a, im())
  }
  var bv = this.navigator && /WebKit/.test(this.navigator.userAgent) ? -1 : 0
  ;(dU.touch = function(g, c, f) {
    if ((arguments.length < 3 && ((f = c), (c = im().changedTouches)), c)) {
      for (var d, a = 0, b = c.length; b > a; ++a) {
        if ((d = c[a]).identifier === f) {
          return jg(g, d)
        }
      }
    }
  }),
    (dU.behavior.drag = function() {
      function h() {
        this.on('mousedown.drag', d).on('touchstart.drag', b)
      }
      function g(o, l, m, k, j) {
        return function() {
          function e() {
            var v,
              s,
              p = l(q, A)
            p &&
              ((v = p[0] - u[0]),
              (s = p[1] - u[1]),
              (a |= v | s),
              (u = p),
              r({ type: 'drag', x: p[0] + x[0], y: p[1] + x[1], dx: v, dy: s }))
          }
          function n() {
            l(q, A) &&
              (i.on(k + w, null).on(j + w, null), z(a), r({ type: 'dragend' }))
          }
          var x,
            B = this,
            t = dU.event.target.correspondingElement || dU.event.target,
            q = B.parentNode,
            r = f.of(B, arguments),
            a = 0,
            A = o(),
            w = '.drag' + (null == A ? '' : '-' + A),
            i = dU
              .select(m(t))
              .on(k + w, e)
              .on(j + w, n),
            z = iS(t),
            u = l(q, A)
          c
            ? ((x = c.apply(B, arguments)), (x = [x.x - u[0], x.y - u[1]]))
            : (x = [0, 0]),
            r({ type: 'dragstart' })
        }
      }
      var f = i7(h, 'drag', 'dragstart', 'dragend'),
        c = null,
        d = g(iJ, dU.mouse, h2, 'mousemove', 'mouseup'),
        b = g(jp, dU.touch, hV, 'touchmove', 'touchend')
      return (
        (h.origin = function(a) {
          return arguments.length ? ((c = a), h) : c
        }),
        dU.rebind(h, f, 'on')
      )
    }),
    (dU.touches = function(b, a) {
      return (
        arguments.length < 2 && (a = im().touches),
        a
          ? iH(a).map(function(c) {
              var d = jg(b, c)
              return (d.identifier = c.identifier), d
            })
          : []
      )
    })
  var f9 = 0.000001,
    ez = f9 * f9,
    fP = Math.PI,
    aM = 2 * fP,
    j9 = aM - f9,
    gA = fP / 2,
    jH = fP / 180,
    cW = 180 / fP,
    cA = Math.SQRT2,
    d0 = 2,
    dg = 4
  ;(dU.interpolateZoom = function(z, k) {
    var G,
      w,
      j = z[0],
      C = z[1],
      J = z[2],
      y = k[0],
      B = k[1],
      I = k[2],
      q = y - j,
      F = B - C,
      D = q * q + F * F
    if (ez > D) {
      ;(w = Math.log(I / J) / cA),
        (G = function(a) {
          return [j + a * q, C + a * F, J * Math.exp(cA * a * w)]
        })
    } else {
      var E = Math.sqrt(D),
        x = (I * I - J * J + dg * D) / (2 * J * d0 * E),
        b = (I * I - J * J - dg * D) / (2 * I * d0 * E),
        H = Math.log(Math.sqrt(x * x + 1) - x),
        A = Math.log(Math.sqrt(b * b + 1) - b)
      ;(w = (A - H) / cA),
        (G = function(f) {
          var a = f * w,
            c = cI(H),
            d = (J / (d0 * E)) * (c * bx(cA * a + H) - hh(H))
          return [j + d * q, C + d * F, (J * c) / cI(cA * a + H)]
        })
    }
    return (G.duration = 1000 * w), G
  }),
    (dU.behavior.zoom = function() {
      function Z(a) {
        a.on(F, ar)
          .on(b6 + '.zoom', ao)
          .on('dblclick.zoom', X)
          .on(t, an)
      }
      function at(a) {
        return [(a[0] - ai.x) / ai.k, (a[1] - ai.y) / ai.k]
      }
      function U(a) {
        return [a[0] * ai.k + ai.x, a[1] * ai.k + ai.y]
      }
      function N(a) {
        ai.k = Math.max(W[0], Math.min(W[1], a))
      }
      function al(b, a) {
        ;(a = U(a)), (ai.x += b[0] - a[0]), (ai.y += b[1] - a[1])
      }
      function bi(c, f, d, b) {
        ;(c.__chart__ = { x: ai.x, y: ai.y, k: ai.k }),
          N(Math.pow(2, b)),
          al((au = f), d),
          (c = dU.select(c)),
          S > 0 && (c = c.transition().duration(S)),
          c.call(Z.event)
      }
      function Y() {
        be &&
          be.domain(
            I.range()
              .map(function(a) {
                return (a - ai.x) / ai.k
              })
              .map(I.invert)
          ),
          J &&
            J.domain(
              bl
                .range()
                .map(function(a) {
                  return (a - ai.y) / ai.k
                })
                .map(bl.invert)
            )
      }
      function ae(a) {
        G++ || a({ type: 'zoomstart' })
      }
      function ba(a) {
        Y(), a({ type: 'zoom', scale: ai.k, translate: [ai.x, ai.y] })
      }
      function Q(a) {
        --G || (a({ type: 'zoomend' }), (au = null))
      }
      function ar() {
        function m() {
          ;(l = 1), al(dU.mouse(c), d), ba(b)
        }
        function i() {
          k.on(V, null).on(j, null), e(l), Q(b)
        }
        var c = this,
          b = P.of(c, arguments),
          l = 0,
          k = dU
            .select(h2(c))
            .on(V, m)
            .on(j, i),
          d = at(dU.mouse(c)),
          e = iS(c)
        js.call(c), ae(b)
      }
      function an() {
        function f() {
          var b = dU.touches(c)
          return (
            (i = ai.k),
            b.forEach(function(d) {
              d.identifier in l && (l[d.identifier] = at(d))
            }),
            b
          )
        }
        function D() {
          var w = dU.event.target
          dU
            .select(w)
            .on(u, a)
            .on(q, e),
            A.push(w)
          for (
            var p = dU.event.changedTouches, v = 0, d = p.length;
            d > v;
            ++v
          ) {
            l[p[v].identifier] = null
          }
          var b = f(),
            r = Date.now()
          if (1 === b.length) {
            if (500 > r - B) {
              var x = b[0]
              bi(
                c,
                x,
                l[x.identifier],
                Math.floor(Math.log(ai.k) / Math.LN2) + 1
              ),
                iY()
            }
            B = r
          } else {
            if (b.length > 1) {
              var x = b[0],
                o = b[1],
                m = x[0] - o[0],
                n = x[1] - o[1]
              h = m * m + n * n
            }
          }
        }
        function a() {
          var g,
            x,
            v,
            b,
            w = dU.touches(c)
          js.call(c)
          for (var d = 0, m = w.length; m > d; ++d, b = null) {
            if (((v = w[d]), (b = l[v.identifier]))) {
              if (x) {
                break
              }
              ;(g = v), (x = b)
            }
          }
          if (b) {
            var y = (y = v[0] - g[0]) * y + (y = v[1] - g[1]) * y,
              p = h && Math.sqrt(y / h)
            ;(g = [(g[0] + v[0]) / 2, (g[1] + v[1]) / 2]),
              (x = [(x[0] + b[0]) / 2, (x[1] + b[1]) / 2]),
              N(p * i)
          }
          ;(B = null), al(g, x), ba(C)
        }
        function e() {
          if (dU.event.touches.length) {
            for (
              var d = dU.event.changedTouches, m = 0, g = d.length;
              g > m;
              ++m
            ) {
              delete l[d[m].identifier]
            }
            for (var b in l) {
              return void f()
            }
          }
          dU.selectAll(A).on(s, null), z.on(F, ar).on(t, an), k(), Q(C)
        }
        var i,
          c = this,
          C = P.of(c, arguments),
          l = {},
          h = 0,
          s = '.zoom-' + dU.event.changedTouches[0].identifier,
          u = 'touchmove' + s,
          q = 'touchend' + s,
          A = [],
          z = dU.select(c),
          k = iS(c)
        D(), ae(C), z.on(F, null).on(t, D)
      }
      function ao() {
        var a = P.of(this, arguments)
        H
          ? clearTimeout(H)
          : (js.call(this), (K = at((au = aa || dU.mouse(this)))), ae(a)),
          (H = setTimeout(function() {
            ;(H = null), Q(a)
          }, 50)),
          iY(),
          N(Math.pow(2, 0.002 * ek()) * ai.k),
          al(au, K),
          ba(a)
      }
      function X() {
        var b = dU.mouse(this),
          a = Math.log(ai.k) / Math.LN2
        bi(
          this,
          b,
          at(b),
          dU.event.shiftKey ? Math.ceil(a) - 1 : Math.floor(a) + 1
        )
      }
      var K,
        au,
        aa,
        H,
        B,
        I,
        be,
        bl,
        J,
        ai = { x: 0, y: 0, k: 1 },
        O = [960, 500],
        W = dF,
        S = 250,
        G = 0,
        F = 'mousedown.zoom',
        V = 'mousemove.zoom',
        j = 'mouseup.zoom',
        t = 'touchstart.zoom',
        P = i7(Z, 'zoomstart', 'zoom', 'zoomend')
      return (
        b6 ||
          (b6 =
            'onwheel' in cs
              ? ((ek = function() {
                  return -dU.event.deltaY * (dU.event.deltaMode ? 120 : 1)
                }),
                'wheel')
              : 'onmousewheel' in cs
                ? ((ek = function() {
                    return dU.event.wheelDelta
                  }),
                  'mousewheel')
                : ((ek = function() {
                    return -dU.event.detail
                  }),
                  'MozMousePixelScroll')),
        (Z.event = function(a) {
          a.each(function() {
            var c = P.of(this, arguments),
              b = ai
            j1
              ? dU
                  .select(this)
                  .transition()
                  .each('start.zoom', function() {
                    ;(ai = this.__chart__ || { x: 0, y: 0, k: 1 }), ae(c)
                  })
                  .tween('zoom:zoom', function() {
                    var k = O[0],
                      h = O[1],
                      f = au ? au[0] : k / 2,
                      g = au ? au[1] : h / 2,
                      d = dU.interpolateZoom(
                        [(f - ai.x) / ai.k, (g - ai.y) / ai.k, k / ai.k],
                        [(f - b.x) / b.k, (g - b.y) / b.k, k / b.k]
                      )
                    return function(e) {
                      var i = d(e),
                        l = k / i[2]
                      ;(this.__chart__ = ai = {
                        x: f - i[0] * l,
                        y: g - i[1] * l,
                        k: l,
                      }),
                        ba(c)
                    }
                  })
                  .each('interrupt.zoom', function() {
                    Q(c)
                  })
                  .each('end.zoom', function() {
                    Q(c)
                  })
              : ((this.__chart__ = ai), ae(c), ba(c), Q(c))
          })
        }),
        (Z.translate = function(a) {
          return arguments.length
            ? ((ai = { x: +a[0], y: +a[1], k: ai.k }), Y(), Z)
            : [ai.x, ai.y]
        }),
        (Z.scale = function(a) {
          return arguments.length
            ? ((ai = { x: ai.x, y: ai.y, k: null }), N(+a), Y(), Z)
            : ai.k
        }),
        (Z.scaleExtent = function(a) {
          return arguments.length
            ? ((W = null == a ? dF : [+a[0], +a[1]]), Z)
            : W
        }),
        (Z.center = function(a) {
          return arguments.length ? ((aa = a && [+a[0], +a[1]]), Z) : aa
        }),
        (Z.size = function(a) {
          return arguments.length ? ((O = a && [+a[0], +a[1]]), Z) : O
        }),
        (Z.duration = function(a) {
          return arguments.length ? ((S = +a), Z) : S
        }),
        (Z.x = function(a) {
          return arguments.length
            ? ((be = a), (I = a.copy()), (ai = { x: 0, y: 0, k: 1 }), Z)
            : be
        }),
        (Z.y = function(a) {
          return arguments.length
            ? ((J = a), (bl = a.copy()), (ai = { x: 0, y: 0, k: 1 }), Z)
            : J
        }),
        dU.rebind(Z, P, 'on')
      )
    })
  var ek,
    b6,
    dF = [0, 1 / 0]
  ;(dU.color = dK),
    (dK.prototype.toString = function() {
      return this.rgb() + ''
    }),
    (dU.hsl = eW)
  var iN = (eW.prototype = new dK())
  ;(iN.brighter = function(a) {
    return (
      (a = Math.pow(0.7, arguments.length ? a : 1)),
      new eW(this.h, this.s, this.l / a)
    )
  }),
    (iN.darker = function(a) {
      return (
        (a = Math.pow(0.7, arguments.length ? a : 1)),
        new eW(this.h, this.s, a * this.l)
      )
    }),
    (iN.rgb = function() {
      return ib(this.h, this.s, this.l)
    }),
    (dU.hcl = b8)
  var ac = (b8.prototype = new dK())
  ;(ac.brighter = function(a) {
    return new b8(
      this.h,
      this.c,
      Math.min(100, this.l + hU * (arguments.length ? a : 1))
    )
  }),
    (ac.darker = function(a) {
      return new b8(
        this.h,
        this.c,
        Math.max(0, this.l - hU * (arguments.length ? a : 1))
      )
    }),
    (ac.rgb = function() {
      return gX(this.h, this.c, this.l).rgb()
    }),
    (dU.lab = gg)
  var hU = 18,
    fS = 0.95047,
    d8 = 1,
    bO = 1.08883,
    hg = (gg.prototype = new dK())
  ;(hg.brighter = function(a) {
    return new gg(
      Math.min(100, this.l + hU * (arguments.length ? a : 1)),
      this.a,
      this.b
    )
  }),
    (hg.darker = function(a) {
      return new gg(
        Math.max(0, this.l - hU * (arguments.length ? a : 1)),
        this.a,
        this.b
      )
    }),
    (hg.rgb = function() {
      return gF(this.l, this.a, this.b)
    }),
    (dU.rgb = kc)
  var cH = (kc.prototype = new dK())
  ;(cH.brighter = function(f) {
    f = Math.pow(0.7, arguments.length ? f : 1)
    var b = this.r,
      d = this.g,
      c = this.b,
      a = 30
    return b || d || c
      ? (b && a > b && (b = a),
        d && a > d && (d = a),
        c && a > c && (c = a),
        new kc(
          Math.min(255, b / f),
          Math.min(255, d / f),
          Math.min(255, c / f)
        ))
      : new kc(a, a, a)
  }),
    (cH.darker = function(a) {
      return (
        (a = Math.pow(0.7, arguments.length ? a : 1)),
        new kc(a * this.r, a * this.g, a * this.b)
      )
    }),
    (cH.hsl = function() {
      return aO(this.r, this.g, this.b)
    }),
    (cH.toString = function() {
      return '#' + i3(this.r) + i3(this.g) + i3(this.b)
    })
  var bw = dU.map({
    aliceblue: 15792383,
    antiquewhite: 16444375,
    aqua: 65535,
    aquamarine: 8388564,
    azure: 15794175,
    beige: 16119260,
    bisque: 16770244,
    black: 0,
    blanchedalmond: 16772045,
    blue: 255,
    blueviolet: 9055202,
    brown: 10824234,
    burlywood: 14596231,
    cadetblue: 6266528,
    chartreuse: 8388352,
    chocolate: 13789470,
    coral: 16744272,
    cornflowerblue: 6591981,
    cornsilk: 16775388,
    crimson: 14423100,
    cyan: 65535,
    darkblue: 139,
    darkcyan: 35723,
    darkgoldenrod: 12092939,
    darkgray: 11119017,
    darkgreen: 25600,
    darkgrey: 11119017,
    darkkhaki: 12433259,
    darkmagenta: 9109643,
    darkolivegreen: 5597999,
    darkorange: 16747520,
    darkorchid: 10040012,
    darkred: 9109504,
    darksalmon: 15308410,
    darkseagreen: 9419919,
    darkslateblue: 4734347,
    darkslategray: 3100495,
    darkslategrey: 3100495,
    darkturquoise: 52945,
    darkviolet: 9699539,
    deeppink: 16716947,
    deepskyblue: 49151,
    dimgray: 6908265,
    dimgrey: 6908265,
    dodgerblue: 2003199,
    firebrick: 11674146,
    floralwhite: 16775920,
    forestgreen: 2263842,
    fuchsia: 16711935,
    gainsboro: 14474460,
    ghostwhite: 16316671,
    gold: 16766720,
    goldenrod: 14329120,
    gray: 8421504,
    green: 32768,
    greenyellow: 11403055,
    grey: 8421504,
    honeydew: 15794160,
    hotpink: 16738740,
    indianred: 13458524,
    indigo: 4915330,
    ivory: 16777200,
    khaki: 15787660,
    lavender: 15132410,
    lavenderblush: 16773365,
    lawngreen: 8190976,
    lemonchiffon: 16775885,
    lightblue: 11393254,
    lightcoral: 15761536,
    lightcyan: 14745599,
    lightgoldenrodyellow: 16448210,
    lightgray: 13882323,
    lightgreen: 9498256,
    lightgrey: 13882323,
    lightpink: 16758465,
    lightsalmon: 16752762,
    lightseagreen: 2142890,
    lightskyblue: 8900346,
    lightslategray: 7833753,
    lightslategrey: 7833753,
    lightsteelblue: 11584734,
    lightyellow: 16777184,
    lime: 65280,
    limegreen: 3329330,
    linen: 16445670,
    magenta: 16711935,
    maroon: 8388608,
    mediumaquamarine: 6737322,
    mediumblue: 205,
    mediumorchid: 12211667,
    mediumpurple: 9662683,
    mediumseagreen: 3978097,
    mediumslateblue: 8087790,
    mediumspringgreen: 64154,
    mediumturquoise: 4772300,
    mediumvioletred: 13047173,
    midnightblue: 1644912,
    mintcream: 16121850,
    mistyrose: 16770273,
    moccasin: 16770229,
    navajowhite: 16768685,
    navy: 128,
    oldlace: 16643558,
    olive: 8421376,
    olivedrab: 7048739,
    orange: 16753920,
    orangered: 16729344,
    orchid: 14315734,
    palegoldenrod: 15657130,
    palegreen: 10025880,
    paleturquoise: 11529966,
    palevioletred: 14381203,
    papayawhip: 16773077,
    peachpuff: 16767673,
    peru: 13468991,
    pink: 16761035,
    plum: 14524637,
    powderblue: 11591910,
    purple: 8388736,
    rebeccapurple: 6697881,
    red: 16711680,
    rosybrown: 12357519,
    royalblue: 4286945,
    saddlebrown: 9127187,
    salmon: 16416882,
    sandybrown: 16032864,
    seagreen: 3050327,
    seashell: 16774638,
    sienna: 10506797,
    silver: 12632256,
    skyblue: 8900331,
    slateblue: 6970061,
    slategray: 7372944,
    slategrey: 7372944,
    snow: 16775930,
    springgreen: 65407,
    steelblue: 4620980,
    tan: 13808780,
    teal: 32896,
    thistle: 14204888,
    tomato: 16737095,
    turquoise: 4251856,
    violet: 15631086,
    wheat: 16113331,
    white: 16777215,
    whitesmoke: 16119285,
    yellow: 16776960,
    yellowgreen: 10145074,
  })
  bw.forEach(function(b, a) {
    bw.set(b, g4(a))
  }),
    (dU.functor = aX),
    (dU.xhr = cx(hV)),
    (dU.dsv = function(d, m) {
      function h(q, p, o) {
        arguments.length < 3 && ((o = p), (p = null))
        var l = bG(q, m, null == p ? b : k(p), o)
        return (
          (l.row = function(a) {
            return arguments.length ? l.response(null == (p = a) ? b : k(a)) : p
          }),
          l
        )
      }
      function b(a) {
        return h.parse(a.responseText)
      }
      function k(a) {
        return function(e) {
          return h.parse(e.responseText, a)
        }
      }
      function g(a) {
        return a.map(j).join(d)
      }
      function j(a) {
        return c.test(a) ? '"' + a.replace(/\"/g, '""') + '"' : a
      }
      var c = new RegExp('["' + d + '\n]'),
        f = d.charCodeAt(0)
      return (
        (h.parse = function(i, a) {
          var e
          return h.parseRows(i, function(p, o) {
            if (e) {
              return e(p, o - 1)
            }
            var l = new Function(
              'd',
              'return {' +
                p
                  .map(function(r, q) {
                    return JSON.stringify(r) + ': d[' + q + ']'
                  })
                  .join(',') +
                '}'
            )
            e = a
              ? function(r, q) {
                  return a(l(r), q)
                }
              : l
          })
        }),
        (h.parseRows = function(q, C) {
          function y() {
            if (D >= z) {
              return A
            }
            if (B) {
              return (B = !1), v
            }
            var a = D
            if (34 === q.charCodeAt(a)) {
              for (var n = a; n++ < z; ) {
                if (34 === q.charCodeAt(n)) {
                  if (34 !== q.charCodeAt(n + 1)) {
                    break
                  }
                  ++n
                }
              }
              D = n + 2
              var i = q.charCodeAt(n + 1)
              return (
                13 === i
                  ? ((B = !0), 10 === q.charCodeAt(n + 2) && ++D)
                  : 10 === i && (B = !0),
                q.slice(a + 1, n).replace(/""/g, '"')
              )
            }
            for (; z > D; ) {
              var i = q.charCodeAt(D++),
                s = 1
              if (10 === i) {
                B = !0
              } else {
                if (13 === i) {
                  ;(B = !0), 10 === q.charCodeAt(D) && (++D, ++s)
                } else {
                  if (i !== f) {
                    continue
                  }
                }
              }
              return q.slice(a, D - s)
            }
            return q.slice(a)
          }
          for (
            var l, B, v = {}, A = {}, p = [], z = q.length, D = 0, x = 0;
            (l = y()) !== A;

          ) {
            for (var w = []; l !== v && l !== A; ) {
              w.push(l), (l = y())
            }
            ;(C && null == (w = C(w, x++))) || p.push(w)
          }
          return p
        }),
        (h.format = function(e) {
          if (Array.isArray(e[0])) {
            return h.formatRows(e)
          }
          var i = new ih(),
            a = []
          return (
            e.forEach(function(o) {
              for (var l in o) {
                i.has(l) || a.push(i.add(l))
              }
            }),
            [a.map(j).join(d)]
              .concat(
                e.map(function(l) {
                  return a
                    .map(function(o) {
                      return j(l[o])
                    })
                    .join(d)
                })
              )
              .join('\n')
          )
        }),
        (h.formatRows = function(a) {
          return a.map(g).join('\n')
        }),
        h
      )
    }),
    (dU.csv = dU.dsv(',', 'text/csv')),
    (dU.tsv = dU.dsv('	', 'text/tab-separated-values'))
  var fW,
    jP,
    dJ,
    eV,
    h7 =
      this[hW(this, 'requestAnimationFrame')] ||
      function(a) {
        setTimeout(a, 17)
      }
  ;(dU.timer = function() {
    c1.apply(this, arguments)
  }),
    (dU.timer.flush = function() {
      fs(), bh()
    }),
    (dU.round = function(b, a) {
      return a ? Math.round(b * (a = Math.pow(10, a))) / a : Math.round(b)
    })
  var b7 = [
    'y',
    'z',
    'a',
    'f',
    'p',
    'n',
    '\xb5',
    'm',
    '',
    'k',
    'M',
    'G',
    'T',
    'P',
    'E',
    'Z',
    'Y',
  ].map(ep)
  dU.formatPrefix = function(c, a) {
    var b = 0
    return (
      (c = +c) &&
        (0 > c && (c *= -1),
        a && (c = dU.round(c, f3(c, a))),
        (b = 1 + Math.floor(1e-12 + Math.log(c) / Math.LN10)),
        (b = Math.max(-24, Math.min(24, 3 * Math.floor((b - 1) / 3))))),
      b7[8 + b / 3]
    )
  }
  var gW = /(?:([^{])?([<>=^]))?([+\- ])?([$#])?(0)?(\d+)?(,)?(\.-?\d+)?([a-z%])?/i,
    gf = dU.map({
      b: function(a) {
        return a.toString(2)
      },
      c: function(a) {
        return String.fromCharCode(a)
      },
      o: function(a) {
        return a.toString(8)
      },
      x: function(a) {
        return a.toString(16)
      },
      X: function(a) {
        return a.toString(16).toUpperCase()
      },
      g: function(b, a) {
        return b.toPrecision(a)
      },
      e: function(b, a) {
        return b.toExponential(a)
      },
      f: function(b, a) {
        return b.toFixed(a)
      },
      r: function(b, a) {
        return (b = dU.round(b, f3(b, a))).toFixed(
          Math.max(0, Math.min(20, f3(b * (1 + 1e-15), a)))
        )
      },
    }),
    gE = (dU.time = {}),
    dm = Date
  jZ.prototype = {
    getDate: function() {
      return this._.getUTCDate()
    },
    getDay: function() {
      return this._.getUTCDay()
    },
    getFullYear: function() {
      return this._.getUTCFullYear()
    },
    getHours: function() {
      return this._.getUTCHours()
    },
    getMilliseconds: function() {
      return this._.getUTCMilliseconds()
    },
    getMinutes: function() {
      return this._.getUTCMinutes()
    },
    getMonth: function() {
      return this._.getUTCMonth()
    },
    getSeconds: function() {
      return this._.getUTCSeconds()
    },
    getTime: function() {
      return this._.getTime()
    },
    getTimezoneOffset: function() {
      return 0
    },
    valueOf: function() {
      return this._.valueOf()
    },
    setDate: function() {
      a5.setUTCDate.apply(this._, arguments)
    },
    setDay: function() {
      a5.setUTCDay.apply(this._, arguments)
    },
    setFullYear: function() {
      a5.setUTCFullYear.apply(this._, arguments)
    },
    setHours: function() {
      a5.setUTCHours.apply(this._, arguments)
    },
    setMilliseconds: function() {
      a5.setUTCMilliseconds.apply(this._, arguments)
    },
    setMinutes: function() {
      a5.setUTCMinutes.apply(this._, arguments)
    },
    setMonth: function() {
      a5.setUTCMonth.apply(this._, arguments)
    },
    setSeconds: function() {
      a5.setUTCSeconds.apply(this._, arguments)
    },
    setTime: function() {
      a5.setTime.apply(this._, arguments)
    },
  }
  var a5 = Date.prototype
  ;(gE.year = gs(
    function(a) {
      return (a = gE.day(a)), a.setMonth(0, 1), a
    },
    function(b, a) {
      b.setFullYear(b.getFullYear() + a)
    },
    function(a) {
      return a.getFullYear()
    }
  )),
    (gE.years = gE.year.range),
    (gE.years.utc = gE.year.utc.range),
    (gE.day = gs(
      function(b) {
        var a = new dm(2000, 0)
        return a.setFullYear(b.getFullYear(), b.getMonth(), b.getDate()), a
      },
      function(b, a) {
        b.setDate(b.getDate() + a)
      },
      function(a) {
        return a.getDate() - 1
      }
    )),
    (gE.days = gE.day.range),
    (gE.days.utc = gE.day.utc.range),
    (gE.dayOfYear = function(b) {
      var a = gE.year(b)
      return Math.floor(
        (b - a - 60000 * (b.getTimezoneOffset() - a.getTimezoneOffset())) /
          86400000
      )
    }),
    [
      'sunday',
      'monday',
      'tuesday',
      'wednesday',
      'thursday',
      'friday',
      'saturday',
    ].forEach(function(c, a) {
      a = 7 - a
      var b = (gE[c] = gs(
        function(d) {
          return (
            (d = gE.day(d)).setDate(d.getDate() - ((d.getDay() + a) % 7)), d
          )
        },
        function(e, d) {
          e.setDate(e.getDate() + 7 * Math.floor(d))
        },
        function(f) {
          var d = gE.year(f).getDay()
          return Math.floor((gE.dayOfYear(f) + ((d + a) % 7)) / 7) - (d !== a)
        }
      ))
      ;(gE[c + 's'] = b.range),
        (gE[c + 's'].utc = b.utc.range),
        (gE[c + 'OfYear'] = function(f) {
          var d = gE.year(f).getDay()
          return Math.floor((gE.dayOfYear(f) + ((d + a) % 7)) / 7)
        })
    }),
    (gE.week = gE.sunday),
    (gE.weeks = gE.sunday.range),
    (gE.weeks.utc = gE.sunday.utc.range),
    (gE.weekOfYear = gE.sundayOfYear)
  var eD = { '-': '', _: ' ', 0: '0' },
    kb = /^\s*\d+/,
    g3 = /^%/
  dU.locale = function(a) {
    return { numberFormat: fF(a), timeFormat: cP(a) }
  }
  var ad = dU.locale({
    decimal: '.',
    thousands: ',',
    grouping: [3],
    currency: ['$', ''],
    dateTime: '%a %b %e %X %Y',
    date: '%m/%d/%Y',
    time: '%H:%M:%S',
    periods: ['AM', 'PM'],
    days: [
      'Sunday',
      'Monday',
      'Tuesday',
      'Wednesday',
      'Thursday',
      'Friday',
      'Saturday',
    ],
    shortDays: ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'],
    months: [
      'January',
      'February',
      'March',
      'April',
      'May',
      'June',
      'July',
      'August',
      'September',
      'October',
      'November',
      'December',
    ],
    shortMonths: [
      'Jan',
      'Feb',
      'Mar',
      'Apr',
      'May',
      'Jun',
      'Jul',
      'Aug',
      'Sep',
      'Oct',
      'Nov',
      'Dec',
    ],
  })
  ;(dU.format = ad.numberFormat),
    (dU.geo = {}),
    (b4.prototype = {
      s: 0,
      t: 0,
      add: function(a) {
        gU(a, this.t, i0),
          gU(i0.s, this.s, this),
          this.s ? (this.t += i0.t) : (this.s = i0.t)
      },
      reset: function() {
        this.s = this.t = 0
      },
      valueOf: function() {
        return this.s
      },
    })
  var i0 = new b4()
  dU.geo.stream = function(b, a) {
    b && ap.hasOwnProperty(b.type) ? ap[b.type](b, a) : gc(b, a)
  }
  var ap = {
      Feature: function(b, a) {
        gc(b.geometry, a)
      },
      FeatureCollection: function(f, b) {
        for (var d = f.features, c = -1, a = d.length; ++c < a; ) {
          gc(d[c].geometry, b)
        }
      },
    },
    aN = {
      Sphere: function(b, a) {
        a.sphere()
      },
      Point: function(b, a) {
        ;(b = b.coordinates), a.point(b[0], b[1], b[2])
      },
      MultiPoint: function(f, b) {
        for (var d = f.coordinates, c = -1, a = d.length; ++c < a; ) {
          ;(f = d[c]), b.point(f[0], f[1], f[2])
        }
      },
      LineString: function(b, a) {
        gC(b.coordinates, a, 0)
      },
      MultiLineString: function(f, b) {
        for (var d = f.coordinates, c = -1, a = d.length; ++c < a; ) {
          gC(d[c], b, 0)
        }
      },
      Polygon: function(b, a) {
        dj(b.coordinates, a)
      },
      MultiPolygon: function(f, b) {
        for (var d = f.coordinates, c = -1, a = d.length; ++c < a; ) {
          dj(d[c], b)
        }
      },
      GeometryCollection: function(f, b) {
        for (var d = f.geometries, c = -1, a = d.length; ++c < a; ) {
          gc(d[c], b)
        }
      },
    }
  dU.geo.area = function(a) {
    return (e2 = 0), dU.geo.stream(a, gL), e2
  }
  var e2,
    ff = new b4(),
    gL = {
      sphere: function() {
        e2 += 4 * fP
      },
      point: iJ,
      lineStart: iJ,
      lineEnd: iJ,
      polygonStart: function() {
        ff.reset(), (gL.lineStart = a2)
      },
      polygonEnd: function() {
        var a = 2 * ff
        ;(e2 += 0 > a ? 4 * fP + a : a),
          (gL.lineStart = gL.lineEnd = gL.point = iJ)
      },
    }
  ;(dU.geo.bounds = (function() {
    function F(b, a) {
      j.push((q = [(B = b), (J = b)])), L > a && (L = a), a > K && (K = a)
    }
    function A(M, h) {
      var b = hE([M * jH, h * jH])
      if (G) {
        var x = j7(G, b),
          g = [x[1], -x[0], 0],
          p = j7(g, x)
        iU(p), (p = aj(p))
        var f = M - D,
          n = f > 0 ? 1 : -1,
          s = p[0] * cW * n,
          m = hb(f) > 180
        if (m ^ (s > n * D && n * M > s)) {
          var o = p[1] * cW
          o > K && (K = o)
        } else {
          if (((s = ((s + 360) % 360) - 180), m ^ (s > n * D && n * M > s))) {
            var o = -p[1] * cW
            L > o && (L = o)
          } else {
            L > h && (L = h), h > K && (K = h)
          }
        }
        m
          ? D > M
            ? E(B, M) > E(B, J) && (J = M)
            : E(M, J) > E(B, J) && (B = M)
          : J >= B
            ? (B > M && (B = M), M > J && (J = M))
            : M > D
              ? E(B, M) > E(B, J) && (J = M)
              : E(M, J) > E(B, J) && (B = M)
      } else {
        F(M, h)
      }
      ;(G = b), (D = M)
    }
    function N() {
      Q.point = A
    }
    function C() {
      ;(q[0] = B), (q[1] = J), (Q.point = F), (G = null)
    }
    function z(c, b) {
      if (G) {
        var a = c - D
        k += hb(a) > 180 ? a + (a > 0 ? 360 : -360) : a
      } else {
        ;(w = c), (O = b)
      }
      gL.point(c, b), A(c, b)
    }
    function I() {
      gL.lineStart()
    }
    function R() {
      z(w, O),
        gL.lineEnd(),
        hb(k) > f9 && (B = -(J = 180)),
        (q[0] = B),
        (q[1] = J),
        (G = null)
    }
    function E(b, a) {
      return (a -= b) < 0 ? a + 360 : a
    }
    function H(b, a) {
      return b[0] - a[0]
    }
    function P(b, a) {
      return a[0] <= a[1] ? a[0] <= b && b <= a[1] : b < a[0] || a[1] < b
    }
    var B,
      L,
      J,
      K,
      D,
      w,
      O,
      G,
      k,
      j,
      q,
      Q = {
        point: F,
        lineStart: N,
        lineEnd: C,
        polygonStart: function() {
          ;(Q.point = z),
            (Q.lineStart = I),
            (Q.lineEnd = R),
            (k = 0),
            gL.polygonStart()
        },
        polygonEnd: function() {
          gL.polygonEnd(),
            (Q.point = F),
            (Q.lineStart = N),
            (Q.lineEnd = C),
            0 > ff
              ? ((B = -(J = 180)), (L = -(K = 90)))
              : k > f9
                ? (K = 90)
                : -f9 > k && (L = -90),
            (q[0] = B),
            (q[1] = J)
        },
      }
    return function(m) {
      ;(K = J = -(B = L = 1 / 0)), (j = []), dU.geo.stream(m, Q)
      var f = j.length
      if (f) {
        j.sort(H)
        for (var l, g = 1, c = j[0], d = [c]; f > g; ++g) {
          ;(l = j[g]),
            P(l[0], c) || P(l[1], c)
              ? (E(c[0], l[1]) > E(c[0], c[1]) && (c[1] = l[1]),
                E(l[0], c[1]) > E(c[0], c[1]) && (c[0] = l[0]))
              : d.push((c = l))
        }
        for (
          var b, l, h = -(1 / 0), f = d.length - 1, g = 0, c = d[f];
          f >= g;
          c = l, ++g
        ) {
          ;(l = d[g]),
            (b = E(c[1], l[0])) > h && ((h = b), (B = l[0]), (J = c[1]))
        }
      }
      return (
        (j = q = null),
        B === 1 / 0 || L === 1 / 0 ? [[NaN, NaN], [NaN, NaN]] : [[B, L], [J, K]]
      )
    }
  })()),
    (dU.geo.centroid = function(f) {
      ;(aW = cw = bF = jI = hs = c0 = eK = fq = bg = f2 = em = 0),
        dU.geo.stream(f, fE)
      var b = bg,
        d = f2,
        c = em,
        a = b * b + d * d + c * c
      return ez > a &&
        ((b = c0),
        (d = eK),
        (c = fq),
        f9 > cw && ((b = bF), (d = jI), (c = hs)),
        (a = b * b + d * d + c * c),
        ez > a)
        ? [NaN, NaN]
        : [Math.atan2(d, b) * cW, bP(c / Math.sqrt(a)) * cW]
    })
  var aW,
    cw,
    bF,
    jI,
    hs,
    c0,
    eK,
    fq,
    bg,
    f2,
    em,
    fE = {
      sphere: iJ,
      point: eZ,
      lineStart: gI,
      lineEnd: aT,
      polygonStart: function() {
        fE.lineStart = cp
      },
      polygonEnd: function() {
        fE.lineStart = gI
      },
    },
    aC = fk(jF, fC, jU, [-fP, -fP / 2]),
    jY = 1000000000
  ;(dU.geo.clipExtent = function() {
    var j,
      f,
      h,
      g,
      c,
      d,
      b = {
        stream: function(a) {
          return c && (c.valid = !1), (c = d(a)), (c.valid = !0), c
        },
        extent: function(a) {
          return arguments.length
            ? ((d = cc(
                (j = +a[0][0]),
                (f = +a[0][1]),
                (h = +a[1][0]),
                (g = +a[1][1])
              )),
              c && ((c.valid = !1), (c = null)),
              b)
            : [[j, f], [h, g]]
        },
      }
    return b.extent([[0, 0], [960, 500]])
  }),
    ((dU.geo.conicEqualArea = function() {
      return dN(c4)
    }).raw = c4),
    (dU.geo.albers = function() {
      return dU.geo
        .conicEqualArea()
        .rotate([96, 0])
        .center([-0.6, 38.7])
        .parallels([29.5, 45.5])
        .scale(1070)
    }),
    (dU.geo.albersUsa = function() {
      function d(o) {
        var l = o[0],
          e = o[1]
        return (m = null), h(l, e), m || (b(l, e), m) || k(l, e), m
      }
      var m,
        h,
        b,
        k,
        g = dU.geo.albers(),
        j = dU.geo
          .conicEqualArea()
          .rotate([154, 0])
          .center([-2, 58.5])
          .parallels([55, 65]),
        c = dU.geo
          .conicEqualArea()
          .rotate([157, 0])
          .center([-3, 19.9])
          .parallels([8, 18]),
        f = {
          point: function(i, a) {
            m = [i, a]
          },
        }
      return (
        (d.invert = function(p) {
          var i = g.scale(),
            o = g.translate(),
            l = (p[0] - o[0]) / i,
            a = (p[1] - o[1]) / i
          return (a >= 0.12 && 0.234 > a && l >= -0.425 && -0.214 > l
            ? j
            : a >= 0.166 && 0.234 > a && l >= -0.214 && -0.115 > l
              ? c
              : g
          ).invert(p)
        }),
        (d.stream = function(o) {
          var a = g.stream(o),
            l = j.stream(o),
            i = c.stream(o)
          return {
            point: function(p, e) {
              a.point(p, e), l.point(p, e), i.point(p, e)
            },
            sphere: function() {
              a.sphere(), l.sphere(), i.sphere()
            },
            lineStart: function() {
              a.lineStart(), l.lineStart(), i.lineStart()
            },
            lineEnd: function() {
              a.lineEnd(), l.lineEnd(), i.lineEnd()
            },
            polygonStart: function() {
              a.polygonStart(), l.polygonStart(), i.polygonStart()
            },
            polygonEnd: function() {
              a.polygonEnd(), l.polygonEnd(), i.polygonEnd()
            },
          }
        }),
        (d.precision = function(a) {
          return arguments.length
            ? (g.precision(a), j.precision(a), c.precision(a), d)
            : g.precision()
        }),
        (d.scale = function(a) {
          return arguments.length
            ? (g.scale(a),
              j.scale(0.35 * a),
              c.scale(a),
              d.translate(g.translate()))
            : g.scale()
        }),
        (d.translate = function(a) {
          if (!arguments.length) {
            return g.translate()
          }
          var l = g.scale(),
            e = +a[0],
            i = +a[1]
          return (
            (h = g
              .translate(a)
              .clipExtent([
                [e - 0.455 * l, i - 0.238 * l],
                [e + 0.455 * l, i + 0.238 * l],
              ])
              .stream(f).point),
            (b = j
              .translate([e - 0.307 * l, i + 0.201 * l])
              .clipExtent([
                [e - 0.425 * l + f9, i + 0.12 * l + f9],
                [e - 0.214 * l - f9, i + 0.234 * l - f9],
              ])
              .stream(f).point),
            (k = c
              .translate([e - 0.205 * l, i + 0.212 * l])
              .clipExtent([
                [e - 0.214 * l + f9, i + 0.166 * l + f9],
                [e - 0.115 * l - f9, i + 0.234 * l - f9],
              ])
              .stream(f).point),
            d
          )
        }),
        d.scale(1070)
      )
    })
  var gq,
    jh,
    cO,
    cg,
    dQ,
    c7,
    d1 = {
      point: iJ,
      lineStart: iJ,
      lineEnd: iJ,
      polygonStart: function() {
        ;(jh = 0), (d1.lineStart = dY)
      },
      polygonEnd: function() {
        ;(d1.lineStart = d1.lineEnd = d1.point = iJ), (gq += hb(jh / 2))
      },
    },
    bY = {
      point: bU,
      lineStart: iJ,
      lineEnd: iJ,
      polygonStart: iJ,
      polygonEnd: iJ,
    },
    dy = {
      point: kj,
      lineStart: hJ,
      lineEnd: fI,
      polygonStart: function() {
        dy.lineStart = eq
      },
      polygonEnd: function() {
        ;(dy.point = kj), (dy.lineStart = hJ), (dy.lineEnd = fI)
      },
    }
  ;(dU.geo.path = function() {
    function k(a) {
      return (
        a &&
          ('function' == typeof j && d.pointRadius(+j.apply(this, arguments)),
          (b && b.valid) || (b = c(d)),
          dU.geo.stream(a, b)),
        d.result()
      )
    }
    function f() {
      return (b = null), k
    }
    var h,
      g,
      c,
      d,
      b,
      j = 4.5
    return (
      (k.area = function(a) {
        return (gq = 0), dU.geo.stream(a, c(d1)), gq
      }),
      (k.centroid = function(a) {
        return (
          (bF = jI = hs = c0 = eK = fq = bg = f2 = em = 0),
          dU.geo.stream(a, c(dy)),
          em
            ? [bg / em, f2 / em]
            : fq
              ? [c0 / fq, eK / fq]
              : hs
                ? [bF / hs, jI / hs]
                : [NaN, NaN]
        )
      }),
      (k.bounds = function(a) {
        return (
          (dQ = c7 = -(cO = cg = 1 / 0)),
          dU.geo.stream(a, c(bY)),
          [[cO, cg], [dQ, c7]]
        )
      }),
      (k.projection = function(a) {
        return arguments.length
          ? ((c = (h = a) ? a.stream || cQ(a) : hV), f())
          : h
      }),
      (k.context = function(a) {
        return arguments.length
          ? ((d = null == (g = a) ? new dv() : new bT(a)),
            'function' != typeof j && d.pointRadius(j),
            f())
          : g
      }),
      (k.pointRadius = function(a) {
        return arguments.length
          ? ((j = 'function' == typeof a ? a : (d.pointRadius(+a), +a)), k)
          : j
      }),
      k.projection(dU.geo.albersUsa()).context(null)
    )
  }),
    (dU.geo.transform = function(a) {
      return {
        stream: function(b) {
          var d = new bB(b)
          for (var c in a) {
            d[c] = a[c]
          }
          return d
        },
      }
    }),
    (bB.prototype = {
      point: function(b, a) {
        this.stream.point(b, a)
      },
      sphere: function() {
        this.stream.sphere()
      },
      lineStart: function() {
        this.stream.lineStart()
      },
      lineEnd: function() {
        this.stream.lineEnd()
      },
      polygonStart: function() {
        this.stream.polygonStart()
      },
      polygonEnd: function() {
        this.stream.polygonEnd()
      },
    }),
    (dU.geo.projection = j0),
    (dU.geo.projectionMutator = dS),
    ((dU.geo.equirectangular = function() {
      return j0(iC)
    }).raw = iC.invert = iC),
    (dU.geo.rotation = function(b) {
      function a(c) {
        return (c = b(c[0] * jH, c[1] * jH)), (c[0] *= cW), (c[1] *= cW), c
      }
      return (
        (b = g5((b[0] % 360) * jH, b[1] * jH, b.length > 2 ? b[2] * jH : 0)),
        (a.invert = function(c) {
          return (
            (c = b.invert(c[0] * jH, c[1] * jH)), (c[0] *= cW), (c[1] *= cW), c
          )
        }),
        a
      )
    }),
    (ck.invert = iC),
    (dU.geo.circle = function() {
      function f() {
        var h = 'function' == typeof c ? c.apply(this, arguments) : c,
          g = g5(-h[0] * jH, -h[1] * jH, 0).invert,
          e = []
        return (
          d(null, null, 1, {
            point: function(j, i) {
              e.push((j = g(j, i))), (j[0] *= cW), (j[1] *= cW)
            },
          }),
          { type: 'Polygon', coordinates: [e] }
        )
      }
      var b,
        d,
        c = [0, 0],
        a = 6
      return (
        (f.origin = function(e) {
          return arguments.length ? ((c = e), f) : c
        }),
        (f.angle = function(e) {
          return arguments.length ? ((d = bb((b = +e) * jH, a * jH)), f) : b
        }),
        (f.precision = function(e) {
          return arguments.length ? ((d = bb(b * jH, (a = +e) * jH)), f) : a
        }),
        f.angle(90)
      )
    }),
    (dU.geo.distance = function(g, w) {
      var m,
        b = (w[0] - g[0]) * jH,
        v = g[1] * jH,
        j = w[1] * jH,
        q = Math.sin(b),
        d = Math.cos(b),
        h = Math.sin(v),
        p = Math.cos(v),
        x = Math.sin(j),
        k = Math.cos(j)
      return Math.atan2(
        Math.sqrt((m = k * q) * m + (m = p * x - h * k * d) * m),
        h * x + p * k * d
      )
    }),
    (dU.geo.graticule = function() {
      function B() {
        return { type: 'MultiLineString', coordinates: q() }
      }
      function q() {
        return dU
          .range(Math.ceil(E / J) * J, k, J)
          .map(F)
          .concat(dU.range(Math.ceil(K / C) * C, D, C).map(G))
          .concat(
            dU
              .range(Math.ceil(x / z) * z, I, z)
              .filter(function(a) {
                return hb(a % J) > f9
              })
              .map(w)
          )
          .concat(
            dU
              .range(Math.ceil(A / j) * j, L, j)
              .filter(function(a) {
                return hb(a % C) > f9
              })
              .map(H)
          )
      }
      var I,
        x,
        k,
        E,
        L,
        A,
        D,
        K,
        w,
        H,
        F,
        G,
        z = 10,
        j = z,
        J = 90,
        C = 360,
        b = 2.5
      return (
        (B.lines = function() {
          return q().map(function(a) {
            return { type: 'LineString', coordinates: a }
          })
        }),
        (B.outline = function() {
          return {
            type: 'Polygon',
            coordinates: [
              F(E).concat(
                G(D).slice(1),
                F(k)
                  .reverse()
                  .slice(1),
                G(K)
                  .reverse()
                  .slice(1)
              ),
            ],
          }
        }),
        (B.extent = function(a) {
          return arguments.length
            ? B.majorExtent(a).minorExtent(a)
            : B.minorExtent()
        }),
        (B.majorExtent = function(a) {
          return arguments.length
            ? ((E = +a[0][0]),
              (k = +a[1][0]),
              (K = +a[0][1]),
              (D = +a[1][1]),
              E > k && ((a = E), (E = k), (k = a)),
              K > D && ((a = K), (K = D), (D = a)),
              B.precision(b))
            : [[E, K], [k, D]]
        }),
        (B.minorExtent = function(a) {
          return arguments.length
            ? ((x = +a[0][0]),
              (I = +a[1][0]),
              (A = +a[0][1]),
              (L = +a[1][1]),
              x > I && ((a = x), (x = I), (I = a)),
              A > L && ((a = A), (A = L), (L = a)),
              B.precision(b))
            : [[x, A], [I, L]]
        }),
        (B.step = function(a) {
          return arguments.length ? B.majorStep(a).minorStep(a) : B.minorStep()
        }),
        (B.majorStep = function(a) {
          return arguments.length ? ((J = +a[0]), (C = +a[1]), B) : [J, C]
        }),
        (B.minorStep = function(a) {
          return arguments.length ? ((z = +a[0]), (j = +a[1]), B) : [z, j]
        }),
        (B.precision = function(a) {
          return arguments.length
            ? ((b = +a),
              (w = eM(A, L, 90)),
              (H = kh(x, I, b)),
              (F = eM(K, D, 90)),
              (G = kh(E, k, b)),
              B)
            : b
        }),
        B.majorExtent([[-180, -90 + f9], [180, 90 - f9]]).minorExtent([
          [-180, -80 - f9],
          [180, 80 + f9],
        ])
      )
    }),
    (dU.geo.greatArc = function() {
      function f() {
        return {
          type: 'LineString',
          coordinates: [
            b || c.apply(this, arguments),
            d || a.apply(this, arguments),
          ],
        }
      }
      var b,
        d,
        c = g9,
        a = aw
      return (
        (f.distance = function() {
          return dU.geo.distance(
            b || c.apply(this, arguments),
            d || a.apply(this, arguments)
          )
        }),
        (f.source = function(g) {
          return arguments.length
            ? ((c = g), (b = 'function' == typeof g ? null : g), f)
            : c
        }),
        (f.target = function(e) {
          return arguments.length
            ? ((a = e), (d = 'function' == typeof e ? null : e), f)
            : a
        }),
        (f.precision = function() {
          return arguments.length ? f : 0
        }),
        f
      )
    }),
    (dU.geo.interpolate = function(b, a) {
      return jq(b[0] * jH, b[1] * jH, a[0] * jH, a[1] * jH)
    }),
    (dU.geo.length = function(a) {
      return (ix = 0), dU.geo.stream(a, kp), ix
    })
  var ix,
    kp = {
      sphere: iJ,
      point: iJ,
      lineStart: aE,
      lineEnd: iJ,
      polygonStart: iJ,
      polygonEnd: iJ,
    },
    hM = aS(
      function(a) {
        return Math.sqrt(2 / (1 + a))
      },
      function(a) {
        return 2 * Math.asin(a / 2)
      }
    )
  ;(dU.geo.azimuthalEqualArea = function() {
    return j0(hM)
  }).raw = hM
  var fL = aS(function(b) {
    var a = Math.acos(b)
    return a && a / Math.sin(a)
  }, hV)
  ;((dU.geo.azimuthalEquidistant = function() {
    return j0(fL)
  }).raw = fL),
    ((dU.geo.conicConformal = function() {
      return dN(e8)
    }).raw = e8),
    ((dU.geo.conicEquidistant = function() {
      return dN(fv)
    }).raw = fv)
  var ed = aS(function(a) {
    return 1 / a
  }, Math.atan)
  ;((dU.geo.gnomonic = function() {
    return j0(ed)
  }).raw = ed),
    (gR.invert = function(b, a) {
      return [b, 2 * Math.atan(Math.exp(a)) - gA]
    }),
    ((dU.geo.mercator = function() {
      return a1(gR)
    }).raw = gR)
  var bQ = aS(function() {
    return 1
  }, Math.asin)
  ;(dU.geo.orthographic = function() {
    return j0(bQ)
  }).raw = bQ
  var hk = aS(
    function(a) {
      return 1 / (1 + a)
    },
    function(a) {
      return 2 * Math.atan(a)
    }
  )
  ;((dU.geo.stereographic = function() {
    return j0(hk)
  }).raw = hk),
    (cD.invert = function(b, a) {
      return [-a, 2 * Math.atan(Math.exp(b)) - gA]
    }),
    ((dU.geo.transverseMercator = function() {
      var c = a1(cD),
        a = c.center,
        b = c.rotate
      return (
        (c.center = function(d) {
          return d ? a([-d[1], d[0]]) : ((d = a()), [d[1], -d[0]])
        }),
        (c.rotate = function(d) {
          return d
            ? b([d[0], d[1], d.length > 2 ? d[2] + 90 : 90])
            : ((d = b()), [d[0], d[1], d[2] - 90])
        }),
        b([0, 0, 90])
      )
    }).raw = cD),
    (dU.geom = {}),
    (dU.geom.hull = function(d) {
      function a(j) {
        if (j.length < 3) {
          return []
        }
        var y,
          x = aX(c),
          m = aX(b),
          w = j.length,
          e = [],
          k = []
        for (y = 0; w > y; y++) {
          e.push([+x.call(this, j[y], y), +m.call(this, j[y], y), y])
        }
        for (e.sort(c9), y = 0; w > y; y++) {
          k.push([e[y][0], -e[y][1]])
        }
        var v = hB(e),
          z = hB(k),
          r = z[0] === v[0],
          p = z[z.length - 1] === v[v.length - 1],
          q = []
        for (y = v.length - 1; y >= 0; --y) {
          q.push(j[e[v[y]][2]])
        }
        for (y = +r; y < z.length - p; ++y) {
          q.push(j[e[z[y]][2]])
        }
        return q
      }
      var c = bK,
        b = jS
      return arguments.length
        ? a(d)
        : ((a.x = function(e) {
            return arguments.length ? ((c = e), a) : c
          }),
          (a.y = function(e) {
            return arguments.length ? ((b = e), a) : b
          }),
          a)
    }),
    (dU.geom.polygon = function(a) {
      return e9(a, cK), a
    })
  var cK = (dU.geom.polygon.prototype = [])
  ;(cK.area = function() {
    for (var f, b = -1, d = this.length, c = this[d - 1], a = 0; ++b < d; ) {
      ;(f = c), (c = this[b]), (a += f[1] * c[0] - f[0] * c[1])
    }
    return 0.5 * a
  }),
    (cK.centroid = function(k) {
      var f,
        h,
        g = -1,
        c = this.length,
        d = 0,
        b = 0,
        j = this[c - 1]
      for (arguments.length || (k = -1 / (6 * this.area())); ++g < c; ) {
        ;(f = j),
          (j = this[g]),
          (h = f[0] * j[1] - j[0] * f[1]),
          (d += (f[0] + j[0]) * h),
          (b += (f[1] + j[1]) * h)
      }
      return [d * k, b * k]
    }),
    (cK.clip = function(f) {
      for (
        var q,
          j,
          b,
          p,
          h,
          m,
          d = bp(f),
          g = -1,
          k = this.length - bp(this),
          v = this[k - 1];
        ++g < k;

      ) {
        for (
          q = f.slice(),
            f.length = 0,
            p = this[g],
            h = q[(b = q.length - d) - 1],
            j = -1;
          ++j < b;

        ) {
          ;(m = q[j]),
            eQ(m, v, p)
              ? (eQ(h, v, p) || f.push(fz(h, m, v, p)), f.push(m))
              : eQ(h, v, p) && f.push(fz(h, m, v, p)),
            (h = m)
        }
        d && f.push(f[0]), (v = p)
      }
      return f
    })
  var by,
    fY,
    jT,
    dM,
    eY,
    ig = [],
    cb = []
  ;(cV.prototype.prepare = function() {
    for (var c, a = this.edges, b = a.length; b--; ) {
      ;(c = a[b].edge), (c.b && c.a) || a.splice(b, 1)
    }
    return a.sort(dX), a.length
  }),
    (bM.prototype = {
      start: function() {
        return this.edge.l === this.site ? this.edge.a : this.edge.b
      },
      end: function() {
        return this.edge.l === this.site ? this.edge.b : this.edge.a
      },
    }),
    (hf.prototype = {
      insert: function(f, b) {
        var d, c, a
        if (f) {
          if (((b.P = f), (b.N = f.N), f.N && (f.N.P = b), (f.N = b), f.R)) {
            for (f = f.R; f.L; ) {
              f = f.L
            }
            f.L = b
          } else {
            f.R = b
          }
          d = f
        } else {
          this._
            ? ((f = jN(this._)),
              (b.P = null),
              (b.N = f),
              (f.P = f.L = b),
              (d = f))
            : ((b.P = b.N = null), (this._ = b), (d = null))
        }
        for (b.L = b.R = null, b.U = d, b.C = !0, f = b; d && d.C; ) {
          ;(c = d.U),
            d === c.L
              ? ((a = c.R),
                a && a.C
                  ? ((d.C = a.C = !1), (c.C = !0), (f = c))
                  : (f === d.R && (bs(this, d), (f = d), (d = f.U)),
                    (d.C = !1),
                    (c.C = !0),
                    fV(this, c)))
              : ((a = c.L),
                a && a.C
                  ? ((d.C = a.C = !1), (c.C = !0), (f = c))
                  : (f === d.L && (fV(this, d), (f = d), (d = f.U)),
                    (d.C = !1),
                    (c.C = !0),
                    bs(this, c))),
            (d = f.U)
        }
        this._.C = !1
      },
      remove: function(j) {
        j.N && (j.N.P = j.P), j.P && (j.P.N = j.N), (j.N = j.P = null)
        var f,
          h,
          g,
          c = j.U,
          d = j.L,
          b = j.R
        if (
          ((h = d ? (b ? jN(b) : d) : b),
          c ? (c.L === j ? (c.L = h) : (c.R = h)) : (this._ = h),
          d && b
            ? ((g = h.C),
              (h.C = j.C),
              (h.L = d),
              (d.U = h),
              h !== b
                ? ((c = h.U),
                  (h.U = j.U),
                  (j = h.R),
                  (c.L = j),
                  (h.R = b),
                  (b.U = h))
                : ((h.U = c), (c = h), (j = h.R)))
            : ((g = j.C), (j = h)),
          j && (j.U = c),
          !g)
        ) {
          if (j && j.C) {
            return void (j.C = !1)
          }
          do {
            if (j === this._) {
              break
            }
            if (j === c.L) {
              if (
                ((f = c.R),
                f.C && ((f.C = !1), (c.C = !0), bs(this, c), (f = c.R)),
                (f.L && f.L.C) || (f.R && f.R.C))
              ) {
                ;(f.R && f.R.C) ||
                  ((f.L.C = !1), (f.C = !0), fV(this, f), (f = c.R)),
                  (f.C = c.C),
                  (c.C = f.R.C = !1),
                  bs(this, c),
                  (j = this._)
                break
              }
            } else {
              if (
                ((f = c.L),
                f.C && ((f.C = !1), (c.C = !0), fV(this, c), (f = c.L)),
                (f.L && f.L.C) || (f.R && f.R.C))
              ) {
                ;(f.L && f.L.C) ||
                  ((f.R.C = !1), (f.C = !0), bs(this, f), (f = c.L)),
                  (f.C = c.C),
                  (c.C = f.L.C = !1),
                  fV(this, c),
                  (j = this._)
                break
              }
            }
            ;(f.C = !0), (j = c), (c = c.U)
          } while (!j.C)
          j && (j.C = !1)
        }
      },
    }),
    (dU.geom.voronoi = function(k) {
      function f(q) {
        var o = new Array(q.length),
          p = j[0][0],
          l = j[0][1],
          m = j[1][0],
          e = j[1][1]
        return (
          dI(h(q), j).cells.forEach(function(n, r) {
            var a = n.edges,
              t = n.site,
              i = (o[r] = a.length
                ? a.map(function(u) {
                    var s = u.start()
                    return [s.x, s.y]
                  })
                : t.x >= p && t.x <= m && t.y >= l && t.y <= e
                  ? [[p, e], [m, e], [m, l], [p, l]]
                  : [])
            i.point = q[r]
          }),
          o
        )
      }
      function h(a) {
        return a.map(function(i, e) {
          return {
            x: Math.round(d(i, e) / f9) * f9,
            y: Math.round(b(i, e) / f9) * f9,
            i: e,
          }
        })
      }
      var g = bK,
        c = jS,
        d = g,
        b = c,
        j = gZ
      return k
        ? f(k)
        : ((f.links = function(a) {
            return dI(h(a))
              .edges.filter(function(e) {
                return e.l && e.r
              })
              .map(function(e) {
                return { source: a[e.l.i], target: a[e.r.i] }
              })
          }),
          (f.triangles = function(e) {
            var a = []
            return (
              dI(h(e)).cells.forEach(function(v, m) {
                for (
                  var y,
                    q,
                    x = v.site,
                    n = v.edges.sort(dX),
                    p = -1,
                    w = n.length,
                    z = n[w - 1].edge,
                    t = z.l === x ? z.r : z.l;
                  ++p < w;

                ) {
                  ;(y = z),
                    (q = t),
                    (z = n[p].edge),
                    (t = z.l === x ? z.r : z.l),
                    m < q.i &&
                      m < t.i &&
                      h4(x, q, t) < 0 &&
                      a.push([e[m], e[q.i], e[t.i]])
                }
              }),
              a
            )
          }),
          (f.x = function(a) {
            return arguments.length ? ((d = aX((g = a))), f) : g
          }),
          (f.y = function(a) {
            return arguments.length ? ((b = aX((c = a))), f) : c
          }),
          (f.clipExtent = function(a) {
            return arguments.length
              ? ((j = null == a ? gZ : a), f)
              : j === gZ
                ? null
                : j
          }),
          (f.size = function(a) {
            return arguments.length
              ? f.clipExtent(a && [[0, 0], a])
              : j === gZ
                ? null
                : j && j[1]
          }),
          f)
    })
  var gZ = [[-1000000, -1000000], [1000000, 1000000]]
  ;(dU.geom.delaunay = function(a) {
    return dU.geom.voronoi().triangles(a)
  }),
    (dU.geom.quadtree = function(d, m, h, b, k) {
      function g(z) {
        function C(w, O, L, p, N, y, M, v) {
          if (!isNaN(L) && !isNaN(p)) {
            if (w.leaf) {
              var x = w.x,
                P = w.y
              if (null != x) {
                if (hb(x - L) + hb(P - p) < 0.01) {
                  H(w, O, L, p, N, y, M, v)
                } else {
                  var K = w.point
                  ;(w.x = w.y = w.point = null),
                    H(w, K, x, P, N, y, M, v),
                    H(w, O, L, p, N, y, M, v)
                }
              } else {
                ;(w.x = L), (w.y = p), (w.point = O)
              }
            } else {
              H(w, O, L, p, N, y, M, v)
            }
          }
        }
        function H(v, P, L, i, O, N, p, w) {
          var M = 0.5 * (O + p),
            Q = 0.5 * (N + w),
            K = L >= M,
            x = i >= Q,
            y = (x << 1) | K
          ;(v.leaf = !1),
            (v = v.nodes[y] || (v.nodes[y] = gd())),
            K ? (O = M) : (p = M),
            x ? (N = Q) : (w = Q),
            C(v, P, L, i, O, N, p, w)
        }
        var t,
          F,
          D,
          E,
          u,
          r,
          G,
          A,
          l,
          e = aX(c),
          o = aX(f)
        if (null != m) {
          ;(r = m), (G = h), (A = b), (l = k)
        } else {
          if (
            ((A = l = -(r = G = 1 / 0)), (F = []), (D = []), (u = z.length), j)
          ) {
            for (E = 0; u > E; ++E) {
              ;(t = z[E]),
                t.x < r && (r = t.x),
                t.y < G && (G = t.y),
                t.x > A && (A = t.x),
                t.y > l && (l = t.y),
                F.push(t.x),
                D.push(t.y)
            }
          } else {
            for (E = 0; u > E; ++E) {
              var I = +e((t = z[E]), E),
                J = +o(t, E)
              r > I && (r = I),
                G > J && (G = J),
                I > A && (A = I),
                J > l && (l = J),
                F.push(I),
                D.push(J)
            }
          }
        }
        var q = A - r,
          a = l - G
        q > a ? (l = G + q) : (A = r + a)
        var B = gd()
        if (
          ((B.add = function(i) {
            C(B, i, +e(i, ++E), +o(i, E), r, G, A, l)
          }),
          (B.visit = function(i) {
            gD(i, B, r, G, A, l)
          }),
          (B.find = function(i) {
            return dk(B, i[0], i[1], r, G, A, l)
          }),
          (E = -1),
          null == m)
        ) {
          for (; ++E < u; ) {
            C(B, z[E], F[E], D[E], r, G, A, l)
          }
          --E
        } else {
          z.forEach(B.add)
        }
        return (F = D = z = t = null), B
      }
      var j,
        c = bK,
        f = jS
      return (j = arguments.length)
        ? ((c = b5), (f = gV), 3 === j && ((k = h), (b = m), (h = m = 0)), g(d))
        : ((g.x = function(a) {
            return arguments.length ? ((c = a), g) : c
          }),
          (g.y = function(a) {
            return arguments.length ? ((f = a), g) : f
          }),
          (g.extent = function(a) {
            return arguments.length
              ? (null == a
                  ? (m = h = b = k = null)
                  : ((m = +a[0][0]),
                    (h = +a[0][1]),
                    (b = +a[1][0]),
                    (k = +a[1][1])),
                g)
              : null == m
                ? null
                : [[m, h], [b, k]]
          }),
          (g.size = function(a) {
            return arguments.length
              ? (null == a
                  ? (m = h = b = k = null)
                  : ((m = h = 0), (b = +a[0]), (k = +a[1])),
                g)
              : null == m
                ? null
                : [b - m, k - h]
          }),
          g)
    }),
    (dU.interpolateRgb = a3),
    (dU.interpolateObject = hF),
    (dU.interpolateNumber = eC),
    (dU.interpolateString = j8)
  var gj = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g,
    gH = new RegExp(gj.source, 'g')
  ;(dU.interpolate = g1),
    (dU.interpolators = [
      function(c, a) {
        var b = typeof a
        return ('string' === b
          ? bw.has(a.toLowerCase()) || /^(#|rgb\(|hsl\()/i.test(a)
            ? a3
            : j8
          : a instanceof dK
            ? a3
            : Array.isArray(a)
              ? ab
              : 'object' === b && isNaN(a)
                ? hF
                : eC)(c, a)
      },
    ]),
    (dU.interpolateArray = ab)
  var ds = function() {
      return hV
    },
    a7 = dU.map({
      linear: ds,
      poly: aU,
      quad: function() {
        return e0
      },
      cubic: function() {
        return fd
      },
      sin: function() {
        return cq
      },
      exp: function() {
        return bD
      },
      circle: function() {
        return jG
      },
      elastic: hp,
      back: cZ,
      bounce: function() {
        return eI
      },
    }),
    hI = dU.map({
      in: hV,
      out: ak,
      'in-out': aL,
      'out-in': function(a) {
        return aL(ak(a))
      },
    })
  ;(dU.ease = function(d) {
    var a = d.indexOf('-'),
      c = a >= 0 ? d.slice(0, a) : d,
      b = a >= 0 ? d.slice(a + 1) : 'in'
    return (
      (c = a7.get(c) || ds),
      (b = hI.get(b) || hV),
      iX(b(c.apply(null, e6.call(arguments, 1))))
    )
  }),
    (dU.interpolateHcl = fm),
    (dU.interpolateHsl = bd),
    (dU.interpolateLab = f0),
    (dU.interpolateRound = eh),
    (dU.transform = function(b) {
      var a = cs.createElementNS(dU.ns.prefix.svg, 'g')
      return (dU.transform = function(d) {
        if (null != d) {
          a.setAttribute('transform', d)
          var c = a.transform.baseVal.consolidate()
        }
        return new fD(c ? c.matrix : eG)
      })(b)
    }),
    (fD.prototype.toString = function() {
      return (
        'translate(' +
        this.translate +
        ')rotate(' +
        this.rotate +
        ')skewX(' +
        this.skew +
        ')scale(' +
        this.scale +
        ')'
      )
    })
  var eG = { a: 1, b: 0, c: 0, d: 1, e: 0, f: 0 }
  ;(dU.interpolateTransform = dZ),
    (dU.layout = {}),
    (dU.layout.bundle = function() {
      return function(d) {
        for (var a = [], c = -1, b = d.length; ++c < b; ) {
          a.push(ip(d[c]))
        }
        return a
      }
    }),
    (dU.layout.chord = function() {
      function f() {
        var e,
          z,
          s,
          o,
          r,
          a = {},
          F = [],
          u = dU.range(h),
          i = []
        for (j = [], b = [], e = 0, o = -1; ++o < h; ) {
          for (z = 0, r = -1; ++r < h; ) {
            z += p[o][r]
          }
          F.push(z), i.push(dU.range(h)), (e += z)
        }
        for (
          m &&
            u.sort(function(w, c) {
              return m(F[w], F[c])
            }),
            d &&
              i.forEach(function(w, c) {
                w.sort(function(y, x) {
                  return d(p[c][y], p[c][x])
                })
              }),
            e = (aM - v * h) / e,
            z = 0,
            o = -1;
          ++o < h;

        ) {
          for (s = z, r = -1; ++r < h; ) {
            var B = u[o],
              t = i[B][r],
              C = p[B][t],
              A = z,
              E = (z += C * e)
            a[B + '-' + t] = {
              index: B,
              subindex: t,
              startAngle: A,
              endAngle: E,
              value: C,
            }
          }
          ;(b[B] = { index: B, startAngle: s, endAngle: z, value: F[B] }),
            (z += v)
        }
        for (o = -1; ++o < h; ) {
          for (r = o - 1; ++r < h; ) {
            var D = a[o + '-' + r],
              l = a[r + '-' + o]
            ;(D.value || l.value) &&
              j.push(
                D.value < l.value
                  ? { source: l, target: D }
                  : { source: D, target: l }
              )
          }
        }
        g && q()
      }
      function q() {
        j.sort(function(c, a) {
          return g(
            (c.source.value + c.target.value) / 2,
            (a.source.value + a.target.value) / 2
          )
        })
      }
      var j,
        b,
        p,
        h,
        m,
        d,
        g,
        k = {},
        v = 0
      return (
        (k.matrix = function(a) {
          return arguments.length
            ? ((h = (p = a) && p.length), (j = b = null), k)
            : p
        }),
        (k.padding = function(a) {
          return arguments.length ? ((v = a), (j = b = null), k) : v
        }),
        (k.sortGroups = function(a) {
          return arguments.length ? ((m = a), (j = b = null), k) : m
        }),
        (k.sortSubgroups = function(a) {
          return arguments.length ? ((d = a), (j = null), k) : d
        }),
        (k.sortChords = function(a) {
          return arguments.length ? ((g = a), j && q(), k) : g
        }),
        (k.chords = function() {
          return j || f(), j
        }),
        (k.groups = function() {
          return b || f(), b
        }),
        k
      )
    }),
    (dU.layout.force = function() {
      function C(a) {
        return function(v, m, d, s) {
          if (v.point !== a) {
            var h = v.cx - a.x,
              p = v.cy - a.y,
              f = s - m,
              g = h * h + p * p
            if (g > (f * f) / D) {
              if (k > g) {
                var n = v.charge / g
                ;(a.px -= h * n), (a.py -= p * n)
              }
              return !0
            }
            if (v.point && g && k > g) {
              var n = v.pointCharge / g
              ;(a.px -= h * n), (a.py -= p * n)
            }
          }
          return !v.charge
        }
      }
      function w(a) {
        ;(a.px = dU.event.x), (a.py = dU.event.y), E.resume()
      }
      var J,
        z,
        q,
        F,
        N,
        B,
        E = {},
        L = dU.dispatch('start', 'tick', 'end'),
        y = [1, 1],
        I = 0.9,
        G = kd,
        H = g6,
        A = -30,
        k = ah,
        K = 0.1,
        D = 0.64,
        b = [],
        j = []
      return (
        (E.tick = function() {
          if ((q *= 0.99) < 0.005) {
            return (J = null), L.end({ type: 'end', alpha: (q = 0) }), !0
          }
          var u,
            a,
            d,
            e,
            f,
            s,
            c,
            n,
            i,
            p = b.length,
            o = j.length
          for (a = 0; o > a; ++a) {
            ;(d = j[a]),
              (e = d.source),
              (f = d.target),
              (n = f.x - e.x),
              (i = f.y - e.y),
              (s = n * n + i * i) &&
                ((s = (q * N[a] * ((s = Math.sqrt(s)) - F[a])) / s),
                (n *= s),
                (i *= s),
                (f.x -=
                  n *
                  (c =
                    e.weight + f.weight
                      ? e.weight / (e.weight + f.weight)
                      : 0.5)),
                (f.y -= i * c),
                (e.x += n * (c = 1 - c)),
                (e.y += i * c))
          }
          if ((c = q * K) && ((n = y[0] / 2), (i = y[1] / 2), (a = -1), c)) {
            for (; ++a < p; ) {
              ;(d = b[a]), (d.x += (n - d.x) * c), (d.y += (i - d.y) * c)
            }
          }
          if (A) {
            for (cC((u = dU.geom.quadtree(b)), q, B), a = -1; ++a < p; ) {
              ;(d = b[a]).fixed || u.visit(C(d))
            }
          }
          for (a = -1; ++a < p; ) {
            ;(d = b[a]),
              d.fixed
                ? ((d.x = d.px), (d.y = d.py))
                : ((d.x -= (d.px - (d.px = d.x)) * I),
                  (d.y -= (d.py - (d.py = d.y)) * I))
          }
          L.tick({ type: 'tick', alpha: q })
        }),
        (E.nodes = function(a) {
          return arguments.length ? ((b = a), E) : b
        }),
        (E.links = function(a) {
          return arguments.length ? ((j = a), E) : j
        }),
        (E.size = function(a) {
          return arguments.length ? ((y = a), E) : y
        }),
        (E.linkDistance = function(a) {
          return arguments.length
            ? ((G = 'function' == typeof a ? a : +a), E)
            : G
        }),
        (E.distance = E.linkDistance),
        (E.linkStrength = function(a) {
          return arguments.length
            ? ((H = 'function' == typeof a ? a : +a), E)
            : H
        }),
        (E.friction = function(a) {
          return arguments.length ? ((I = +a), E) : I
        }),
        (E.charge = function(a) {
          return arguments.length
            ? ((A = 'function' == typeof a ? a : +a), E)
            : A
        }),
        (E.chargeDistance = function(a) {
          return arguments.length ? ((k = a * a), E) : Math.sqrt(k)
        }),
        (E.gravity = function(a) {
          return arguments.length ? ((K = +a), E) : K
        }),
        (E.theta = function(a) {
          return arguments.length ? ((D = a * a), E) : Math.sqrt(D)
        }),
        (E.alpha = function(a) {
          return arguments.length
            ? ((a = +a),
              q
                ? a > 0
                  ? (q = a)
                  : ((J.c = null),
                    (J.t = NaN),
                    (J = null),
                    L.end({ type: 'end', alpha: (q = 0) }))
                : a > 0 &&
                  (L.start({ type: 'start', alpha: (q = a) }),
                  (J = c1(E.tick))),
              E)
            : q
        }),
        (E.start = function() {
          function o(v, t) {
            if (!l) {
              for (l = new Array(d), c = 0; d > c; ++c) {
                l[c] = []
              }
              for (c = 0; m > c; ++c) {
                var f = j[c]
                l[f.source.index].push(f.target),
                  l[f.target.index].push(f.source)
              }
            }
            for (var e, u = l[g], c = -1, p = u.length; ++c < p; ) {
              if (!isNaN((e = u[c][v]))) {
                return e
              }
            }
            return Math.random() * t
          }
          var g,
            l,
            h,
            d = b.length,
            m = j.length,
            i = y[0],
            a = y[1]
          for (g = 0; d > g; ++g) {
            ;((h = b[g]).index = g), (h.weight = 0)
          }
          for (g = 0; m > g; ++g) {
            ;(h = j[g]),
              'number' == typeof h.source && (h.source = b[h.source]),
              'number' == typeof h.target && (h.target = b[h.target]),
              ++h.source.weight,
              ++h.target.weight
          }
          for (g = 0; d > g; ++g) {
            ;(h = b[g]),
              isNaN(h.x) && (h.x = o('x', i)),
              isNaN(h.y) && (h.y = o('y', a)),
              isNaN(h.px) && (h.px = h.x),
              isNaN(h.py) && (h.py = h.y)
          }
          if (((F = []), 'function' == typeof G)) {
            for (g = 0; m > g; ++g) {
              F[g] = +G.call(this, j[g], g)
            }
          } else {
            for (g = 0; m > g; ++g) {
              F[g] = G
            }
          }
          if (((N = []), 'function' == typeof H)) {
            for (g = 0; m > g; ++g) {
              N[g] = +H.call(this, j[g], g)
            }
          } else {
            for (g = 0; m > g; ++g) {
              N[g] = H
            }
          }
          if (((B = []), 'function' == typeof A)) {
            for (g = 0; d > g; ++g) {
              B[g] = +A.call(this, b[g], g)
            }
          } else {
            for (g = 0; d > g; ++g) {
              B[g] = A
            }
          }
          return E.resume()
        }),
        (E.resume = function() {
          return E.alpha(0.1)
        }),
        (E.stop = function() {
          return E.alpha(0)
        }),
        (E.drag = function() {
          return (
            z ||
              (z = dU.behavior
                .drag()
                .origin(hV)
                .on('dragstart.force', fJ)
                .on('drag.force', w)
                .on('dragend.force', d3)),
            arguments.length
              ? void this.on('mouseover.force', bJ)
                  .on('mouseout.force', hc)
                  .call(z)
              : z
          )
        }),
        dU.rebind(E, L, 'on')
      )
    })
  var kd = 20,
    g6 = 1,
    ah = 1 / 0
  ;(dU.layout.hierarchy = function() {
    function d(g) {
      var h,
        f = [g],
        k = []
      for (g.depth = 0; null != (h = f.pop()); ) {
        if ((k.push(h), (m = c.call(d, h, h.depth)) && (e = m.length))) {
          for (var e, m, j; --e >= 0; ) {
            f.push((j = m[e])), (j.parent = h), (j.depth = h.depth + 1)
          }
          b && (h.value = 0), (h.children = m)
        } else {
          b && (h.value = +b.call(d, h, h.depth) || 0), delete h.children
        }
      }
      return (
        jK(g, function(o) {
          var l, i
          a && (l = o.children) && l.sort(a),
            b && (i = o.parent) && (i.value += o.value)
        }),
        k
      )
    }
    var a = hZ,
      c = dG,
      b = eS
    return (
      (d.sort = function(f) {
        return arguments.length ? ((a = f), d) : a
      }),
      (d.children = function(e) {
        return arguments.length ? ((c = e), d) : c
      }),
      (d.value = function(e) {
        return arguments.length ? ((b = e), d) : b
      }),
      (d.revalue = function(e) {
        return (
          b &&
            (fT(e, function(f) {
              f.children && (f.value = 0)
            }),
            jK(e, function(f) {
              var g
              f.children || (f.value = +b.call(d, f, f.depth) || 0),
                (g = f.parent) && (g.value += f.value)
            })),
          e
        )
      }),
      d
    )
  }),
    (dU.layout.partition = function() {
      function f(s, m, g, q) {
        var k = s.children
        if (
          ((s.x = m),
          (s.y = s.depth * q),
          (s.dx = g),
          (s.dy = q),
          k && (p = k.length))
        ) {
          var p,
            h,
            j,
            n = -1
          for (g = s.value ? g / s.value : 0; ++n < p; ) {
            f((h = k[n]), m, (j = h.value * g), q), (m += j)
          }
        }
      }
      function b(l) {
        var k = l.children,
          j = 0
        if (k && (g = k.length)) {
          for (var g, h = -1; ++h < g; ) {
            j = Math.max(j, b(k[h]))
          }
        }
        return 1 + j
      }
      function d(j, h) {
        var g = c.call(this, j, h)
        return f(g[0], 0, a[0], a[1] / b(g[0])), g
      }
      var c = dU.layout.hierarchy(),
        a = [1, 1]
      return (
        (d.size = function(e) {
          return arguments.length ? ((a = e), d) : a
        }),
        bm(d, c)
      )
    }),
    (dU.layout.pie = function() {
      function g(w) {
        var i,
          k = w.length,
          u = w.map(function(l, h) {
            return +c.call(g, l, h)
          }),
          y = +('function' == typeof d ? d.apply(this, arguments) : d),
          r = ('function' == typeof a ? a.apply(this, arguments) : a) - y,
          n = Math.min(
            Math.abs(r) / k,
            +('function' == typeof b ? b.apply(this, arguments) : b)
          ),
          q = n * (0 > r ? -1 : 1),
          e = dU.sum(u),
          x = e ? (r - k * q) / e : 0,
          t = dU.range(k),
          j = []
        return (
          null != f &&
            t.sort(
              f === i6
                ? function(l, h) {
                    return u[h] - u[l]
                  }
                : function(l, h) {
                    return f(w[l], w[h])
                  }
            ),
          t.forEach(function(h) {
            j[h] = {
              data: w[h],
              value: (i = u[h]),
              startAngle: y,
              endAngle: (y += i * x + q),
              padAngle: n,
            }
          }),
          j
        )
      }
      var c = Number,
        f = i6,
        d = 0,
        a = aM,
        b = 0
      return (
        (g.value = function(h) {
          return arguments.length ? ((c = h), g) : c
        }),
        (g.sort = function(e) {
          return arguments.length ? ((f = e), g) : f
        }),
        (g.startAngle = function(e) {
          return arguments.length ? ((d = e), g) : d
        }),
        (g.endAngle = function(e) {
          return arguments.length ? ((a = e), g) : a
        }),
        (g.padAngle = function(e) {
          return arguments.length ? ((b = e), g) : b
        }),
        g
      )
    })
  var i6 = {}
  dU.layout.stack = function() {
    function j(e, k) {
      if (!(n = e.length)) {
        return e
      }
      var u = e.map(function(m, l) {
          return f.call(j, m, l)
        }),
        x = u.map(function(l) {
          return l.map(function(m, o) {
            return [d.call(j, m, o), b.call(j, m, o)]
          })
        }),
        r = h.call(j, x, k)
      ;(u = dU.permute(u, r)), (x = dU.permute(x, r))
      var n,
        q,
        a,
        w,
        t = g.call(j, x, k),
        i = u[0].length
      for (a = 0; i > a; ++a) {
        for (c.call(j, u[0][a], (w = t[a]), x[0][a][1]), q = 1; n > q; ++q) {
          c.call(j, u[q][a], (w += x[q - 1][a][1]), x[q][a][1])
        }
      }
      return e
    }
    var f = hV,
      h = dh,
      g = a0,
      c = gB,
      d = gT,
      b = gb
    return (
      (j.values = function(a) {
        return arguments.length ? ((f = a), j) : f
      }),
      (j.order = function(a) {
        return arguments.length
          ? ((h = 'function' == typeof a ? a : ax.get(a) || dh), j)
          : h
      }),
      (j.offset = function(a) {
        return arguments.length
          ? ((g = 'function' == typeof a ? a : aP.get(a) || a0), j)
          : g
      }),
      (j.x = function(a) {
        return arguments.length ? ((d = a), j) : d
      }),
      (j.y = function(a) {
        return arguments.length ? ((b = a), j) : b
      }),
      (j.out = function(a) {
        return arguments.length ? ((c = a), j) : c
      }),
      j
    )
  }
  var ax = dU.map({
      'inside-out': function(f) {
        var q,
          j,
          b = f.length,
          p = f.map(hD),
          h = f.map(eA),
          m = dU.range(b).sort(function(c, a) {
            return p[c] - p[a]
          }),
          d = 0,
          g = 0,
          k = [],
          v = []
        for (q = 0; b > q; ++q) {
          ;(j = m[q]),
            g > d ? ((d += h[j]), k.push(j)) : ((g += h[j]), v.push(j))
        }
        return v.reverse().concat(k)
      },
      reverse: function(a) {
        return dU.range(a.length).reverse()
      },
      default: dh,
    }),
    aP = dU.map({
      silhouette: function(d) {
        var m,
          h,
          b,
          k = d.length,
          g = d[0].length,
          j = [],
          c = 0,
          f = []
        for (h = 0; g > h; ++h) {
          for (m = 0, b = 0; k > m; m++) {
            b += d[m][h][1]
          }
          b > c && (c = b), j.push(b)
        }
        for (h = 0; g > h; ++h) {
          f[h] = (c - j[h]) / 2
        }
        return f
      },
      wiggle: function(j) {
        var A,
          w,
          b,
          z,
          m,
          y,
          d,
          k,
          x,
          B = j.length,
          v = j[0],
          p = v.length,
          q = []
        for (q[0] = k = x = 0, w = 1; p > w; ++w) {
          for (A = 0, z = 0; B > A; ++A) {
            z += j[A][w][1]
          }
          for (A = 0, m = 0, d = v[w][0] - v[w - 1][0]; B > A; ++A) {
            for (
              b = 0, y = (j[A][w][1] - j[A][w - 1][1]) / (2 * d);
              A > b;
              ++b
            ) {
              y += (j[b][w][1] - j[b][w - 1][1]) / d
            }
            m += y * j[A][w][1]
          }
          ;(q[w] = k -= z ? (m / z) * d : 0), x > k && (x = k)
        }
        for (w = 0; p > w; ++w) {
          q[w] -= x
        }
        return q
      },
      expand: function(k) {
        var f,
          h,
          g,
          c = k.length,
          d = k[0].length,
          b = 1 / c,
          j = []
        for (h = 0; d > h; ++h) {
          for (f = 0, g = 0; c > f; f++) {
            g += k[f][h][1]
          }
          if (g) {
            for (f = 0; c > f; f++) {
              k[f][h][1] /= g
            }
          } else {
            for (f = 0; c > f; f++) {
              k[f][h][1] = b
            }
          }
        }
        for (h = 0; d > h; ++h) {
          j[h] = 0
        }
        return j
      },
      zero: a0,
    })
  ;(dU.layout.histogram = function() {
    function f(k, q) {
      for (
        var w,
          j,
          m = [],
          v = k.map(d, this),
          x = c.call(this, v, q),
          u = a.call(this, x, v, q),
          q = -1,
          r = v.length,
          t = u.length - 1,
          e = b ? 1 : 1 / r;
        ++q < t;

      ) {
        ;(w = m[q] = []), (w.dx = u[q + 1] - (w.x = u[q])), (w.y = 0)
      }
      if (t > 0) {
        for (q = -1; ++q < r; ) {
          ;(j = v[q]),
            j >= x[0] &&
              j <= x[1] &&
              ((w = m[dU.bisect(u, j, 1, t) - 1]), (w.y += e), w.push(k[q]))
        }
      }
      return m
    }
    var b = !0,
      d = Number,
      c = iR,
      a = gY
    return (
      (f.value = function(e) {
        return arguments.length ? ((d = e), f) : d
      }),
      (f.range = function(e) {
        return arguments.length ? ((c = aX(e)), f) : c
      }),
      (f.bins = function(e) {
        return arguments.length
          ? ((a =
              'number' == typeof e
                ? function(g) {
                    return kw(g, e)
                  }
                : aX(e)),
            f)
          : a
      }),
      (f.frequency = function(g) {
        return arguments.length ? ((b = !!g), f) : b
      }),
      f
    )
  }),
    (dU.layout.pack = function() {
      function f(q, h) {
        var g = d.call(this, q, h),
          m = g[0],
          e = a[0],
          p = a[1],
          j =
            null == b
              ? Math.sqrt
              : 'function' == typeof b
                ? b
                : function() {
                    return b
                  }
        if (
          ((m.x = m.y = 0),
          jK(m, function(i) {
            i.r = +j(i.value)
          }),
          jK(m, gG),
          c)
        ) {
          var k = (c * (b ? 1 : Math.max((2 * m.r) / e, (2 * m.r) / p))) / 2
          jK(m, function(i) {
            i.r += k
          }),
            jK(m, gG),
            jK(m, function(i) {
              i.r -= k
            })
        }
        return (
          bA(
            m,
            e / 2,
            p / 2,
            b ? 1 : 1 / Math.max((2 * m.r) / e, (2 * m.r) / p)
          ),
          g
        )
      }
      var b,
        d = dU.layout.hierarchy().sort(ag),
        c = 0,
        a = [1, 1]
      return (
        (f.size = function(e) {
          return arguments.length ? ((a = e), f) : a
        }),
        (f.radius = function(g) {
          return arguments.length
            ? ((b = null == g || 'function' == typeof g ? g : +g), f)
            : b
        }),
        (f.padding = function(e) {
          return arguments.length ? ((c = +e), f) : c
        }),
        bm(f, d)
      )
    }),
    (dU.layout.tree = function() {
      function f(c, x) {
        var z = m.call(this, c, x),
          o = z[0],
          i = q(o)
        if ((jK(i, j), (i.parent.m = -i.z), fT(i, b), k)) {
          fT(o, h)
        } else {
          var l = o,
            a = o,
            w = o
          fT(o, function(s) {
            s.x < l.x && (l = s),
              s.x > a.x && (a = s),
              s.depth > w.depth && (w = s)
          })
          var r = d(l, a) / 2 - l.x,
            e = g[0] / (a.x + d(a, l) / 2 + r),
            t = g[1] / (w.depth || 1)
          fT(o, function(s) {
            ;(s.x = (s.x + r) * e), (s.y = s.depth * t)
          })
        }
        return z
      }
      function q(z) {
        for (
          var v, x = { A: null, children: [z] }, w = [x];
          null != (v = w.pop());

        ) {
          for (var l, s = v.children, c = 0, y = s.length; y > c; ++c) {
            w.push(
              ((s[c] = l = {
                _: s[c],
                parent: v,
                children: ((l = s[c].children) && l.slice()) || [],
                A: null,
                a: null,
                z: 0,
                m: 0,
                c: 0,
                s: 0,
                t: null,
                i: c,
              }).a = l)
            )
          }
        }
        return x.children[0]
      }
      function j(s) {
        var c = s.children,
          o = s.parent.children,
          l = s.i ? o[s.i - 1] : null
        if (c.length) {
          a9(s)
          var a = (c[0].z + c[c.length - 1].z) / 2
          l ? ((s.z = l.z + d(s._, l._)), (s.m = s.z - a)) : (s.z = a)
        } else {
          l && (s.z = l.z + d(s._, l._))
        }
        s.parent.A = p(s, l, s.parent.A || o[0])
      }
      function b(a) {
        ;(a._.x = a.z + a.parent.m), (a.m += a.parent.m)
      }
      function p(v, E, A) {
        if (E) {
          for (
            var o,
              D = v,
              x = v,
              C = E,
              w = D.parent.children[0],
              B = D.m,
              F = x.m,
              z = C.m,
              y = w.m;
            (C = eF(C)), (D = cX(D)), C && D;

          ) {
            ;(w = cX(w)),
              (x = eF(x)),
              (x.a = v),
              (o = C.z + z - D.z - B + d(C._, D._)),
              o > 0 && (fh(fX(C, v, A), v, o), (B += o), (F += o)),
              (z += C.m),
              (B += D.m),
              (y += w.m),
              (F += x.m)
          }
          C && !eF(x) && ((x.t = C), (x.m += z - F)),
            D && !cX(w) && ((w.t = D), (w.m += B - y), (A = v))
        }
        return A
      }
      function h(a) {
        ;(a.x *= g[0]), (a.y = a.depth * g[1])
      }
      var m = dU.layout
          .hierarchy()
          .sort(null)
          .value(null),
        d = hj,
        g = [1, 1],
        k = null
      return (
        (f.separation = function(a) {
          return arguments.length ? ((d = a), f) : d
        }),
        (f.size = function(a) {
          return arguments.length
            ? ((k = null == (g = a) ? h : null), f)
            : k
              ? null
              : g
        }),
        (f.nodeSize = function(a) {
          return arguments.length
            ? ((k = null == (g = a) ? null : h), f)
            : k
              ? g
              : null
        }),
        bm(f, m)
      )
    }),
    (dU.layout.cluster = function() {
      function f(j, m) {
        var u,
          e = b.call(this, j, m),
          k = e[0],
          t = 0
        jK(k, function(h) {
          var g = h.children
          g && g.length
            ? ((h.x = fB(g)), (h.y = eb(g)))
            : ((h.x = u ? (t += d(h, u)) : 0), (h.y = 0), (u = h))
        })
        var v = av(k),
          r = jR(k),
          p = v.x - d(v, r) / 2,
          q = r.x + d(r, v) / 2
        return (
          jK(
            k,
            a
              ? function(g) {
                  ;(g.x = (g.x - k.x) * c[0]), (g.y = (k.y - g.y) * c[1])
                }
              : function(g) {
                  ;(g.x = ((g.x - p) / (q - p)) * c[0]),
                    (g.y = (1 - (k.y ? g.y / k.y : 1)) * c[1])
                }
          ),
          e
        )
      }
      var b = dU.layout
          .hierarchy()
          .sort(null)
          .value(null),
        d = hj,
        c = [1, 1],
        a = !1
      return (
        (f.separation = function(e) {
          return arguments.length ? ((d = e), f) : d
        }),
        (f.size = function(e) {
          return arguments.length ? ((a = null == (c = e)), f) : a ? null : c
        }),
        (f.nodeSize = function(e) {
          return arguments.length ? ((a = null != (c = e)), f) : a ? c : null
        }),
        bm(f, b)
      )
    }),
    (dU.layout.treemap = function() {
      function k(l, f) {
        for (var h, g, a = -1, c = l.length; ++a < c; ) {
          ;(g = (h = l[a]).value * (0 > f ? 0 : f)),
            (h.area = isNaN(g) || 0 >= g ? 0 : g)
        }
      }
      function C(u) {
        var r = u.children
        if (r && r.length) {
          var F,
            g,
            n,
            E = x(u),
            H = [],
            t = r.slice(),
            f = 1 / 0,
            G =
              'slice' === w
                ? E.dx
                : 'dice' === w
                  ? E.dy
                  : 'slice-dice' === w
                    ? 1 & u.depth
                      ? E.dy
                      : E.dx
                    : Math.min(E.dx, E.dy)
          for (
            k(t, (E.dx * E.dy) / u.value), H.area = 0;
            (n = t.length) > 0;

          ) {
            H.push((F = t[n - 1])),
              (H.area += F.area),
              'squarify' !== w || (g = b(H, G)) <= f
                ? (t.pop(), (f = g))
                : ((H.area -= H.pop().area),
                  B(H, G, E, !1),
                  (G = Math.min(E.dx, E.dy)),
                  (H.length = H.area = 0),
                  (f = 1 / 0))
          }
          H.length && (B(H, G, E, !0), (H.length = H.area = 0)), r.forEach(C)
        }
      }
      function y(g) {
        var h = g.children
        if (h && h.length) {
          var f,
            e = x(g),
            n = h.slice(),
            c = []
          for (k(n, (e.dx * e.dy) / g.value), c.area = 0; (f = n.pop()); ) {
            c.push(f),
              (c.area += f.area),
              null != f.z &&
                (B(c, f.z ? e.dx : e.dy, e, !n.length), (c.length = c.area = 0))
          }
          h.forEach(y)
        }
      }
      function b(E, h) {
        for (
          var p, l = E.area, f = 0, g = 1 / 0, c = -1, s = E.length;
          ++c < s;

        ) {
          ;(p = E[c].area) && (g > p && (g = p), p > f && (f = p))
        }
        return (
          (l *= l),
          (h *= h),
          l ? Math.max((h * f * d) / l, l / (h * g * d)) : 1 / 0
        )
      }
      function B(h, H, p, f) {
        var G,
          l = -1,
          F = h.length,
          g = p.x,
          E = p.y,
          I = H ? m(h.area / H) : 0
        if (H == p.dx) {
          for ((f || I > p.dy) && (I = p.dy); ++l < F; ) {
            ;(G = h[l]),
              (G.x = g),
              (G.y = E),
              (G.dy = I),
              (g += G.dx = Math.min(p.x + p.dx - g, I ? m(G.area / I) : 0))
          }
          ;(G.z = !0), (G.dx += p.x + p.dx - g), (p.y += I), (p.dy -= I)
        } else {
          for ((f || I > p.dx) && (I = p.dx); ++l < F; ) {
            ;(G = h[l]),
              (G.x = g),
              (G.y = E),
              (G.dx = I),
              (E += G.dy = Math.min(p.y + p.dy - E, I ? m(G.area / I) : 0))
          }
          ;(G.z = !1), (G.dy += p.y + p.dy - E), (p.x += I), (p.dx -= I)
        }
      }
      function q(e) {
        var a = A || j(e),
          c = a[0]
        return (
          (c.x = c.y = 0),
          c.value ? ((c.dx = z[0]), (c.dy = z[1])) : (c.dx = c.dy = 0),
          A && j.revalue(c),
          k([c], (c.dx * c.dy) / c.value),
          (A ? y : C)(c),
          v && (A = a),
          a
        )
      }
      var A,
        j = dU.layout.hierarchy(),
        m = Math.round,
        z = [1, 1],
        D = null,
        x = gh,
        v = !1,
        w = 'squarify',
        d = 0.5 * (1 + Math.sqrt(5))
      return (
        (q.size = function(a) {
          return arguments.length ? ((z = a), q) : z
        }),
        (q.padding = function(g) {
          function a(h) {
            var i = g.call(q, h, h.depth)
            return null == i
              ? gh(h)
              : i4(h, 'number' == typeof i ? [i, i, i, i] : i)
          }
          function f(e) {
            return i4(e, g)
          }
          if (!arguments.length) {
            return D
          }
          var c
          return (
            (x =
              null == (D = g)
                ? gh
                : 'function' == (c = typeof g)
                  ? a
                  : 'number' === c
                    ? ((g = [g, g, g, g]), f)
                    : f),
            q
          )
        }),
        (q.round = function(a) {
          return arguments.length
            ? ((m = a ? Math.round : Number), q)
            : m != Number
        }),
        (q.sticky = function(a) {
          return arguments.length ? ((v = a), (A = null), q) : v
        }),
        (q.ratio = function(a) {
          return arguments.length ? ((d = a), q) : d
        }),
        (q.mode = function(a) {
          return arguments.length ? ((w = a + ''), q) : w
        }),
        bm(q, j)
      )
    }),
    (dU.random = {
      normal: function(c, a) {
        var b = arguments.length
        return (
          2 > b && (a = 1),
          1 > b && (c = 0),
          function() {
            var g, f, d
            do {
              ;(g = 2 * Math.random() - 1),
                (f = 2 * Math.random() - 1),
                (d = g * g + f * f)
            } while (!d || d > 1)
            return c + a * g * Math.sqrt((-2 * Math.log(d)) / d)
          }
        )
      },
      logNormal: function() {
        var a = dU.random.normal.apply(dU, arguments)
        return function() {
          return Math.exp(a())
        }
      },
      bates: function(b) {
        var a = dU.random.irwinHall(b)
        return function() {
          return a() / b
        }
      },
      irwinHall: function(a) {
        return function() {
          for (var b = 0, c = 0; a > c; c++) {
            b += Math.random()
          }
          return b
        }
      },
    }),
    (dU.scale = {})
  var e5 = { floor: hV, ceil: hV }
  dU.scale.linear = function() {
    return dq([0, 1], [0, 1], g1, !1)
  }
  var fj = { s: 1, g: 1, p: 1, r: 1, e: 1 }
  dU.scale.log = function() {
    return cN(dU.scale.linear().domain([0, 1]), 10, !0, [1, 10])
  }
  var gO = dU.format('.0e'),
    aY = {
      floor: function(a) {
        return -Math.ceil(-a)
      },
      ceil: function(a) {
        return -Math.floor(-a)
      },
    }
  ;(dU.scale.pow = function() {
    return bz(dU.scale.linear(), 1, [0, 1])
  }),
    (dU.scale.sqrt = function() {
      return dU.scale.pow().exponent(0.5)
    }),
    (dU.scale.ordinal = function() {
      return jX([], { t: 'range', a: [[]] })
    }),
    (dU.scale.category10 = function() {
      return dU.scale.ordinal().range(cz)
    }),
    (dU.scale.category20 = function() {
      return dU.scale.ordinal().range(bH)
    }),
    (dU.scale.category20b = function() {
      return dU.scale.ordinal().range(jL)
    }),
    (dU.scale.category20c = function() {
      return dU.scale.ordinal().range(hx)
    })
  var cz = [
      2062260,
      16744206,
      2924588,
      14034728,
      9725885,
      9197131,
      14907330,
      8355711,
      12369186,
      1556175,
    ].map(af),
    bH = [
      2062260,
      11454440,
      16744206,
      16759672,
      2924588,
      10018698,
      14034728,
      16750742,
      9725885,
      12955861,
      9197131,
      12885140,
      14907330,
      16234194,
      8355711,
      13092807,
      12369186,
      14408589,
      1556175,
      10410725,
    ].map(af),
    jL = [
      3750777,
      5395619,
      7040719,
      10264286,
      6519097,
      9216594,
      11915115,
      13556636,
      9202993,
      12426809,
      15186514,
      15190932,
      8666169,
      11356490,
      14049643,
      15177372,
      8077683,
      10834324,
      13528509,
      14589654,
    ].map(af),
    hx = [
      3244733,
      7057110,
      10406625,
      13032431,
      15095053,
      16616764,
      16625259,
      16634018,
      3253076,
      7652470,
      10607003,
      13101504,
      7695281,
      10394312,
      12369372,
      14342891,
      6513507,
      9868950,
      12434877,
      14277081,
    ].map(af)
  ;(dU.scale.quantile = function() {
    return dP([], [])
  }),
    (dU.scale.quantize = function() {
      return e1(0, 1, [0, 1])
    }),
    (dU.scale.threshold = function() {
      return iq([0.5], [0, 1])
    }),
    (dU.scale.identity = function() {
      return cf([0, 1])
    }),
    (dU.svg = {}),
    (dU.svg.arc = function() {
      function d() {
        var bl = Math.max(0, +h.apply(this, arguments)),
          ci = Math.max(0, +b.apply(this, arguments)),
          ba = j.apply(this, arguments) - gA,
          ca = c.apply(this, arguments) - gA,
          bt = Math.abs(ca - ba),
          bu = ba > ca ? 0 : 1
        if ((bl > ci && ((bi = ci), (ci = bl), (bl = bi)), bt >= j9)) {
          return m(ci, bu) + (bl ? m(bl, 1 - bu) : '') + 'Z'
        }
        var bi,
          au,
          ce,
          bn,
          ao,
          J,
          ar,
          cl,
          cn,
          at,
          r,
          bo,
          G = 0,
          aa = 0,
          al = []
        if (
          ((bn = (+f.apply(this, arguments) || 0) / 2) &&
            ((ce =
              g === c3
                ? Math.sqrt(bl * bl + ci * ci)
                : +g.apply(this, arguments)),
            bu || (aa *= -1),
            ci && (aa = bP((ce / ci) * Math.sin(bn))),
            bl && (G = bP((ce / bl) * Math.sin(bn)))),
          ci)
        ) {
          ;(ao = ci * Math.cos(ba + aa)),
            (J = ci * Math.sin(ba + aa)),
            (ar = ci * Math.cos(ca - aa)),
            (cl = ci * Math.sin(ca - aa))
          var ai = Math.abs(ca - ba - 2 * aa) <= fP ? 0 : 1
          if (aa && (eJ(ao, J, ar, cl) === bu) ^ ai) {
            var an = (ba + ca) / 2
            ;(ao = ci * Math.cos(an)), (J = ci * Math.sin(an)), (ar = cl = null)
          }
        } else {
          ao = J = 0
        }
        if (bl) {
          ;(cn = bl * Math.cos(ca - G)),
            (at = bl * Math.sin(ca - G)),
            (r = bl * Math.cos(ba + G)),
            (bo = bl * Math.sin(ba + G))
          var K = Math.abs(ba - ca + 2 * G) <= fP ? 0 : 1
          if (G && (eJ(cn, at, r, bo) === 1 - bu) ^ K) {
            var be = (ba + ca) / 2
            ;(cn = bl * Math.cos(be)), (at = bl * Math.sin(be)), (r = bo = null)
          }
        } else {
          cn = at = 0
        }
        if (
          bt > f9 &&
          (bi = Math.min(Math.abs(ci - bl) / 2, +k.apply(this, arguments))) >
            0.001
        ) {
          au = (ci > bl) ^ bu ? 0 : 1
          var o = bi,
            t = bi
          if (fP > bt) {
            var ae =
                null == r
                  ? [cn, at]
                  : null == ar
                    ? [ao, J]
                    : fz([ao, J], [r, bo], [ar, cl], [cn, at]),
              u = ao - ae[0],
              l = J - ae[1],
              br = ar - ae[0],
              X = cl - ae[1],
              W =
                1 /
                Math.sin(
                  Math.acos(
                    (u * br + l * X) /
                      (Math.sqrt(u * u + l * l) * Math.sqrt(br * br + X * X))
                  ) / 2
                ),
              B = Math.sqrt(ae[0] * ae[0] + ae[1] * ae[1])
            ;(t = Math.min(bi, (bl - B) / (W - 1))),
              (o = Math.min(bi, (ci - B) / (W + 1)))
          }
          if (null != ar) {
            var Q = kf(null == r ? [cn, at] : [r, bo], [ao, J], ci, o, bu),
              e = kf([ar, cl], [cn, at], ci, o, bu)
            bi === o
              ? al.push(
                  'M',
                  Q[0],
                  'A',
                  o,
                  ',',
                  o,
                  ' 0 0,',
                  au,
                  ' ',
                  Q[1],
                  'A',
                  ci,
                  ',',
                  ci,
                  ' 0 ',
                  (1 - bu) ^ eJ(Q[1][0], Q[1][1], e[1][0], e[1][1]),
                  ',',
                  bu,
                  ' ',
                  e[1],
                  'A',
                  o,
                  ',',
                  o,
                  ' 0 0,',
                  au,
                  ' ',
                  e[0]
                )
              : al.push('M', Q[0], 'A', o, ',', o, ' 0 1,', au, ' ', e[0])
          } else {
            al.push('M', ao, ',', J)
          }
          if (null != r) {
            var a = kf([ao, J], [r, bo], bl, -t, bu),
              i = kf([cn, at], null == ar ? [ao, J] : [ar, cl], bl, -t, bu)
            bi === t
              ? al.push(
                  'L',
                  i[0],
                  'A',
                  t,
                  ',',
                  t,
                  ' 0 0,',
                  au,
                  ' ',
                  i[1],
                  'A',
                  bl,
                  ',',
                  bl,
                  ' 0 ',
                  bu ^ eJ(i[1][0], i[1][1], a[1][0], a[1][1]),
                  ',',
                  1 - bu,
                  ' ',
                  a[1],
                  'A',
                  t,
                  ',',
                  t,
                  ' 0 0,',
                  au,
                  ' ',
                  a[0]
                )
              : al.push('L', i[0], 'A', t, ',', t, ' 0 0,', au, ' ', a[0])
          } else {
            al.push('L', cn, ',', at)
          }
        } else {
          al.push('M', ao, ',', J),
            null != ar &&
              al.push('A', ci, ',', ci, ' 0 ', ai, ',', bu, ' ', ar, ',', cl),
            al.push('L', cn, ',', at),
            null != r &&
              al.push('A', bl, ',', bl, ' 0 ', K, ',', 1 - bu, ' ', r, ',', bo)
        }
        return al.push('Z'), al.join('')
      }
      function m(e, a) {
        return (
          'M0,' +
          e +
          'A' +
          e +
          ',' +
          e +
          ' 0 1,' +
          a +
          ' 0,' +
          -e +
          'A' +
          e +
          ',' +
          e +
          ' 0 1,' +
          a +
          ' 0,' +
          e
        )
      }
      var h = gp,
        b = gK,
        k = g2,
        g = c3,
        j = dx,
        c = a8,
        f = hL
      return (
        (d.innerRadius = function(a) {
          return arguments.length ? ((h = aX(a)), d) : h
        }),
        (d.outerRadius = function(a) {
          return arguments.length ? ((b = aX(a)), d) : b
        }),
        (d.cornerRadius = function(a) {
          return arguments.length ? ((k = aX(a)), d) : k
        }),
        (d.padRadius = function(a) {
          return arguments.length ? ((g = a == c3 ? c3 : aX(a)), d) : g
        }),
        (d.startAngle = function(a) {
          return arguments.length ? ((j = aX(a)), d) : j
        }),
        (d.endAngle = function(a) {
          return arguments.length ? ((c = aX(a)), d) : c
        }),
        (d.padAngle = function(a) {
          return arguments.length ? ((f = aX(a)), d) : f
        }),
        (d.centroid = function() {
          var e = (+h.apply(this, arguments) + +b.apply(this, arguments)) / 2,
            a = (+j.apply(this, arguments) + +c.apply(this, arguments)) / 2 - gA
          return [Math.cos(a) * e, Math.sin(a) * e]
        }),
        d
      )
    })
  var c3 = 'auto'
  dU.svg.line = function() {
    return g8(hV)
  }
  var eN = dU.map({
    linear: am,
    'linear-closed': jd,
    step: aB,
    'step-before': aQ,
    'step-after': e7,
    basis: jO,
    'basis-open': hA,
    'basis-closed': c6,
    bundle: eP,
    cardinal: aZ,
    'cardinal-open': fp,
    'cardinal-closed': gQ,
    monotone: aH,
  })
  eN.forEach(function(b, a) {
    ;(a.key = b), (a.closed = /-closed$/.test(b))
  })
  var fw = [0, 2 / 3, 1 / 3, 0],
    bj = [0, 1 / 3, 2 / 3, 0],
    f5 = [0, 1 / 6, 2 / 3, 1 / 6]
  ;(dU.svg.line.radial = function() {
    var a = g8(j3)
    return (a.radius = a.x), delete a.x, (a.angle = a.y), delete a.y, a
  }),
    (aQ.reverse = e7),
    (e7.reverse = aQ),
    (dU.svg.area = function() {
      return gy(hV)
    }),
    (dU.svg.area.radial = function() {
      var a = gy(j3)
      return (
        (a.radius = a.x),
        delete a.x,
        (a.innerRadius = a.x0),
        delete a.x0,
        (a.outerRadius = a.x1),
        delete a.x1,
        (a.angle = a.y),
        delete a.y,
        (a.startAngle = a.y0),
        delete a.y0,
        (a.endAngle = a.y1),
        delete a.y1,
        a
      )
    }),
    (dU.svg.chord = function() {
      function f(r, e) {
        var a = q(this, h, r, e),
          i = q(this, m, r, e)
        return (
          'M' +
          a.p0 +
          b(a.r, a.p1, a.a1 - a.a0) +
          (j(a, i)
            ? p(a.r, a.p1, a.r, a.p0)
            : p(a.r, a.p1, i.r, i.p0) +
              b(i.r, i.p1, i.a1 - i.a0) +
              p(i.r, i.p1, a.r, a.p0)) +
          'Z'
        )
      }
      function q(z, v, y, x) {
        var l = v.call(z, y, x),
          o = d.call(z, l, x),
          c = g.call(z, l, x) - gA,
          w = k.call(z, l, x) - gA
        return {
          r: o,
          a0: c,
          a1: w,
          p0: [o * Math.cos(c), o * Math.sin(c)],
          p1: [o * Math.cos(w), o * Math.sin(w)],
        }
      }
      function j(c, a) {
        return c.a0 == a.a0 && c.a1 == a.a1
      }
      function b(i, a, c) {
        return 'A' + i + ',' + i + ' 0 ' + +(c > fP) + ',1 ' + a
      }
      function p(l, a, i, c) {
        return 'Q 0,0 ' + c
      }
      var h = g9,
        m = aw,
        d = jz,
        g = dx,
        k = a8
      return (
        (f.radius = function(a) {
          return arguments.length ? ((d = aX(a)), f) : d
        }),
        (f.source = function(a) {
          return arguments.length ? ((h = aX(a)), f) : h
        }),
        (f.target = function(a) {
          return arguments.length ? ((m = aX(a)), f) : m
        }),
        (f.startAngle = function(a) {
          return arguments.length ? ((g = aX(a)), f) : g
        }),
        (f.endAngle = function(a) {
          return arguments.length ? ((k = aX(a)), f) : k
        }),
        f
      )
    }),
    (dU.svg.diagonal = function() {
      function d(k, g) {
        var h = a.call(this, k, g),
          f = c.call(this, k, g),
          j = (h.y + f.y) / 2,
          e = [h, { x: h.x, y: j }, { x: f.x, y: j }, f]
        return (e = e.map(b)), 'M' + e[0] + 'C' + e[1] + ' ' + e[2] + ' ' + e[3]
      }
      var a = g9,
        c = aw,
        b = cU
      return (
        (d.source = function(f) {
          return arguments.length ? ((a = aX(f)), d) : a
        }),
        (d.target = function(e) {
          return arguments.length ? ((c = aX(e)), d) : c
        }),
        (d.projection = function(e) {
          return arguments.length ? ((b = e), d) : b
        }),
        d
      )
    }),
    (dU.svg.diagonal.radial = function() {
      var c = dU.svg.diagonal(),
        a = cU,
        b = c.projection
      return (
        (c.projection = function(d) {
          return arguments.length ? b(cv((a = d))) : a
        }),
        c
      )
    }),
    (dU.svg.symbol = function() {
      function c(e, d) {
        return (es.get(a.call(this, e, d)) || d7)(b.call(this, e, d))
      }
      var a = dd,
        b = dV
      return (
        (c.type = function(d) {
          return arguments.length ? ((a = aX(d)), c) : a
        }),
        (c.size = function(d) {
          return arguments.length ? ((b = aX(d)), c) : b
        }),
        c
      )
    })
  var es = dU.map({
    circle: d7,
    cross: function(b) {
      var a = Math.sqrt(b / 5) / 2
      return (
        'M' +
        -3 * a +
        ',' +
        -a +
        'H' +
        -a +
        'V' +
        -3 * a +
        'H' +
        a +
        'V' +
        -a +
        'H' +
        3 * a +
        'V' +
        a +
        'H' +
        a +
        'V' +
        3 * a +
        'H' +
        -a +
        'V' +
        a +
        'H' +
        -3 * a +
        'Z'
      )
    },
    diamond: function(c) {
      var a = Math.sqrt(c / (2 * aF)),
        b = a * aF
      return 'M0,' + -a + 'L' + b + ',0 0,' + a + ' ' + -b + ',0Z'
    },
    square: function(b) {
      var a = Math.sqrt(b) / 2
      return (
        'M' +
        -a +
        ',' +
        -a +
        'L' +
        a +
        ',' +
        -a +
        ' ' +
        a +
        ',' +
        a +
        ' ' +
        -a +
        ',' +
        a +
        'Z'
      )
    },
    'triangle-down': function(c) {
      var a = Math.sqrt(c / fH),
        b = (a * fH) / 2
      return 'M0,' + b + 'L' + a + ',' + -b + ' ' + -a + ',' + -b + 'Z'
    },
    'triangle-up': function(c) {
      var a = Math.sqrt(c / fH),
        b = (a * fH) / 2
      return 'M0,' + -b + 'L' + a + ',' + b + ' ' + -a + ',' + b + 'Z'
    },
  })
  dU.svg.symbolTypes = es.keys()
  var fH = Math.sqrt(3),
    aF = Math.tan(30 * jH)
  ;(cG.transition = function(g) {
    for (
      var w,
        m,
        b = j1 || ++cm,
        v = hR(g),
        j = [],
        q = gw || { time: Date.now(), ease: gJ, delay: 0, duration: 250 },
        d = -1,
        h = this.length;
      ++d < h;

    ) {
      j.push((w = []))
      for (var p = this[d], x = -1, k = p.length; ++x < k; ) {
        ;(m = p[x]) && fQ(m, x, v, b, q), w.push(m)
      }
    }
    return dD(j, v, b)
  }),
    (cG.interrupt = function(a) {
      return this.each(null == a ? js : b1(hR(a)))
    })
  var j1,
    gw,
    js = b1(hR()),
    cR = [],
    cm = 0
  ;(cR.call = cG.call),
    (cR.empty = cG.empty),
    (cR.node = cG.node),
    (cR.size = cG.size),
    (dU.transition = function(b, a) {
      return b && b.transition
        ? j1
          ? b.transition(a)
          : b
        : dU.selection().transition(b)
    }),
    (dU.transition.prototype = cR),
    (cR.select = function(g) {
      var w,
        m,
        b,
        v = this.id,
        j = this.namespace,
        q = []
      g = jC(g)
      for (var d = -1, h = this.length; ++d < h; ) {
        q.push((w = []))
        for (var p = this[d], x = -1, k = p.length; ++x < k; ) {
          ;(b = p[x]) && (m = g.call(b, b.__data__, x, d))
            ? ('__data__' in b && (m.__data__ = b.__data__),
              fQ(m, x, j, v, b[j][v]),
              w.push(m))
            : w.push(null)
        }
      }
      return dD(q, j, v)
    }),
    (cR.selectAll = function(k) {
      var E,
        z,
        b,
        D,
        q,
        B = this.id,
        j = this.namespace,
        m = []
      k = jA(k)
      for (var A = -1, F = this.length; ++A < F; ) {
        for (var y = this[A], w = -1, x = y.length; ++w < x; ) {
          if ((b = y[w])) {
            ;(q = b[j][B]), (z = k.call(b, b.__data__, w, A)), m.push((E = []))
            for (var d = -1, C = z.length; ++d < C; ) {
              ;(D = z[d]) && fQ(D, d, j, B, q), E.push(D)
            }
          }
        }
      }
      return dD(m, j, B)
    }),
    (cR.filter = function(d) {
      var m,
        h,
        b,
        k = []
      'function' != typeof d && (d = i5(d))
      for (var g = 0, j = this.length; j > g; g++) {
        k.push((m = []))
        for (var h = this[g], c = 0, f = h.length; f > c; c++) {
          ;(b = h[c]) && d.call(b, b.__data__, c, g) && m.push(b)
        }
      }
      return dD(k, this.namespace, this.id)
    }),
    (cR.tween = function(d, a) {
      var c = this.id,
        b = this.namespace
      return arguments.length < 2
        ? this.node()[b][c].tween.get(d)
        : iP(
            this,
            null == a
              ? function(e) {
                  e[b][c].tween.remove(d)
                }
              : function(e) {
                  e[b][c].tween.set(d, a)
                }
          )
    }),
    (cR.attr = function(k, f) {
      function h() {
        this.removeAttribute(j)
      }
      function g() {
        this.removeAttributeNS(j.space, j.local)
      }
      function c(a) {
        return null == a
          ? h
          : ((a += ''),
            function() {
              var i,
                l = this.getAttribute(j)
              return (
                l !== a &&
                ((i = b(l, a)),
                function(e) {
                  this.setAttribute(j, i(e))
                })
              )
            })
      }
      function d(a) {
        return null == a
          ? g
          : ((a += ''),
            function() {
              var i,
                l = this.getAttributeNS(j.space, j.local)
              return (
                l !== a &&
                ((i = b(l, a)),
                function(e) {
                  this.setAttributeNS(j.space, j.local, i(e))
                })
              )
            })
      }
      if (arguments.length < 2) {
        for (f in k) {
          this.attr(f, k[f])
        }
        return this
      }
      var b = 'transform' == k ? dZ : g1,
        j = dU.ns.qualify(k)
      return iI(this, 'attr.' + k, f, j.local ? d : c)
    }),
    (cR.attrTween = function(f, b) {
      function d(i, h) {
        var g = b.call(this, i, h, this.getAttribute(a))
        return (
          g &&
          function(e) {
            this.setAttribute(a, g(e))
          }
        )
      }
      function c(i, h) {
        var g = b.call(this, i, h, this.getAttributeNS(a.space, a.local))
        return (
          g &&
          function(e) {
            this.setAttributeNS(a.space, a.local, g(e))
          }
        )
      }
      var a = dU.ns.qualify(f)
      return this.tween('attr.' + f, a.local ? c : d)
    }),
    (cR.style = function(h, g, f) {
      function c() {
        this.style.removeProperty(h)
      }
      function d(a) {
        return null == a
          ? c
          : ((a += ''),
            function() {
              var e,
                j = h2(this)
                  .getComputedStyle(this, null)
                  .getPropertyValue(h)
              return (
                j !== a &&
                ((e = g1(j, a)),
                function(i) {
                  this.style.setProperty(h, e(i), f)
                })
              )
            })
      }
      var b = arguments.length
      if (3 > b) {
        if ('string' != typeof h) {
          2 > b && (g = '')
          for (f in h) {
            this.style(f, h[f], g)
          }
          return this
        }
        f = ''
      }
      return iI(this, 'style.' + h, g, d)
    }),
    (cR.styleTween = function(d, c, b) {
      function a(f, g) {
        var e = c.call(
          this,
          f,
          g,
          h2(this)
            .getComputedStyle(this, null)
            .getPropertyValue(d)
        )
        return (
          e &&
          function(h) {
            this.style.setProperty(d, e(h), b)
          }
        )
      }
      return arguments.length < 3 && (b = ''), this.tween('style.' + d, a)
    }),
    (cR.text = function(a) {
      return iI(this, 'text', a, kv)
    }),
    (cR.remove = function() {
      var a = this.namespace
      return this.each('end.transition', function() {
        var b
        this[a].count < 2 && (b = this.parentNode) && b.removeChild(this)
      })
    }),
    (cR.ease = function(c) {
      var a = this.id,
        b = this.namespace
      return arguments.length < 1
        ? this.node()[b][a].ease
        : ('function' != typeof c && (c = dU.ease.apply(dU, arguments)),
          iP(this, function(d) {
            d[b][a].ease = c
          }))
    }),
    (cR.delay = function(c) {
      var a = this.id,
        b = this.namespace
      return arguments.length < 1
        ? this.node()[b][a].delay
        : iP(
            this,
            'function' == typeof c
              ? function(f, d, e) {
                  f[b][a].delay = +c.call(f, f.__data__, d, e)
                }
              : ((c = +c),
                function(d) {
                  d[b][a].delay = c
                })
          )
    }),
    (cR.duration = function(c) {
      var a = this.id,
        b = this.namespace
      return arguments.length < 1
        ? this.node()[b][a].duration
        : iP(
            this,
            'function' == typeof c
              ? function(f, d, e) {
                  f[b][a].duration = Math.max(1, c.call(f, f.__data__, d, e))
                }
              : ((c = Math.max(1, c)),
                function(d) {
                  d[b][a].duration = c
                })
          )
    }),
    (cR.each = function(g, c) {
      var f = this.id,
        d = this.namespace
      if (arguments.length < 2) {
        var a = gw,
          b = j1
        try {
          ;(j1 = f),
            iP(this, function(j, e, h) {
              ;(gw = j[d][f]), g.call(j, j.__data__, e, h)
            })
        } finally {
          ;(gw = a), (j1 = b)
        }
      } else {
        iP(this, function(e) {
          var h = e[d][f]
          ;(h.event || (h.event = dU.dispatch('start', 'end', 'interrupt'))).on(
            g,
            c
          )
        })
      }
      return this
    }),
    (cR.transition = function() {
      for (
        var g,
          w,
          m,
          b,
          v = this.id,
          j = ++cm,
          q = this.namespace,
          d = [],
          h = 0,
          p = this.length;
        p > h;
        h++
      ) {
        d.push((g = []))
        for (var w = this[h], x = 0, k = w.length; k > x; x++) {
          ;(m = w[x]) &&
            ((b = m[q][v]),
            fQ(m, x, q, j, {
              time: b.time,
              ease: b.ease,
              delay: b.delay + b.duration,
              duration: b.duration,
            })),
            g.push(m)
        }
      }
      return dD(d, q, j)
    }),
    (dU.svg.axis = function() {
      function d(a) {
        a.each(function() {
          var O,
            Y = dU.select(this),
            G = this.__chart__ || h,
            W = (this.__chart__ = h.copy()),
            U = null == f ? (W.ticks ? W.ticks.apply(W, c) : W.domain()) : f,
            V = null == m ? (W.tickFormat ? W.tickFormat.apply(W, c) : hV) : m,
            K = Y.selectAll('.tick').data(U, W),
            D = K.enter()
              .insert('g', '.domain')
              .attr('class', 'tick')
              .style('opacity', f9),
            X = dU
              .transition(K.exit())
              .style('opacity', f9)
              .remove(),
            P = dU.transition(K.order()).style('opacity', 1),
            r = Math.max(k, 0) + j,
            y = b9(W),
            Z = Y.selectAll('.domain').data([0]),
            aa = (Z.enter()
              .append('path')
              .attr('class', 'domain'),
            dU.transition(Z))
          D.append('line'), D.append('text')
          var B,
            i,
            Q,
            o,
            F = D.select('line'),
            J = P.select('line'),
            H = K.select('text').text(V),
            u = D.select('text'),
            t = P.select('text'),
            I = 'top' === b || 'left' === b ? -1 : 1
          if (
            ('bottom' === b || 'top' === b
              ? ((O = ew),
                (B = 'x'),
                (Q = 'y'),
                (i = 'x2'),
                (o = 'y2'),
                H.attr('dy', 0 > I ? '0em' : '.71em').style(
                  'text-anchor',
                  'middle'
                ),
                aa.attr(
                  'd',
                  'M' + y[0] + ',' + I * g + 'V0H' + y[1] + 'V' + I * g
                ))
              : ((O = bX),
                (B = 'y'),
                (Q = 'x'),
                (i = 'y2'),
                (o = 'x2'),
                H.attr('dy', '.32em').style(
                  'text-anchor',
                  0 > I ? 'end' : 'start'
                ),
                aa.attr(
                  'd',
                  'M' + I * g + ',' + y[0] + 'H0V' + y[1] + 'H' + I * g
                )),
            F.attr(o, I * k),
            u.attr(Q, I * r),
            J.attr(i, 0).attr(o, I * k),
            t.attr(B, 0).attr(Q, I * r),
            W.rangeBand)
          ) {
            var e = W,
              l = e.rangeBand() / 2
            G = W = function(p) {
              return e(p) + l
            }
          } else {
            G.rangeBand ? (G = W) : X.call(O, W, G)
          }
          D.call(O, G, W), P.call(O, W, W)
        })
      }
      var m,
        h = dU.scale.linear(),
        b = dT,
        k = 6,
        g = 6,
        j = 3,
        c = [10],
        f = null
      return (
        (d.scale = function(a) {
          return arguments.length ? ((h = a), d) : h
        }),
        (d.orient = function(a) {
          return arguments.length ? ((b = a in db ? a + '' : dT), d) : b
        }),
        (d.ticks = function() {
          return arguments.length ? ((c = iH(arguments)), d) : c
        }),
        (d.tickValues = function(a) {
          return arguments.length ? ((f = a), d) : f
        }),
        (d.tickFormat = function(a) {
          return arguments.length ? ((m = a), d) : m
        }),
        (d.tickSize = function(a) {
          var i = arguments.length
          return i ? ((k = +a), (g = +arguments[i - 1]), d) : k
        }),
        (d.innerTickSize = function(a) {
          return arguments.length ? ((k = +a), d) : k
        }),
        (d.outerTickSize = function(a) {
          return arguments.length ? ((g = +a), d) : g
        }),
        (d.tickPadding = function(a) {
          return arguments.length ? ((j = +a), d) : j
        }),
        (d.tickSubdivide = function() {
          return arguments.length && d
        }),
        d
      )
    })
  var dT = 'bottom',
    db = { top: 1, right: 1, bottom: 1, left: 1 }
  dU.svg.brush = function() {
    function k(a) {
      a.each(function() {
        var g = dU
            .select(this)
            .style('pointer-events', 'all')
            .style('-webkit-tap-highlight-color', 'rgba(0,0,0,0)')
            .on('mousedown.brush', q)
            .on('touchstart.brush', q),
          e = g.selectAll('.background').data([0])
        e
          .enter()
          .append('rect')
          .attr('class', 'background')
          .style('visibility', 'hidden')
          .style('cursor', 'crosshair'),
          g
            .selectAll('.extent')
            .data([0])
            .enter()
            .append('rect')
            .attr('class', 'extent')
            .style('cursor', 'move')
        var p = g.selectAll('.resize').data(B, hV)
        p.exit().remove(),
          p
            .enter()
            .append('g')
            .attr('class', function(f) {
              return 'resize ' + f
            })
            .style('cursor', function(f) {
              return d5[f]
            })
            .append('rect')
            .attr('x', function(f) {
              return /[ew]$/.test(f) ? -3 : null
            })
            .attr('y', function(f) {
              return /^[ns]/.test(f) ? -3 : null
            })
            .attr('width', 6)
            .attr('height', 6)
            .style('visibility', 'hidden'),
          p.style('display', k.empty() ? 'none' : null)
        var c,
          n = dU.transition(g),
          i = dU.transition(e)
        z && ((c = b9(z)), i.attr('x', c[0]).attr('width', c[1] - c[0]), b(n)),
          D &&
            ((c = b9(D)), i.attr('y', c[0]).attr('height', c[1] - c[0]), C(n)),
          y(n)
      })
    }
    function y(a) {
      a.selectAll('.resize').attr('transform', function(c) {
        return 'translate(' + x[+/e$/.test(c)] + ',' + t[+/^s/.test(c)] + ')'
      })
    }
    function b(a) {
      a.select('.extent').attr('x', x[0]),
        a.selectAll('.extent,.n>rect,.s>rect').attr('width', x[1] - x[0])
    }
    function C(a) {
      a.select('.extent').attr('y', t[0]),
        a.selectAll('.extent,.e>rect,.w>rect').attr('height', t[1] - t[0])
    }
    function q() {
      function I() {
        32 == dU.event.keyCode &&
          (s || ((f = null), (g[0] -= x[1]), (g[1] -= t[1]), (s = 2)), iY())
      }
      function p() {
        32 == dU.event.keyCode &&
          2 == s &&
          ((g[0] += x[1]), (g[1] += t[1]), (s = 0), iY())
      }
      function J() {
        var v = dU.mouse(K),
          i = !1
        n && ((v[0] += n[0]), (v[1] += n[1])),
          s ||
            (dU.event.altKey
              ? (f || (f = [(x[0] + x[1]) / 2, (t[0] + t[1]) / 2]),
                (g[0] = x[+(v[0] < f[0])]),
                (g[1] = t[+(v[1] < f[1])]))
              : (f = null)),
          r && G(v, z, 0) && (b(H), (i = !0)),
          F && G(v, D, 1) && (C(H), (i = !0)),
          i && (y(H), o({ type: 'brush', mode: s ? 'move' : 'resize' }))
      }
      function G(L, U, P) {
        var E,
          T,
          N = b9(U),
          M = N[0],
          R = N[1],
          V = g[P],
          S = P ? t : x,
          Q = S[1] - S[0]
        return (
          s && ((M -= V), (R -= Q + V)),
          (E = (P ? d : w) ? Math.max(M, Math.min(R, L[P])) : L[P]),
          s
            ? (T = (E += V) + Q)
            : (f && (V = Math.max(M, Math.min(R, 2 * f[P] - E))),
              E > V ? ((T = E), (E = V)) : (T = V)),
          S[0] != E || S[1] != T
            ? (P ? (j = null) : (A = null), (S[0] = E), (S[1] = T), !0)
            : void 0
        )
      }
      function l() {
        J(),
          H.style('pointer-events', 'all')
            .selectAll('.resize')
            .style('display', k.empty() ? 'none' : null),
          dU.select('body').style('cursor', null),
          u
            .on('mousemove.brush', null)
            .on('mouseup.brush', null)
            .on('touchmove.brush', null)
            .on('touchend.brush', null)
            .on('keydown.brush', null)
            .on('keyup.brush', null),
          h(),
          o({ type: 'brushend' })
      }
      var f,
        n,
        K = this,
        O = dU.select(dU.event.target),
        o = m.of(K, arguments),
        H = dU.select(K),
        e = O.datum(),
        r = !/^(n|s)$/.test(e) && z,
        F = !/^(e|w)$/.test(e) && D,
        s = O.classed('extent'),
        h = iS(K),
        g = dU.mouse(K),
        u = dU
          .select(h2(K))
          .on('keydown.brush', I)
          .on('keyup.brush', p)
      if (
        (dU.event.changedTouches
          ? u.on('touchmove.brush', J).on('touchend.brush', l)
          : u.on('mousemove.brush', J).on('mouseup.brush', l),
        H.interrupt()
          .selectAll('*')
          .interrupt(),
        s)
      ) {
        ;(g[0] = x[0] - g[0]), (g[1] = t[0] - g[1])
      } else {
        if (e) {
          var a = +/w$/.test(e),
            c = +/^n/.test(e)
          ;(n = [x[1 - a] - g[0], t[1 - c] - g[1]]),
            (g[0] = x[a]),
            (g[1] = t[c])
        } else {
          dU.event.altKey && (f = g.slice())
        }
      }
      H.style('pointer-events', 'none')
        .selectAll('.resize')
        .style('display', null),
        dU.select('body').style('cursor', O.style('cursor')),
        o({ type: 'brushstart' }),
        J()
    }
    var A,
      j,
      m = i7(k, 'brushstart', 'brush', 'brushend'),
      z = null,
      D = null,
      x = [0, 0],
      t = [0, 0],
      w = !0,
      d = !0,
      B = b0[0]
    return (
      (k.event = function(a) {
        a.each(function() {
          var g = m.of(this, arguments),
            c = { x: x, y: t, i: A, j: j },
            f = this.__chart__ || c
          ;(this.__chart__ = c),
            j1
              ? dU
                  .select(this)
                  .transition()
                  .each('start.brush', function() {
                    ;(A = f.i),
                      (j = f.j),
                      (x = f.x),
                      (t = f.y),
                      g({ type: 'brushstart' })
                  })
                  .tween('brush:brush', function() {
                    var i = ab(x, c.x),
                      h = ab(t, c.y)
                    return (
                      (A = j = null),
                      function(e) {
                        ;(x = c.x = i(e)),
                          (t = c.y = h(e)),
                          g({ type: 'brush', mode: 'resize' })
                      }
                    )
                  })
                  .each('end.brush', function() {
                    ;(A = c.i),
                      (j = c.j),
                      g({ type: 'brush', mode: 'resize' }),
                      g({ type: 'brushend' })
                  })
              : (g({ type: 'brushstart' }),
                g({ type: 'brush', mode: 'resize' }),
                g({ type: 'brushend' }))
        })
      }),
      (k.x = function(a) {
        return arguments.length ? ((z = a), (B = b0[(!z << 1) | !D]), k) : z
      }),
      (k.y = function(a) {
        return arguments.length ? ((D = a), (B = b0[(!z << 1) | !D]), k) : D
      }),
      (k.clamp = function(a) {
        return arguments.length
          ? (z && D
              ? ((w = !!a[0]), (d = !!a[1]))
              : z
                ? (w = !!a)
                : D && (d = !!a),
            k)
          : z && D
            ? [w, d]
            : z
              ? w
              : D
                ? d
                : null
      }),
      (k.extent = function(g) {
        var n, h, c, f, a
        return arguments.length
          ? (z &&
              ((n = g[0]),
              (h = g[1]),
              D && ((n = n[0]), (h = h[0])),
              (A = [n, h]),
              z.invert && ((n = z(n)), (h = z(h))),
              n > h && ((a = n), (n = h), (h = a)),
              (n != x[0] || h != x[1]) && (x = [n, h])),
            D &&
              ((c = g[0]),
              (f = g[1]),
              z && ((c = c[1]), (f = f[1])),
              (j = [c, f]),
              D.invert && ((c = D(c)), (f = D(f))),
              c > f && ((a = c), (c = f), (f = a)),
              (c != t[0] || f != t[1]) && (t = [c, f])),
            k)
          : (z &&
              (A
                ? ((n = A[0]), (h = A[1]))
                : ((n = x[0]),
                  (h = x[1]),
                  z.invert && ((n = z.invert(n)), (h = z.invert(h))),
                  n > h && ((a = n), (n = h), (h = a)))),
            D &&
              (j
                ? ((c = j[0]), (f = j[1]))
                : ((c = t[0]),
                  (f = t[1]),
                  D.invert && ((c = D.invert(c)), (f = D.invert(f))),
                  c > f && ((a = c), (c = f), (f = a)))),
            z && D ? [[n, c], [h, f]] : z ? [n, h] : D && [c, f])
      }),
      (k.clear = function() {
        return k.empty() || ((x = [0, 0]), (t = [0, 0]), (A = j = null)), k
      }),
      (k.empty = function() {
        return (!!z && x[0] == x[1]) || (!!D && t[0] == t[1])
      }),
      dU.rebind(k, m, 'on')
    )
  }
  var d5 = {
      n: 'ns-resize',
      e: 'ew-resize',
      s: 'ns-resize',
      w: 'ew-resize',
      nw: 'nwse-resize',
      ne: 'nesw-resize',
      se: 'nwse-resize',
      sw: 'nesw-resize',
    },
    b0 = [
      ['n', 'e', 's', 'w', 'nw', 'ne', 'se', 'sw'],
      ['e', 'w'],
      ['n', 's'],
      [],
    ],
    dB = (gE.format = ad.timeFormat),
    iD = dB.utc,
    ks = iD('%Y-%m-%dT%H:%M:%S.%LZ')
  ;(dB.iso =
    Date.prototype.toISOString && +new Date('2000-01-01T00:00:00.000Z')
      ? hz
      : ks),
    (hz.parse = function(b) {
      var a = new Date(b)
      return isNaN(a) ? null : a
    }),
    (hz.toString = ks.toString),
    (gE.second = gs(
      function(a) {
        return new dm(1000 * Math.floor(a / 1000))
      },
      function(b, a) {
        b.setTime(b.getTime() + 1000 * Math.floor(a))
      },
      function(a) {
        return a.getSeconds()
      }
    )),
    (gE.seconds = gE.second.range),
    (gE.seconds.utc = gE.second.utc.range),
    (gE.minute = gs(
      function(a) {
        return new dm(60000 * Math.floor(a / 60000))
      },
      function(b, a) {
        b.setTime(b.getTime() + 60000 * Math.floor(a))
      },
      function(a) {
        return a.getMinutes()
      }
    )),
    (gE.minutes = gE.minute.range),
    (gE.minutes.utc = gE.minute.utc.range),
    (gE.hour = gs(
      function(b) {
        var a = b.getTimezoneOffset() / 60
        return new dm(3600000 * (Math.floor(b / 3600000 - a) + a))
      },
      function(b, a) {
        b.setTime(b.getTime() + 3600000 * Math.floor(a))
      },
      function(a) {
        return a.getHours()
      }
    )),
    (gE.hours = gE.hour.range),
    (gE.hours.utc = gE.hour.utc.range),
    (gE.month = gs(
      function(a) {
        return (a = gE.day(a)), a.setDate(1), a
      },
      function(b, a) {
        b.setMonth(b.getMonth() + a)
      },
      function(a) {
        return a.getMonth()
      }
    )),
    (gE.months = gE.month.range),
    (gE.months.utc = gE.month.utc.range)
  var hP = [
      1000,
      5000,
      15000,
      30000,
      60000,
      300000,
      900000,
      1800000,
      3600000,
      10800000,
      21600000,
      43200000,
      86400000,
      172800000,
      604800000,
      2592000000,
      7776000000,
      31536000000,
    ],
    fO = [
      [gE.second, 1],
      [gE.second, 5],
      [gE.second, 15],
      [gE.second, 30],
      [gE.minute, 1],
      [gE.minute, 5],
      [gE.minute, 15],
      [gE.minute, 30],
      [gE.hour, 1],
      [gE.hour, 3],
      [gE.hour, 6],
      [gE.hour, 12],
      [gE.day, 1],
      [gE.day, 2],
      [gE.week, 1],
      [gE.month, 1],
      [gE.month, 3],
      [gE.year, 1],
    ],
    ev = dB.multi([
      [
        '.%L',
        function(a) {
          return a.getMilliseconds()
        },
      ],
      [
        ':%S',
        function(a) {
          return a.getSeconds()
        },
      ],
      [
        '%I:%M',
        function(a) {
          return a.getMinutes()
        },
      ],
      [
        '%I %p',
        function(a) {
          return a.getHours()
        },
      ],
      [
        '%a %d',
        function(a) {
          return a.getDay() && 1 != a.getDate()
        },
      ],
      [
        '%b %d',
        function(a) {
          return 1 != a.getDate()
        },
      ],
      [
        '%B',
        function(a) {
          return a.getMonth()
        },
      ],
      ['%Y', jF],
    ]),
    bV = {
      range: function(c, a, b) {
        return dU.range(Math.ceil(c / b) * b, +a, b).map(bE)
      },
      floor: hV,
      ceil: hV,
    }
  ;(fO.year = gE.year),
    (gE.scale = function() {
      return cT(dU.scale.linear(), fO, ev)
    })
  var hy = fO.map(function(a) {
      return [a[0].utc, a[1]]
    }),
    cS = iD.multi([
      [
        '.%L',
        function(a) {
          return a.getUTCMilliseconds()
        },
      ],
      [
        ':%S',
        function(a) {
          return a.getUTCSeconds()
        },
      ],
      [
        '%I:%M',
        function(a) {
          return a.getUTCMinutes()
        },
      ],
      [
        '%I %p',
        function(a) {
          return a.getUTCHours()
        },
      ],
      [
        '%a %d',
        function(a) {
          return a.getUTCDay() && 1 != a.getUTCDate()
        },
      ],
      [
        '%b %d',
        function(a) {
          return 1 != a.getUTCDate()
        },
      ],
      [
        '%B',
        function(a) {
          return a.getUTCMonth()
        },
      ],
      ['%Y', jF],
    ])
  ;(hy.year = gE.year.utc),
    (gE.scale.utc = function() {
      return cT(dU.scale.linear(), hy, cS)
    }),
    (dU.text = cx(function(a) {
      return a.responseText
    })),
    (dU.json = function(b, a) {
      return bG(b, 'application/json', f6, a)
    }),
    (dU.html = function(b, a) {
      return bG(b, 'text/html', j2, a)
    }),
    (dU.xml = cx(function(a) {
      return a.responseXML
    })),
    'function' == typeof define && define.amd
      ? ((this.d3 = dU), define(dU))
      : 'object' == typeof module && module.exports
        ? (module.exports = dU)
        : (this.d3 = dU)
})()
